// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/spec-mirror-typesafe-ai). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  TypesafeAiProtocol,
  type TypesafeAiOpError,
  type TypesafeAiOpContext,
} from "../protocol.ts";
import { UnknownTypesafeAiError } from "../errors.ts";
import * as Retry from "../retry.ts";

export type { TypesafeAiOpError, TypesafeAiOpContext };

export class UnprocessableEntity
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<UnprocessableEntity>()("UnprocessableEntity", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 422 }],
  ) {}

export interface ListModelsRequest {}
export const ListModelsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}).pipe(T.Http({ method: "GET", uri: "/v1/models", code: 200 })),
).annotate({
  identifier: "ListModelsRequest",
}) as any as S.Schema<ListModelsRequest>;

/** A model or model alias available to the authenticated account. */
export interface ModelMetadata {
  /** Model name or alias accepted by the request's model field. */
  name: string;
  /** Human-readable description of the model and its capabilities. */
  description: string;
  /** Model release date, formatted as YYYY-MM-DD. */
  release_date: string;
}
export const ModelMetadata = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String,
    description: S.String,
    release_date: S.String,
  }),
).annotate({ identifier: "ModelMetadata" }) as any as S.Schema<ModelMetadata>;

/** Available models and aliases. Use a model's name in POST /v1/systemone requests. */
export type ModelMetadataListModelsList = Array<ModelMetadata>;
export const ModelMetadataListModelsList = /*@__PURE__*/ S.Array(
  ModelMetadata,
) as any as S.Schema<ModelMetadataListModelsList>;

/** Models and aliases available to the authenticated account. */
export interface ModelMetadataList {
  /** Available models and aliases. Use a model's name in POST /v1/systemone requests. */
  models: ModelMetadataListModelsList;
}
export const ModelMetadataList = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    models: ModelMetadataListModelsList,
  }),
).annotate({
  identifier: "ModelMetadataList",
}) as any as S.Schema<ModelMetadataList>;

export type SystemOneRequestStateCase1Map = {
  [key: string]: unknown | undefined;
};
export const SystemOneRequestStateCase1Map = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<SystemOneRequestStateCase1Map>;

export type SystemOneRequestStateCase2List = Array<unknown>;
export const SystemOneRequestStateCase2List = /*@__PURE__*/ S.Array(
  S.Unknown,
) as any as S.Schema<SystemOneRequestStateCase2List>;

/** The content all questions in this request refer to. */
export type SystemOneRequestState =
  | string
  | SystemOneRequestStateCase1Map
  | SystemOneRequestStateCase2List;
export const SystemOneRequestState =
  /*@__PURE__*/ S.Unknown as any as S.Schema<SystemOneRequestState>;

export type NoulQuestionInstructionsCase1Map = {
  [key: string]: unknown | undefined;
};
export const NoulQuestionInstructionsCase1Map = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<NoulQuestionInstructionsCase1Map>;

export type NoulQuestionInstructionsCase2List = Array<unknown>;
export const NoulQuestionInstructionsCase2List = /*@__PURE__*/ S.Array(
  S.Unknown,
) as any as S.Schema<NoulQuestionInstructionsCase2List>;

/** The yes/no question or statement to evaluate. */
export type NoulQuestionInstructions =
  | string
  | NoulQuestionInstructionsCase1Map
  | NoulQuestionInstructionsCase2List;
export const NoulQuestionInstructions =
  /*@__PURE__*/ S.Unknown as any as S.Schema<NoulQuestionInstructions>;

export type NoulCriteriaTrueCase1Map = { [key: string]: unknown | undefined };
export const NoulCriteriaTrueCase1Map = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<NoulCriteriaTrueCase1Map>;

export type NoulCriteriaTrueCase2List = Array<unknown>;
export const NoulCriteriaTrueCase2List = /*@__PURE__*/ S.Array(
  S.Unknown,
) as any as S.Schema<NoulCriteriaTrueCase2List>;

/** What counts as a yes answer. */
export type NoulCriteriaTrue =
  | string
  | NoulCriteriaTrueCase1Map
  | NoulCriteriaTrueCase2List;
export const NoulCriteriaTrue =
  /*@__PURE__*/ S.Unknown as any as S.Schema<NoulCriteriaTrue>;

export type NoulCriteriaFalseCase1Map = { [key: string]: unknown | undefined };
export const NoulCriteriaFalseCase1Map = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<NoulCriteriaFalseCase1Map>;

export type NoulCriteriaFalseCase2List = Array<unknown>;
export const NoulCriteriaFalseCase2List = /*@__PURE__*/ S.Array(
  S.Unknown,
) as any as S.Schema<NoulCriteriaFalseCase2List>;

/** What counts as a no answer. */
export type NoulCriteriaFalse =
  | string
  | NoulCriteriaFalseCase1Map
  | NoulCriteriaFalseCase2List;
export const NoulCriteriaFalse =
  /*@__PURE__*/ S.Unknown as any as S.Schema<NoulCriteriaFalse>;

/** Criteria defining what counts as a yes or no answer. */
export interface NoulCriteria {
  /** What counts as a yes answer. */
  true?: NoulCriteriaTrue | null;
  /** What counts as a no answer. */
  false?: NoulCriteriaFalse | null;
}
export const NoulCriteria = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    true: S.optional(S.NullOr(NoulCriteriaTrue)),
    false: S.optional(S.NullOr(NoulCriteriaFalse)),
  }),
).annotate({ identifier: "NoulCriteria" }) as any as S.Schema<NoulCriteria>;

/** A yes/no question or statement, answered with the probability of yes or true. */
export interface NoulQuestion {
  /** Identifies a yes/no question or statement. */
  type: string;
  /** The yes/no question or statement to evaluate. */
  instructions?: NoulQuestionInstructions | null;
  /** Criteria clarifying what counts as a yes or no answer. */
  criteria?: NoulCriteria | null;
}
export const NoulQuestion = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    type: S.String,
    instructions: S.optional(S.NullOr(NoulQuestionInstructions)),
    criteria: S.optional(S.NullOr(NoulCriteria)),
  }),
).annotate({ identifier: "NoulQuestion" }) as any as S.Schema<NoulQuestion>;

export type ChoiceQuestionInstructionsCase1Map = {
  [key: string]: unknown | undefined;
};
export const ChoiceQuestionInstructionsCase1Map = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<ChoiceQuestionInstructionsCase1Map>;

export type ChoiceQuestionInstructionsCase2List = Array<unknown>;
export const ChoiceQuestionInstructionsCase2List = /*@__PURE__*/ S.Array(
  S.Unknown,
) as any as S.Schema<ChoiceQuestionInstructionsCase2List>;

/** What the model should decide when choosing an option. */
export type ChoiceQuestionInstructions =
  | string
  | ChoiceQuestionInstructionsCase1Map
  | ChoiceQuestionInstructionsCase2List;
export const ChoiceQuestionInstructions =
  /*@__PURE__*/ S.Unknown as any as S.Schema<ChoiceQuestionInstructions>;

export type ChoiceQuestionCriteriaValueCase1Map = {
  [key: string]: unknown | undefined;
};
export const ChoiceQuestionCriteriaValueCase1Map = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<ChoiceQuestionCriteriaValueCase1Map>;

export type ChoiceQuestionCriteriaValueCase2List = Array<unknown>;
export const ChoiceQuestionCriteriaValueCase2List = /*@__PURE__*/ S.Array(
  S.Unknown,
) as any as S.Schema<ChoiceQuestionCriteriaValueCase2List>;

export type ChoiceQuestionCriteriaValue =
  | string
  | ChoiceQuestionCriteriaValueCase1Map
  | ChoiceQuestionCriteriaValueCase2List;
export const ChoiceQuestionCriteriaValue =
  /*@__PURE__*/ S.Unknown as any as S.Schema<ChoiceQuestionCriteriaValue>;

/** Choice names and descriptions of when each applies. A choice without a description is interpreted by its name alone. */
export type ChoiceQuestionCriteriaMap = {
  [key: string]: ChoiceQuestionCriteriaValue | null | undefined;
};
export const ChoiceQuestionCriteriaMap = /*@__PURE__*/ S.Record(
  S.String,
  S.NullOr(ChoiceQuestionCriteriaValue),
) as any as S.Schema<ChoiceQuestionCriteriaMap>;

/** A question that selects one option from the choices you define. */
export interface ChoiceQuestion {
  /** Identifies a question that selects one of the choices in criteria. */
  type: string;
  /** What the model should decide when choosing an option. */
  instructions?: ChoiceQuestionInstructions | null;
  /** Choice names and descriptions of when each applies. A choice without a description is interpreted by its name alone. */
  criteria: ChoiceQuestionCriteriaMap;
}
export const ChoiceQuestion = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    type: S.String,
    instructions: S.optional(S.NullOr(ChoiceQuestionInstructions)),
    criteria: ChoiceQuestionCriteriaMap,
  }),
).annotate({ identifier: "ChoiceQuestion" }) as any as S.Schema<ChoiceQuestion>;

export type ScoreQuestionInstructionsCase1Map = {
  [key: string]: unknown | undefined;
};
export const ScoreQuestionInstructionsCase1Map = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<ScoreQuestionInstructionsCase1Map>;

export type ScoreQuestionInstructionsCase2List = Array<unknown>;
export const ScoreQuestionInstructionsCase2List = /*@__PURE__*/ S.Array(
  S.Unknown,
) as any as S.Schema<ScoreQuestionInstructionsCase2List>;

/** What the model should rate. */
export type ScoreQuestionInstructions =
  | string
  | ScoreQuestionInstructionsCase1Map
  | ScoreQuestionInstructionsCase2List;
export const ScoreQuestionInstructions =
  /*@__PURE__*/ S.Unknown as any as S.Schema<ScoreQuestionInstructions>;

export type ScoreQuestionCriteriaItemCase1Map = {
  [key: string]: unknown | undefined;
};
export const ScoreQuestionCriteriaItemCase1Map = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<ScoreQuestionCriteriaItemCase1Map>;

export type ScoreQuestionCriteriaItemCase2List = Array<unknown>;
export const ScoreQuestionCriteriaItemCase2List = /*@__PURE__*/ S.Array(
  S.Unknown,
) as any as S.Schema<ScoreQuestionCriteriaItemCase2List>;

export type ScoreQuestionCriteriaItem =
  | string
  | ScoreQuestionCriteriaItemCase1Map
  | ScoreQuestionCriteriaItemCase2List;
export const ScoreQuestionCriteriaItem =
  /*@__PURE__*/ S.Unknown as any as S.Schema<ScoreQuestionCriteriaItem>;

/** Ordered descriptions of the score levels. Each description's position determines its score, starting at zero. */
export type ScoreQuestionCriteriaList = Array<ScoreQuestionCriteriaItem>;
export const ScoreQuestionCriteriaList = /*@__PURE__*/ S.Array(
  ScoreQuestionCriteriaItem,
) as any as S.Schema<ScoreQuestionCriteriaList>;

/** A question that assigns a score using an ordered rubric. */
export interface ScoreQuestion {
  /** Identifies a question that rates the content using the levels in criteria. */
  type: string;
  /** What the model should rate. */
  instructions?: ScoreQuestionInstructions | null;
  /** Ordered descriptions of the score levels. Each description's position determines its score, starting at zero. */
  criteria: ScoreQuestionCriteriaList;
}
export const ScoreQuestion = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    type: S.String,
    instructions: S.optional(S.NullOr(ScoreQuestionInstructions)),
    criteria: ScoreQuestionCriteriaList,
  }),
).annotate({ identifier: "ScoreQuestion" }) as any as S.Schema<ScoreQuestion>;

/** A question about the supplied content. */
export type Question = NoulQuestion | ChoiceQuestion | ScoreQuestion;
export const Question = /*@__PURE__*/ S.Unknown as any as S.Schema<Question>;

/** Questions to ask about the content, each with a name you choose. The response uses those names to identify the answers. */
export type SystemOneRequestQuestionsMap = {
  [key: string]: Question | undefined;
};
export const SystemOneRequestQuestionsMap = /*@__PURE__*/ S.Record(
  S.String,
  Question,
) as any as S.Schema<SystemOneRequestQuestionsMap>;

export interface SystemOneRequest {
  /** The content all questions in this request refer to. */
  state: SystemOneRequestState;
  /** Name or alias of the model to use. Available names are returned by GET /v1/models. */
  model: string;
  /** Questions to ask about the content, each with a name you choose. The response uses those names to identify the answers. */
  questions: SystemOneRequestQuestionsMap;
}
export const SystemOneRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    state: SystemOneRequestState,
    model: S.String,
    questions: SystemOneRequestQuestionsMap,
  }).pipe(T.Http({ method: "POST", uri: "/v1/systemone", code: 200 })),
).annotate({
  identifier: "SystemOneRequest",
}) as any as S.Schema<SystemOneRequest>;

/** The probability of a yes answer or a true statement. */
export interface NoulAnswer {
  /** Identifies a yes/no answer. */
  type: string;
  /** Probability of a yes answer or a true statement, from 0 to 1. Values near 1 favor yes or true, values near 0 favor no or false, and values near 0.5 indicate uncertainty. */
  noul: number;
}
export const NoulAnswer = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    type: S.String,
    noul: S.Number,
  }),
).annotate({ identifier: "NoulAnswer" }) as any as S.Schema<NoulAnswer>;

export type ScoreAnswerLegendValueCase1Map = {
  [key: string]: unknown | undefined;
};
export const ScoreAnswerLegendValueCase1Map = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<ScoreAnswerLegendValueCase1Map>;

export type ScoreAnswerLegendValueCase2List = Array<unknown>;
export const ScoreAnswerLegendValueCase2List = /*@__PURE__*/ S.Array(
  S.Unknown,
) as any as S.Schema<ScoreAnswerLegendValueCase2List>;

export type ScoreAnswerLegendValue =
  | string
  | ScoreAnswerLegendValueCase1Map
  | ScoreAnswerLegendValueCase2List;
export const ScoreAnswerLegendValue =
  /*@__PURE__*/ S.Unknown as any as S.Schema<ScoreAnswerLegendValue>;

/** The requested criteria mapped to their score levels, so you can interpret the score. */
export type ScoreAnswerLegendMap = {
  [key: string]: ScoreAnswerLegendValue | undefined;
};
export const ScoreAnswerLegendMap = /*@__PURE__*/ S.Record(
  S.String,
  ScoreAnswerLegendValue,
) as any as S.Schema<ScoreAnswerLegendMap>;

/** Probability of each score level, from 0 to 1, using the same keys as legend. Shows how likely the alternatives are; values sum to approximately 1. */
export type ScoreAnswerProbabilitiesMap = { [key: string]: number | undefined };
export const ScoreAnswerProbabilitiesMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Number,
) as any as S.Schema<ScoreAnswerProbabilitiesMap>;

/** An expected score with its rubric, confidence, and score-level probabilities. */
export interface ScoreAnswer {
  /** Identifies a rating against the requested score levels. */
  type: string;
  /** Expected score: the probability-weighted average of the rubric levels. May fall between integer levels. */
  score: number;
  /** Confidence in the score, from 0 to 1. Higher values indicate greater certainty; use lower values to flag uncertain ratings for review. */
  confidence: number;
  /** The requested criteria mapped to their score levels, so you can interpret the score. */
  legend: ScoreAnswerLegendMap;
  /** Probability of each score level, from 0 to 1, using the same keys as legend. Shows how likely the alternatives are; values sum to approximately 1. */
  probabilities: ScoreAnswerProbabilitiesMap;
}
export const ScoreAnswer = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    type: S.String,
    score: S.Number,
    confidence: S.Number,
    legend: ScoreAnswerLegendMap,
    probabilities: ScoreAnswerProbabilitiesMap,
  }),
).annotate({ identifier: "ScoreAnswer" }) as any as S.Schema<ScoreAnswer>;

/** Probability of each choice in criteria, keyed by choice name, from 0 to 1. Shows how likely the alternatives are; values sum to approximately 1. */
export type ChoiceAnswerProbabilitiesMap = {
  [key: string]: number | undefined;
};
export const ChoiceAnswerProbabilitiesMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Number,
) as any as S.Schema<ChoiceAnswerProbabilitiesMap>;

/** The selected choice, confidence, and probabilities for a choice question. */
export interface ChoiceAnswer {
  /** Identifies a selection from the requested choices. */
  type: string;
  /** The name of the choice with the highest probability among the question's criteria. */
  choice: string;
  /** Confidence in the selected choice, from 0 to 1. Higher values indicate greater certainty; use lower values to flag uncertain selections for review. */
  confidence: number;
  /** Probability of each choice in criteria, keyed by choice name, from 0 to 1. Shows how likely the alternatives are; values sum to approximately 1. */
  probabilities: ChoiceAnswerProbabilitiesMap;
}
export const ChoiceAnswer = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    type: S.String,
    choice: S.String,
    confidence: S.Number,
    probabilities: ChoiceAnswerProbabilitiesMap,
  }),
).annotate({ identifier: "ChoiceAnswer" }) as any as S.Schema<ChoiceAnswer>;

/** An answer whose type matches the corresponding question. */
export type Answer = NoulAnswer | ScoreAnswer | ChoiceAnswer;
export const Answer = /*@__PURE__*/ S.Unknown as any as S.Schema<Answer>;

/** Answers keyed by the question names supplied in the request. Each answer's type matches its question's type. */
export type SystemOneResponseAnswersMap = { [key: string]: Answer | undefined };
export const SystemOneResponseAnswersMap = /*@__PURE__*/ S.Record(
  S.String,
  Answer,
) as any as S.Schema<SystemOneResponseAnswersMap>;

/** Token usage for the request. */
export interface Usage {
  /** Number of billable input tokens used to evaluate the request. */
  input_tokens: number;
  /** Number of output tokens used to answer the questions. Output tokens are currently free of charge. */
  output_tokens: number;
}
export const Usage = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    input_tokens: S.Number,
    output_tokens: S.Number,
  }),
).annotate({ identifier: "Usage" }) as any as S.Schema<Usage>;

/** Answers grouped by question name, with the model used and token usage. */
export interface SystemOneResponse {
  /** Name of the model that answered the questions. May differ from the alias supplied in the request. */
  model: string;
  /** Answers keyed by the question names supplied in the request. Each answer's type matches its question's type. */
  answers: SystemOneResponseAnswersMap;
  /** Input and output token counts for this evaluation. */
  usage: Usage;
}
export const SystemOneResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    model: S.String,
    answers: SystemOneResponseAnswersMap,
    usage: Usage,
  }),
).annotate({
  identifier: "SystemOneResponse",
}) as any as S.Schema<SystemOneResponse>;

export type ListModelsError = UnprocessableEntity | TypesafeAiOpError;
/** Models V1 List the models and aliases available to the authenticated account. Pass a returned model name as `model` in a POST /v1/systemone request. */
export const listModels: API.OperationMethod<
  ListModelsRequest,
  ModelMetadataList,
  ListModelsError,
  TypesafeAiOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListModelsRequest,
  output: ModelMetadataList,
  errors: [UnprocessableEntity, UnknownTypesafeAiError],
  protocol: TypesafeAiProtocol,
  retry: Retry.Retry,
}));

export type SystemOneError = UnprocessableEntity | TypesafeAiOpError;
/** Systemone Answer one or more questions about the content supplied in `state`. You can mix question types in one request. Answers use the same names as the questions, so you can match each result to its question. The response also includes the model used and token usage. */
export const systemOne: API.OperationMethod<
  SystemOneRequest,
  SystemOneResponse,
  SystemOneError,
  TypesafeAiOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: SystemOneRequest,
  output: SystemOneResponse,
  errors: [UnprocessableEntity, UnknownTypesafeAiError],
  protocol: TypesafeAiProtocol,
  retry: Retry.Retry,
}));
