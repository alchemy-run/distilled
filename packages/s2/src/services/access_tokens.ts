// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/spec-mirror-s2/specs/openapi.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as Redacted from "effect/Redacted";
import * as API from "@distilled.cloud/core/api";
import * as T from "../traits.ts";
import { S2Protocol, type S2OpError, type S2OpContext } from "../protocol.ts";
import { paginateStartAfter } from "../pagination.ts";
import { UnknownS2Error } from "../errors.ts";
import * as Retry from "../retry.ts";

export type { S2OpError, S2OpContext };

/** Match only the resource with this exact name. Use an empty string to match no resources. */
export interface ResourceSetCase0 {
  /** Match only the resource with this exact name. Use an empty string to match no resources. */
  exact: string;
}
export const ResourceSetCase0 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    exact: S.String,
  }),
).annotate({
  identifier: "ResourceSetCase0",
}) as any as S.Schema<ResourceSetCase0>;

/** Match all resources that start with this prefix. Use an empty string to match all resource. */
export interface ResourceSetCase1 {
  /** Match all resources that start with this prefix. Use an empty string to match all resource. */
  prefix: string;
}
export const ResourceSetCase1 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    prefix: S.String,
  }),
).annotate({
  identifier: "ResourceSetCase1",
}) as any as S.Schema<ResourceSetCase1>;

export type ResourceSet = ResourceSetCase0 | ResourceSetCase1;
export const ResourceSet =
  /*@__PURE__*/ S.Unknown as any as S.Schema<ResourceSet>;

export interface ReadWritePermissions {
  /** Read permission. */
  read?: boolean;
  /** Write permission. */
  write?: boolean;
}
export const ReadWritePermissions = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    read: S.optional(S.Boolean),
    write: S.optional(S.Boolean),
  }),
).annotate({
  identifier: "ReadWritePermissions",
}) as any as S.Schema<ReadWritePermissions>;

export interface PermittedOperationGroups {
  account?: ReadWritePermissions | null;
  basin?: ReadWritePermissions | null;
  stream?: ReadWritePermissions | null;
}
export const PermittedOperationGroups = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    account: S.optional(S.NullOr(ReadWritePermissions)),
    basin: S.optional(S.NullOr(ReadWritePermissions)),
    stream: S.optional(S.NullOr(ReadWritePermissions)),
  }),
).annotate({
  identifier: "PermittedOperationGroups",
}) as any as S.Schema<PermittedOperationGroups>;

export type Operation =
  | "list-basins"
  | "create-basin"
  | "delete-basin"
  | "reconfigure-basin"
  | "get-basin-config"
  | "issue-access-token"
  | "revoke-access-token"
  | "list-access-tokens"
  | "list-streams"
  | "create-stream"
  | "delete-stream"
  | "get-stream-config"
  | "reconfigure-stream"
  | "check-tail"
  | "append"
  | "read"
  | "trim"
  | "fence"
  | "account-metrics"
  | "basin-metrics"
  | "stream-metrics"
  | "list-locations"
  | "get-default-location"
  | "set-default-location";
export const Operation = S.String;

/** Operations allowed for the token. A union of allowed operations and groups is used as an effective set of allowed operations. */
export type AccessTokenScopeOpsList = Array<Operation | (string & {})>;
export const AccessTokenScopeOpsList = /*@__PURE__*/ S.Array(
  Operation,
) as any as S.Schema<AccessTokenScopeOpsList>;

export interface AccessTokenScope {
  access_tokens?: ResourceSet | null;
  basins?: ResourceSet | null;
  op_groups?: PermittedOperationGroups | null;
  /** Operations allowed for the token. A union of allowed operations and groups is used as an effective set of allowed operations. */
  ops?: AccessTokenScopeOpsList | null;
  streams?: ResourceSet | null;
}
export const AccessTokenScope = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    access_tokens: S.optional(S.NullOr(ResourceSet)),
    basins: S.optional(S.NullOr(ResourceSet)),
    op_groups: S.optional(S.NullOr(PermittedOperationGroups)),
    ops: S.optional(S.NullOr(AccessTokenScopeOpsList)),
    streams: S.optional(S.NullOr(ResourceSet)),
  }),
).annotate({
  identifier: "AccessTokenScope",
}) as any as S.Schema<AccessTokenScope>;

export interface IssueAccessTokenRequest {
  /** Namespace streams based on the configured stream-level scope, which must be a prefix. Stream name arguments will be automatically prefixed, and the prefix will be stripped when listing streams. */
  auto_prefix_streams?: boolean;
  /** Expiration time in RFC 3339 format. If not set, the expiration will be set to that of the requestor's token. */
  expires_at?: string | null;
  /** Access token ID. It must be unique to the account and between 1 and 96 bytes in length, and must not contain NUL bytes. */
  id: string;
  /** Access token scope. */
  scope: AccessTokenScope;
}
export const IssueAccessTokenRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    auto_prefix_streams: S.optional(S.Boolean),
    expires_at: S.optional(S.NullOr(S.String)),
    id: S.String,
    scope: AccessTokenScope,
  }).pipe(T.Http({ method: "POST", uri: "/access-tokens", code: 200 })),
).annotate({
  identifier: "IssueAccessTokenRequest",
}) as any as S.Schema<IssueAccessTokenRequest>;

export interface IssueAccessTokenResponse {
  /** Created access token. */
  access_token: string | Redacted.Redacted<string>;
}
export const IssueAccessTokenResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    access_token: S.String.pipe(T.SensitiveValue({})),
  }),
).annotate({
  identifier: "IssueAccessTokenResponse",
}) as any as S.Schema<IssueAccessTokenResponse>;

export interface ListAccessTokensRequest {
  /** Filter to access tokens whose IDs begin with this prefix. It must not contain NUL bytes. */
  prefix?: string;
  /** Filter to access tokens whose IDs lexicographically start after this string. It must not contain NUL bytes. */
  start_after?: string;
  /** Number of results, up to a maximum of 1000. */
  limit?: number;
}
export const ListAccessTokensRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    prefix: S.optional(S.String.pipe(T.Query())),
    start_after: S.optional(S.String.pipe(T.Query())),
    limit: S.optional(S.Number.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/access-tokens", code: 200 })),
).annotate({
  identifier: "ListAccessTokensRequest",
}) as any as S.Schema<ListAccessTokensRequest>;

export interface AccessTokenInfo {
  /** Namespace streams based on the configured stream-level scope. */
  auto_prefix_streams: boolean;
  /** Expiration time in RFC 3339 format. */
  expires_at?: string | null;
  /** Access token ID. */
  id: string;
  /** Access token scope. */
  scope: AccessTokenScope;
}
export const AccessTokenInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    auto_prefix_streams: S.Boolean,
    expires_at: S.optional(S.NullOr(S.String)),
    id: S.String,
    scope: AccessTokenScope,
  }),
).annotate({
  identifier: "AccessTokenInfo",
}) as any as S.Schema<AccessTokenInfo>;

/** Matching access tokens. */
export type ListAccessTokensResponseAccessTokensList = Array<AccessTokenInfo>;
export const ListAccessTokensResponseAccessTokensList = /*@__PURE__*/ S.Array(
  AccessTokenInfo,
) as any as S.Schema<ListAccessTokensResponseAccessTokensList>;

export interface ListAccessTokensResponse {
  /** Matching access tokens. */
  access_tokens: ListAccessTokensResponseAccessTokensList;
  /** Indicates that there are more access tokens that match the criteria. */
  has_more: boolean;
}
export const ListAccessTokensResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    access_tokens: ListAccessTokensResponseAccessTokensList,
    has_more: S.Boolean,
  }),
).annotate({
  identifier: "ListAccessTokensResponse",
}) as any as S.Schema<ListAccessTokensResponse>;

export interface RevokeAccessTokenRequest {
  /** Access token ID. */
  id: string;
}
export const RevokeAccessTokenRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "DELETE", uri: "/access-tokens/{id}", code: 200 })),
).annotate({
  identifier: "RevokeAccessTokenRequest",
}) as any as S.Schema<RevokeAccessTokenRequest>;

export interface RevokeAccessTokenResponse {}
export const RevokeAccessTokenResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "RevokeAccessTokenResponse",
}) as any as S.Schema<RevokeAccessTokenResponse>;

export type IssueAccessTokenError = S2OpError;
/** Issue a new access token. */
export const issueAccessToken: API.OperationMethod<
  IssueAccessTokenRequest,
  IssueAccessTokenResponse,
  IssueAccessTokenError,
  S2OpContext
> = /*@__PURE__*/ API.make(() => ({
  input: IssueAccessTokenRequest,
  output: IssueAccessTokenResponse,
  errors: [UnknownS2Error],
  protocol: S2Protocol,
  retry: Retry.Retry,
}));

export type ListAccessTokensError = S2OpError;
/** List access tokens. */
export const listAccessTokens: API.PaginatedOperationMethod<
  ListAccessTokensRequest,
  ListAccessTokensResponse,
  ListAccessTokensError,
  S2OpContext,
  AccessTokenInfo
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListAccessTokensRequest,
    output: ListAccessTokensResponse,
    errors: [UnknownS2Error],
    protocol: S2Protocol,
    retry: Retry.Retry,
    pagination: {
      inputToken: "start_after",
      outputToken: "id",
      items: "access_tokens",
      pageSize: "limit",
      hasNextPage: "has_more",
    } as const,
  }),
  paginateStartAfter,
) as any;

export type RevokeAccessTokenError = S2OpError;
/** Revoke an access token. */
export const revokeAccessToken: API.OperationMethod<
  RevokeAccessTokenRequest,
  RevokeAccessTokenResponse,
  RevokeAccessTokenError,
  S2OpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RevokeAccessTokenRequest,
  output: RevokeAccessTokenResponse,
  errors: [UnknownS2Error],
  protocol: S2Protocol,
  retry: Retry.Retry,
}));
