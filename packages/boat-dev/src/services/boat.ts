// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/spec-mirror-boat-dev). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as Redacted from "effect/Redacted";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  BoatProtocol,
  type BoatOpError,
  type BoatOpContext,
} from "../protocol.ts";
import { paginateCursor } from "../pagination.ts";
import { UnknownBoatError } from "../errors.ts";
import * as Retry from "../retry.ts";

export type { BoatOpError, BoatOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Conflict
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Conflict>()("Conflict", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withConflictError),
    [{ status: 409 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export interface AddEnvironmentRepoRequest {
  environmentId: string;
  /** The repository `databaseId` from `GET /repos`. */
  repositoryId: string;
  baseBranch?: string;
}
export const AddEnvironmentRepoRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    environmentId: S.String.pipe(T.Label()),
    repositoryId: S.String,
    baseBranch: S.optional(S.String),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/environments/{environmentId}/repos",
      code: 200,
    }),
  ),
).annotate({
  identifier: "AddEnvironmentRepoRequest",
}) as any as S.Schema<AddEnvironmentRepoRequest>;

/** Result of a granular environment change. Every change mints a new immutable version holding just that delta; existing sandboxes stay pinned until upgraded. */
export interface EnvironmentItemChangeResponse {
  success: boolean;
  /** Id of the newly minted environment version. This is a version id, not the environment's own id: the environment id you passed in the path is unchanged. */
  versionId?: string;
  versionNumber?: number;
}
export const EnvironmentItemChangeResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    success: S.Boolean,
    versionId: S.optional(S.String),
    versionNumber: S.optional(S.Number),
  }),
).annotate({
  identifier: "EnvironmentItemChangeResponse",
}) as any as S.Schema<EnvironmentItemChangeResponse>;

export interface ConfigureSshKeyRequest {
  /** Public Sandbox id returned by create/list/get sandbox calls. */
  sandboxId: string;
  /** Public SSH key in OpenSSH format. Private keys are rejected. */
  key: string;
}
export const ConfigureSshKeyRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandboxId: S.String.pipe(T.Label()),
    key: S.String,
  }).pipe(
    T.Http({ method: "POST", uri: "/sandboxes/{sandboxId}/sshkey", code: 200 }),
  ),
).annotate({
  identifier: "ConfigureSshKeyRequest",
}) as any as S.Schema<ConfigureSshKeyRequest>;

export interface ConfigureSshKeyResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  success?: boolean;
  machineIp?: string | null;
  sshUser?: string;
}
export const ConfigureSshKeyResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    success: S.optional(S.Boolean),
    machineIp: S.optional(S.NullOr(S.String)),
    sshUser: S.optional(S.String),
  }),
).annotate({
  identifier: "ConfigureSshKeyResponse",
}) as any as S.Schema<ConfigureSshKeyResponse>;

export interface CreateEnvironmentRequest {
  /** Unique environment name. Letters, numbers, dot, dash, underscore; max 64 chars. */
  name: string;
}
export const CreateEnvironmentRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String,
  }).pipe(T.Http({ method: "POST", uri: "/environments", code: 200 })),
).annotate({
  identifier: "CreateEnvironmentRequest",
}) as any as S.Schema<CreateEnvironmentRequest>;

export interface SecretFile {
  path: string;
  /** Secret file contents. Treat as sensitive. */
  contents: string;
}
export const SecretFile = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    path: S.String,
    contents: S.String,
  }),
).annotate({ identifier: "SecretFile" }) as any as S.Schema<SecretFile>;

export type SandboxEnvironmentSecretFilesList = Array<SecretFile>;
export const SandboxEnvironmentSecretFilesList = /*@__PURE__*/ S.Array(
  SecretFile,
) as any as S.Schema<SandboxEnvironmentSecretFilesList>;

export interface SelectedRepository {
  /** GitHub repository id. */
  id?: number;
  /** Internal repository id used when selecting repositories. */
  databaseId?: string;
  name?: string;
  fullName?: string;
  private?: boolean;
  permissions?: string;
  pushedAt?: string | null;
  baseBranch?: string;
  setupRoutineId?: string | null;
  setupScript?: string;
  setupBlocking?: boolean;
}
export const SelectedRepository = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.optional(S.Number),
    databaseId: S.optional(S.String),
    name: S.optional(S.String),
    fullName: S.optional(S.String),
    private: S.optional(S.Boolean),
    permissions: S.optional(S.String),
    pushedAt: S.optional(S.NullOr(S.String)),
    baseBranch: S.optional(S.String),
    setupRoutineId: S.optional(S.NullOr(S.String)),
    setupScript: S.optional(S.String),
    setupBlocking: S.optional(S.Boolean),
  }),
).annotate({
  identifier: "SelectedRepository",
}) as any as S.Schema<SelectedRepository>;

/** Repositories attached to the latest version, with base branch and setup script. */
export type SandboxEnvironmentSelectedRepositoriesList =
  Array<SelectedRepository>;
export const SandboxEnvironmentSelectedRepositoriesList = /*@__PURE__*/ S.Array(
  SelectedRepository,
) as any as S.Schema<SandboxEnvironmentSelectedRepositoriesList>;

/** An immutable snapshot of an environment's config. Editing an environment mints a new version; sandboxes stay pinned to the version they were created on until upgraded. */
export interface SandboxEnvironmentVersionSummary {
  id: string;
  /** Monotonically increasing per environment; version 1 is the first. */
  versionNumber: number;
  /** Number of the caller's active sandboxes currently pinned to this version. */
  sandboxCount: number;
  createdAt: string;
}
export const SandboxEnvironmentVersionSummary = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    versionNumber: S.Number,
    sandboxCount: S.Number,
    createdAt: S.String,
  }),
).annotate({
  identifier: "SandboxEnvironmentVersionSummary",
}) as any as S.Schema<SandboxEnvironmentVersionSummary>;

export type SandboxEnvironmentVersionsList =
  Array<SandboxEnvironmentVersionSummary>;
export const SandboxEnvironmentVersionsList = /*@__PURE__*/ S.Array(
  SandboxEnvironmentVersionSummary,
) as any as S.Schema<SandboxEnvironmentVersionsList>;

/** A named sandbox environment. All flags/contents reflect the latest version. */
export interface SandboxEnvironment {
  id: string;
  name: string;
  /** Exactly one environment is the default; sandboxes created without an `environment` name use it. */
  isDefault: boolean;
  latestVersionId: string | null;
  /** When true the environment passes nothing to a sandbox (repos, secrets, and all credentials withheld), overriding the fine-grained flags below. Use for sandboxes handed to third parties. */
  safeForThirdParties: boolean;
  /** Attach the environment's GitHub repositories and the GitHub token (so `gh` and pushes work). Ignored when `safeForThirdParties` is true. */
  passGithub: boolean;
  /** Attach the environment's env variables and secret files. Ignored when `safeForThirdParties` is true. */
  passSecrets: boolean;
  /** Attach the sandbox's own service/preview credentials. Ignored when `safeForThirdParties` is true. */
  passSandboxCredentials: boolean;
  /** Attach agent-provider credentials configured on the Agents page. Ignored when `safeForThirdParties` is true. */
  passAgentsCredentials: boolean;
  /** The latest version's .env-style content. Treat as sensitive. */
  envContents: string;
  secretFiles: SandboxEnvironmentSecretFilesList;
  /** Repositories attached to the latest version, with base branch and setup script. */
  selectedRepositories?: SandboxEnvironmentSelectedRepositoriesList;
  versions: SandboxEnvironmentVersionsList;
}
export const SandboxEnvironment = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    name: S.String,
    isDefault: S.Boolean,
    latestVersionId: S.NullOr(S.String),
    safeForThirdParties: S.Boolean,
    passGithub: S.Boolean,
    passSecrets: S.Boolean,
    passSandboxCredentials: S.Boolean,
    passAgentsCredentials: S.Boolean,
    envContents: S.String,
    secretFiles: SandboxEnvironmentSecretFilesList,
    selectedRepositories: S.optional(
      SandboxEnvironmentSelectedRepositoriesList,
    ),
    versions: SandboxEnvironmentVersionsList,
  }),
).annotate({
  identifier: "SandboxEnvironment",
}) as any as S.Schema<SandboxEnvironment>;

export type SandboxEnvironmentListResponseEnvironmentsList =
  Array<SandboxEnvironment>;
export const SandboxEnvironmentListResponseEnvironmentsList =
  /*@__PURE__*/ S.Array(
    SandboxEnvironment,
  ) as any as S.Schema<SandboxEnvironmentListResponseEnvironmentsList>;

export interface SandboxEnvironmentListResponse {
  environments: SandboxEnvironmentListResponseEnvironmentsList;
}
export const SandboxEnvironmentListResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    environments: SandboxEnvironmentListResponseEnvironmentsList,
  }),
).annotate({
  identifier: "SandboxEnvironmentListResponse",
}) as any as S.Schema<SandboxEnvironmentListResponse>;

/** Machine size. `small` consumes machine time at half rate and `large` at twice the default rate (see the Billing guide). `xlarge` costs $0.20 per running hour, requires the effective $100 plan or higher, and requires an explicit bare-metal operator allocation. A fork inherits the source sandbox's type unless the fork request passes its own, and resume and fork can move a sandbox between sizes. */
export type CreateSandboxRequestType = "small" | "default" | "large" | "xlarge";
export const CreateSandboxRequestType = S.String;

/** Per-sandbox environment variables injected into the sandbox's tool environment, on top of the account environment's variables (per-sandbox values win on conflicts). Keys must match `[A-Za-z_][A-Za-z0-9_]{0,127}`; at most 100 variables and 64KB total. Reserved names (`ASCII_TOKEN`, `ASCII_API_URL`, `AGENT_ID`, `PRODUCT_MODE`, `ENVIRONMENT_ID`, `BOAT_ID`, `SERVICE_PREVIEW_TOKEN`, `BOAT_CLI_TOKEN`) are rejected with `invalid_env`. Forked sandboxes inherit the source sandbox's env unless the fork request supplies its own `env`. */
export type CreateSandboxRequestEnvMap = { [key: string]: string | undefined };
export const CreateSandboxRequestEnvMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<CreateSandboxRequestEnvMap>;

export interface CreateSandboxRequest {
  /** Billing wallet for this request. A team id you belong to reads that team's limits / bills a create to that team. Your own account id is personal. Sandboxes, snapshots, and environments stay creator-private. */
  org?: string;
  /** Optional exactly-once key for creating a sandbox. Send your own opaque, account-unique value (a UUID) to make `POST /sandboxes` safe to retry when the response is lost (network timeout, 5xx): the first request creates the sandbox and binds it to the key; every later request with the **same account, key, and request body** returns that same sandbox instead of creating a second, billable one. Behavior: keys are retained for **24 hours**; a concurrent or early retry while the first sandbox is still being minted returns `409` `idempotency_in_progress` (retry shortly, same key); reusing a key with a **different body** returns `409` `idempotency_key_reused`; timeouts and 5xx are safe to retry with the same key; a create that fails before the sandbox exists releases the key within ~2 minutes so a retry can create the sandbox. Omit the header to keep the default (non-idempotent) behavior. */
  idempotencyKey?: string;
  /** Same as the `org` query parameter. Query wins when both are set. */
  xBoatOrg?: string;
  /** Machine size. `small` consumes machine time at half rate and `large` at twice the default rate (see the Billing guide). `xlarge` costs $0.20 per running hour, requires the effective $100 plan or higher, and requires an explicit bare-metal operator allocation. A fork inherits the source sandbox's type unless the fork request passes its own, and resume and fork can move a sandbox between sizes. */
  type?: CreateSandboxRequestType | (string & {});
  /** Number of seconds before automatic archival. `null` disables auto-stop. The backend also accepts the string `infinite` for legacy compatibility; new clients should send null. */
  ttlSeconds?: number | null;
  /** Per-sandbox environment variables injected into the sandbox's tool environment, on top of the account environment's variables (per-sandbox values win on conflicts). Keys must match `[A-Za-z_][A-Za-z0-9_]{0,127}`; at most 100 variables and 64KB total. Reserved names (`ASCII_TOKEN`, `ASCII_API_URL`, `AGENT_ID`, `PRODUCT_MODE`, `ENVIRONMENT_ID`, `BOAT_ID`, `SERVICE_PREVIEW_TOKEN`, `BOAT_CLI_TOKEN`) are rejected with `invalid_env`. Forked sandboxes inherit the source sandbox's env unless the fork request supplies its own `env`. */
  env?: CreateSandboxRequestEnvMap;
  /** Name of the sandbox environment to attach to this sandbox. Environments are managed in the Boat dashboard and bundle the repositories, secrets, and credential toggles a sandbox gets. Omit to use your default environment (`base` unless you changed it). Unknown names are rejected with `unknown_environment`. An environment marked "safe for third parties" passes nothing to the sandbox, exactly like `noEnv`. */
  environment?: string;
  /** Create a sandbox with none of the secrets attached to your account (no environment variables, secret files, or credentials), confined to itself so it cannot act on your account or other sandboxes. For sandboxes you give to your own users. SSH, SCP, desktop, snapshots, and public URLs still work; pass `env` to give the sandbox a secret of its own. A fork of a no-env sandbox is always no-env. Equivalent to attaching an environment marked "safe for third parties". */
  noEnv?: boolean;
  /** Shell script that runs on the sandbox after it is ready. Ready means "ready to accept the user", not "setup done": the script starts in the background once provisioning completes and never blocks the sandbox becoming usable. It runs as the sandbox user via `bash`, with the sandbox's environment applied, and its output goes to a log file on the sandbox. Observe the outcome as `setupStatus` (pending/running/done/failed) and `setupError` on the sandbox. Rejected with a 400 `invalid_setup_script` error when it is not a string or exceeds 64KB. */
  setupScript?: string;
  /** Legacy alias for `org`. Ignored when `org` is also set. */
  teamId?: string;
  /** Create the sandbox from a named snapshot (saved with `POST /named-snapshots`, or `boat snapshot <id> <name>` in the CLI). The sandbox starts from that exact frozen state. Omitting `type` inherits the type the snapshot was saved from; env and no-env inherit from the snapshot's source sandbox unless the request passes its own, with the same rules as forking. */
  from?: string;
}
export const CreateSandboxRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    org: S.optional(S.String.pipe(T.Query())),
    idempotencyKey: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
    xBoatOrg: S.optional(S.String.pipe(T.Header("X-Boat-Org"))),
    type: S.optional(CreateSandboxRequestType),
    ttlSeconds: S.optional(S.NullOr(S.Number)),
    env: S.optional(CreateSandboxRequestEnvMap),
    environment: S.optional(S.String),
    noEnv: S.optional(S.Boolean),
    setupScript: S.optional(S.String),
    teamId: S.optional(S.String),
    from: S.optional(S.String),
  }).pipe(T.Http({ method: "POST", uri: "/sandboxes", code: 200 })),
).annotate({
  identifier: "CreateSandboxRequest",
}) as any as S.Schema<CreateSandboxRequest>;

export type CreateSandboxResponseStatus = "provisioning";
export const CreateSandboxResponseStatus = S.String;

export type SandboxState =
  | "init"
  | "provisioning"
  | "provisioned"
  | "cloning"
  | "ready"
  | "idle"
  | "running"
  | "archiving"
  | "archived"
  | "error";
export const SandboxState = S.String;

/** Current machine size: what the sandbox was created with, or the size it was last resumed or forked onto. Legacy bare-metal sandboxes read as `default`. */
export type SandboxType = "small" | "default" | "large" | "xlarge";
export const SandboxType = S.String;

/** Status of the most recent snapshot attempt, or null if none. A value other than completed while snapshotCompletedAt stays stale indicates failing snapshots. */
export type SandboxLastSnapshotStatus =
  | "queued"
  | "in_progress"
  | "completed"
  | "failed"
  | "cancelled";
export const SandboxLastSnapshotStatus = S.String;

/** Outcome of the create-time `setupScript`: `pending` (stored, not yet started), `running` (executing on the sandbox in the background), `done` (exit code 0) or `failed` (non-zero exit, or the sandbox lost track of the process). Null when the sandbox was created without a setup script. */
export type SandboxSetupStatus = "pending" | "running" | "done" | "failed";
export const SandboxSetupStatus = S.String;

export interface Sandbox {
  id: string;
  name: string;
  state: SandboxState;
  /** Current machine size: what the sandbox was created with, or the size it was last resumed or forked onto. Legacy bare-metal sandboxes read as `default`. */
  type?: SandboxType;
  /** vCPUs guaranteed by this sandbox's type. */
  vcpu?: number;
  /** RAM in GB guaranteed by this sandbox's type. */
  memoryGB?: number;
  /** Rate at which this sandbox consumes machine time. 0.5 for `small`, 1 for `default`, 2 for `large`, and 50/9 for `xlarge`. */
  billingMultiplier?: number;
  /** Machine URL when assigned. */
  url?: string | null;
  /** Machine IPv6 or IPv4 address when assigned. */
  ip?: string | null;
  createdAt?: string | null;
  updatedAt?: string | null;
  /** Automatic archival time, or null when auto-stop is disabled. */
  archiveAfter?: string | null;
  desktopAvailable: boolean;
  /** Secret-bearing desktop stream URL when available. Redact from logs. */
  desktopUrl?: string | null;
  snapshotAvailable: boolean;
  /** Timestamp of the most recent successfully completed snapshot, or null. */
  snapshotCompletedAt?: string | null;
  /** The sandbox's stable three-word subdomain slug (e.g. "frazil-pneuma-rallye"), or null before one is assigned. */
  subdomain?: string | null;
  /** Timestamp of the most recent snapshot attempt of any status (queued, in_progress, completed, failed, cancelled), or null. Use with snapshotCompletedAt to detect snapshots that keep failing. */
  lastSnapshotAttemptAt?: string | null;
  /** Status of the most recent snapshot attempt, or null if none. A value other than completed while snapshotCompletedAt stays stale indicates failing snapshots. */
  lastSnapshotStatus?: SandboxLastSnapshotStatus | null;
  /** Outcome of the create-time `setupScript`: `pending` (stored, not yet started), `running` (executing on the sandbox in the background), `done` (exit code 0) or `failed` (non-zero exit, or the sandbox lost track of the process). Null when the sandbox was created without a setup script. */
  setupStatus?: SandboxSetupStatus | null;
  /** Short failure detail (exit code plus a stderr tail) when `setupStatus` is `failed`; while `pending`, may carry the last start/upload error from a retry in progress. Otherwise null. */
  setupError?: string | null;
  /** Name of the sandbox environment this sandbox is running, or null if it is attached to none (a `noEnv` sandbox, or one whose environment was deleted). A sandbox freezes onto one environment version when it starts and keeps it for life, so this is what the sandbox actually holds, not what the environment says today. */
  environment?: string | null;
  /** Version number of `environment` that this sandbox is pinned to. Compare it against the environment's latest version to see whether an upgrade is pending: a sandbox below the latest is still running the older configuration until someone calls `POST /environments/{environmentId}/upgrade`. */
  environmentVersion?: number | null;
}
export const Sandbox = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    name: S.String,
    state: SandboxState,
    type: S.optional(SandboxType),
    vcpu: S.optional(S.Number),
    memoryGB: S.optional(S.Number),
    billingMultiplier: S.optional(S.Number),
    url: S.optional(S.NullOr(S.String)),
    ip: S.optional(S.NullOr(S.String)),
    createdAt: S.optional(S.NullOr(S.String)),
    updatedAt: S.optional(S.NullOr(S.String)),
    archiveAfter: S.optional(S.NullOr(S.String)),
    desktopAvailable: S.Boolean,
    desktopUrl: S.optional(S.NullOr(S.String)),
    snapshotAvailable: S.Boolean,
    snapshotCompletedAt: S.optional(S.NullOr(S.String)),
    subdomain: S.optional(S.NullOr(S.String)),
    lastSnapshotAttemptAt: S.optional(S.NullOr(S.String)),
    lastSnapshotStatus: S.optional(S.NullOr(SandboxLastSnapshotStatus)),
    setupStatus: S.optional(S.NullOr(SandboxSetupStatus)),
    setupError: S.optional(S.NullOr(S.String)),
    environment: S.optional(S.NullOr(S.String)),
    environmentVersion: S.optional(S.NullOr(S.Number)),
  }),
).annotate({ identifier: "Sandbox" }) as any as S.Schema<Sandbox>;

export interface CreateSandboxResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  status: CreateSandboxResponseStatus;
  ttlSeconds: number | null;
  sandbox: Sandbox;
}
export const CreateSandboxResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    status: CreateSandboxResponseStatus,
    ttlSeconds: S.NullOr(S.Number),
    sandbox: Sandbox,
  }),
).annotate({
  identifier: "CreateSandboxResponse",
}) as any as S.Schema<CreateSandboxResponse>;

export type CreateScopedApiKeyRequestPreset =
  | "read-only"
  | "full-sandbox"
  | "ci"
  | "admin";
export const CreateScopedApiKeyRequestPreset = S.String;

export type CreateScopedApiKeyRequestActionsList = Array<string>;
export const CreateScopedApiKeyRequestActionsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<CreateScopedApiKeyRequestActionsList>;

export type CreateScopedApiKeyRequestSandboxIdsList = Array<string>;
export const CreateScopedApiKeyRequestSandboxIdsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<CreateScopedApiKeyRequestSandboxIdsList>;

export type CreateScopedApiKeyRequestEnvironmentIdsList = Array<string>;
export const CreateScopedApiKeyRequestEnvironmentIdsList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<CreateScopedApiKeyRequestEnvironmentIdsList>;

export interface CreateScopedApiKeyRequest {
  name?: string;
  /** Duration such as 90d, 24h, or seconds. Max 365d. */
  ttl?: string;
  preset?: CreateScopedApiKeyRequestPreset | (string & {});
  actions?: CreateScopedApiKeyRequestActionsList;
  sandboxIds?: CreateScopedApiKeyRequestSandboxIdsList;
  environmentIds?: CreateScopedApiKeyRequestEnvironmentIdsList;
}
export const CreateScopedApiKeyRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.optional(S.String),
    ttl: S.optional(S.String),
    preset: S.optional(CreateScopedApiKeyRequestPreset),
    actions: S.optional(CreateScopedApiKeyRequestActionsList),
    sandboxIds: S.optional(CreateScopedApiKeyRequestSandboxIdsList),
    environmentIds: S.optional(CreateScopedApiKeyRequestEnvironmentIdsList),
  }).pipe(T.Http({ method: "POST", uri: "/api-keys/scoped", code: 200 })),
).annotate({
  identifier: "CreateScopedApiKeyRequest",
}) as any as S.Schema<CreateScopedApiKeyRequest>;

/** Credential storage lane. Scoped secrets are never stored in the legacy hash column. */
export type ApiKeyCredentialLane = "legacy" | "scoped-v1";
export const ApiKeyCredentialLane = S.String;

export interface ApiKeyRequestUsage {
  /** Requests authenticated with this key during the current UTC day and the previous 29 UTC days. */
  requests: number;
  windowDays: number;
}
export const ApiKeyRequestUsage = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    requests: S.Number,
    windowDays: S.Number,
  }),
).annotate({
  identifier: "ApiKeyRequestUsage",
}) as any as S.Schema<ApiKeyRequestUsage>;

export interface ApiKeyResourceTotals {
  total: number;
  sandboxes: number;
  agents: number;
}
export const ApiKeyResourceTotals = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    total: S.Number,
    sandboxes: S.Number,
    agents: S.Number,
  }),
).annotate({
  identifier: "ApiKeyResourceTotals",
}) as any as S.Schema<ApiKeyResourceTotals>;

export type ApiKeyScopeActionsList = Array<string>;
export const ApiKeyScopeActionsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ApiKeyScopeActionsList>;

export type ApiKeyScopeSandboxesCase0 = "*";
export const ApiKeyScopeSandboxesCase0 = S.String;

export type ApiKeyScopeSandboxesCase1List = Array<string>;
export const ApiKeyScopeSandboxesCase1List = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ApiKeyScopeSandboxesCase1List>;

export type ApiKeyScopeSandboxes =
  | ApiKeyScopeSandboxesCase0
  | ApiKeyScopeSandboxesCase1List;
export const ApiKeyScopeSandboxes =
  S.Unknown as any as S.Schema<ApiKeyScopeSandboxes>;

export type ApiKeyScopeEnvironmentsCase0 = "*";
export const ApiKeyScopeEnvironmentsCase0 = S.String;

export type ApiKeyScopeEnvironmentsCase1List = Array<string>;
export const ApiKeyScopeEnvironmentsCase1List = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ApiKeyScopeEnvironmentsCase1List>;

export type ApiKeyScopeEnvironments =
  | ApiKeyScopeEnvironmentsCase0
  | ApiKeyScopeEnvironmentsCase1List;
export const ApiKeyScopeEnvironments =
  S.Unknown as any as S.Schema<ApiKeyScopeEnvironments>;

export interface ApiKeyScope {
  expiresAt?: string | null;
  actions?: ApiKeyScopeActionsList;
  sandboxes?: ApiKeyScopeSandboxes;
  environments?: ApiKeyScopeEnvironments;
  grandfathered?: boolean;
}
export const ApiKeyScope = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    expiresAt: S.optional(S.NullOr(S.String)),
    actions: S.optional(ApiKeyScopeActionsList),
    sandboxes: S.optional(ApiKeyScopeSandboxes),
    environments: S.optional(ApiKeyScopeEnvironments),
    grandfathered: S.optional(S.Boolean),
  }),
).annotate({ identifier: "ApiKeyScope" }) as any as S.Schema<ApiKeyScope>;

export interface ApiKey {
  id: string;
  name: string;
  /** Credential storage lane. Scoped secrets are never stored in the legacy hash column. */
  credentialLane: ApiKeyCredentialLane;
  keyPrefix: string;
  keyLastFour: string;
  /** Sandbox ID for a platform-managed machine key, or null for a user-created key. */
  sandboxId: string | null;
  createdAt: string;
  lastUsedAt: string | null;
  usage: ApiKeyRequestUsage;
  resources: ApiKeyResourceTotals;
  expiresAt?: string | null;
  expired?: boolean;
  expiringSoon?: boolean;
  scope?: ApiKeyScope;
}
export const ApiKey = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    name: S.String,
    credentialLane: ApiKeyCredentialLane,
    keyPrefix: S.String,
    keyLastFour: S.String,
    sandboxId: S.NullOr(S.String),
    createdAt: S.String,
    lastUsedAt: S.NullOr(S.String),
    usage: ApiKeyRequestUsage,
    resources: ApiKeyResourceTotals,
    expiresAt: S.optional(S.NullOr(S.String)),
    expired: S.optional(S.Boolean),
    expiringSoon: S.optional(S.Boolean),
    scope: S.optional(ApiKeyScope),
  }),
).annotate({ identifier: "ApiKey" }) as any as S.Schema<ApiKey>;

export type CreateScopedApiKeyResponseApiKeysList = Array<ApiKey>;
export const CreateScopedApiKeyResponseApiKeysList = /*@__PURE__*/ S.Array(
  ApiKey,
) as any as S.Schema<CreateScopedApiKeyResponseApiKeysList>;

export interface CreateScopedApiKeyResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  apiKey: ApiKey;
  /** Raw secret returned once. Store it before closing the response. */
  secret: string | Redacted.Redacted<string>;
  apiKeys: CreateScopedApiKeyResponseApiKeysList;
}
export const CreateScopedApiKeyResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    apiKey: ApiKey,
    secret: S.String.pipe(T.SensitiveValue({})),
    apiKeys: CreateScopedApiKeyResponseApiKeysList,
  }),
).annotate({
  identifier: "CreateScopedApiKeyResponse",
}) as any as S.Schema<CreateScopedApiKeyResponse>;

export type WebhookEventType =
  | "sandbox.ready"
  | "sandbox.error"
  | "sandbox.archived"
  | "sandbox.hydrated";
export const WebhookEventType = S.String;

export type CreateWebhookRequestEventsList = Array<
  WebhookEventType | (string & {})
>;
export const CreateWebhookRequestEventsList = /*@__PURE__*/ S.Array(
  WebhookEventType,
) as any as S.Schema<CreateWebhookRequestEventsList>;

export interface CreateWebhookRequest {
  name?: string | null;
  url: string;
  events: CreateWebhookRequestEventsList;
}
export const CreateWebhookRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.optional(S.NullOr(S.String)),
    url: S.String,
    events: CreateWebhookRequestEventsList,
  }).pipe(T.Http({ method: "POST", uri: "/webhooks", code: 200 })),
).annotate({
  identifier: "CreateWebhookRequest",
}) as any as S.Schema<CreateWebhookRequest>;

export type WebhookEventsList = Array<WebhookEventType>;
export const WebhookEventsList = /*@__PURE__*/ S.Array(
  WebhookEventType,
) as any as S.Schema<WebhookEventsList>;

export interface Webhook {
  id: string;
  name: string | null;
  /** Public HTTPS endpoint on port 443. */
  url: string;
  events: WebhookEventsList;
  createdAt: string;
  updatedAt: string;
}
export const Webhook = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    name: S.NullOr(S.String),
    url: S.String,
    events: WebhookEventsList,
    createdAt: S.String,
    updatedAt: S.String,
  }),
).annotate({ identifier: "Webhook" }) as any as S.Schema<Webhook>;

export interface CreateWebhookResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  webhook: Webhook;
  /** Signing secret returned only when created or rotated. */
  secret: string | Redacted.Redacted<string>;
}
export const CreateWebhookResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    webhook: Webhook,
    secret: S.String.pipe(T.SensitiveValue({})),
  }),
).annotate({
  identifier: "CreateWebhookResponse",
}) as any as S.Schema<CreateWebhookResponse>;

export interface DeleteEnvironmentRequest {
  /** Environment id returned by `GET /environments`. */
  environmentId: string;
}
export const DeleteEnvironmentRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    environmentId: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/environments/{environmentId}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "DeleteEnvironmentRequest",
}) as any as S.Schema<DeleteEnvironmentRequest>;

export interface SandboxEnvironmentResponse {
  success: boolean;
  environment?: SandboxEnvironment | null;
}
export const SandboxEnvironmentResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    success: S.Boolean,
    environment: S.optional(S.NullOr(SandboxEnvironment)),
  }),
).annotate({
  identifier: "SandboxEnvironmentResponse",
}) as any as S.Schema<SandboxEnvironmentResponse>;

export interface DeleteEnvironmentRepoRequest {
  environmentId: string;
  /** The repository `databaseId`. */
  repositoryId: string;
}
export const DeleteEnvironmentRepoRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    environmentId: S.String.pipe(T.Label()),
    repositoryId: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/environments/{environmentId}/repos/{repositoryId}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "DeleteEnvironmentRepoRequest",
}) as any as S.Schema<DeleteEnvironmentRepoRequest>;

export interface DeleteEnvironmentSecretFileRequest {
  environmentId: string;
  path: string;
}
export const DeleteEnvironmentSecretFileRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    environmentId: S.String.pipe(T.Label()),
    path: S.String.pipe(T.Query()),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/environments/{environmentId}/secret-files",
      code: 200,
    }),
  ),
).annotate({
  identifier: "DeleteEnvironmentSecretFileRequest",
}) as any as S.Schema<DeleteEnvironmentSecretFileRequest>;

export interface DeleteEnvironmentVarRequest {
  environmentId: string;
  /** Environment variable name. */
  key: string;
}
export const DeleteEnvironmentVarRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    environmentId: S.String.pipe(T.Label()),
    key: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/environments/{environmentId}/vars/{key}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "DeleteEnvironmentVarRequest",
}) as any as S.Schema<DeleteEnvironmentVarRequest>;

export interface DeleteNamedSnapshotRequest {
  name: string;
}
export const DeleteNamedSnapshotRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({ method: "DELETE", uri: "/named-snapshots/{name}", code: 200 }),
  ),
).annotate({
  identifier: "DeleteNamedSnapshotRequest",
}) as any as S.Schema<DeleteNamedSnapshotRequest>;

export interface DeleteNamedSnapshotResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  name: string;
  status: string;
}
export const DeleteNamedSnapshotResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    name: S.String,
    status: S.String,
  }),
).annotate({
  identifier: "DeleteNamedSnapshotResponse",
}) as any as S.Schema<DeleteNamedSnapshotResponse>;

export interface DeleteSandboxRequest {
  /** Public Sandbox id returned by create/list/get sandbox calls. */
  sandboxId: string;
  /** Must exactly equal the target `sandboxId` or `snapshotId`. A missing or mismatched value returns `409` without accepting deletion. */
  xAsciiConfirmDelete: string;
}
export const DeleteSandboxRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandboxId: S.String.pipe(T.Label()),
    xAsciiConfirmDelete: S.String.pipe(T.Header("X-Ascii-Confirm-Delete")),
  }).pipe(
    T.Http({ method: "DELETE", uri: "/sandboxes/{sandboxId}", code: 200 }),
  ),
).annotate({
  identifier: "DeleteSandboxRequest",
}) as any as S.Schema<DeleteSandboxRequest>;

export type DeletionOperationKind = "sandbox" | "snapshot";
export const DeletionOperationKind = S.String;

export type DeletionOperationReason = "explicit" | "zdr" | "account";
export const DeletionOperationReason = S.String;

export type DeletionOperationStatus =
  | "pending"
  | "processing"
  | "blocked"
  | "completed";
export const DeletionOperationStatus = S.String;

export interface DeletionOperation {
  id: string;
  kind: DeletionOperationKind;
  targetId: string;
  reason: DeletionOperationReason;
  status: DeletionOperationStatus;
  attemptCount: number;
  requestedAt: string;
  completedAt: string | null;
}
export const DeletionOperation = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    kind: DeletionOperationKind,
    targetId: S.String,
    reason: DeletionOperationReason,
    status: DeletionOperationStatus,
    attemptCount: S.Number,
    requestedAt: S.String,
    completedAt: S.NullOr(S.String),
  }),
).annotate({
  identifier: "DeletionOperation",
}) as any as S.Schema<DeletionOperation>;

export interface DeleteSandboxResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  operation: DeletionOperation;
}
export const DeleteSandboxResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    operation: DeletionOperation,
  }),
).annotate({
  identifier: "DeleteSandboxResponse",
}) as any as S.Schema<DeleteSandboxResponse>;

export interface DeleteSnapshotRequest {
  /** Snapshot id returned by the snapshot list/latest calls. */
  snapshotId: string;
  /** Must exactly equal the target `sandboxId` or `snapshotId`. A missing or mismatched value returns `409` without accepting deletion. */
  xAsciiConfirmDelete: string;
}
export const DeleteSnapshotRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    snapshotId: S.String.pipe(T.Label()),
    xAsciiConfirmDelete: S.String.pipe(T.Header("X-Ascii-Confirm-Delete")),
  }).pipe(
    T.Http({ method: "DELETE", uri: "/snapshots/{snapshotId}", code: 200 }),
  ),
).annotate({
  identifier: "DeleteSnapshotRequest",
}) as any as S.Schema<DeleteSnapshotRequest>;

export interface DeleteSnapshotResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  operation: DeletionOperation;
}
export const DeleteSnapshotResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    operation: DeletionOperation,
  }),
).annotate({
  identifier: "DeleteSnapshotResponse",
}) as any as S.Schema<DeleteSnapshotResponse>;

export interface DeleteWebhookRequest {
  webhookId: string;
}
export const DeleteWebhookRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    webhookId: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({ method: "DELETE", uri: "/webhooks/{webhookId}", code: 200 }),
  ),
).annotate({
  identifier: "DeleteWebhookRequest",
}) as any as S.Schema<DeleteWebhookRequest>;

export interface DeleteWebhookResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  id: string;
}
export const DeleteWebhookResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    id: S.String,
  }),
).annotate({
  identifier: "DeleteWebhookResponse",
}) as any as S.Schema<DeleteWebhookResponse>;

export interface DownloadSandboxArtifactRequest {
  /** Public Sandbox id returned by create/list/get sandbox calls. */
  sandboxId: string;
  path: string;
}
export const DownloadSandboxArtifactRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandboxId: S.String.pipe(T.Label()),
    path: S.String.pipe(T.Query()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/sandboxes/{sandboxId}/artifacts",
      code: 200,
    }),
  ),
).annotate({
  identifier: "DownloadSandboxArtifactRequest",
}) as any as S.Schema<DownloadSandboxArtifactRequest>;

export interface DownloadSandboxArtifactResponse {}
export const DownloadSandboxArtifactResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "DownloadSandboxArtifactResponse",
}) as any as S.Schema<DownloadSandboxArtifactResponse>;

export interface ExecuteSandboxCommandRequest {
  /** Public Sandbox id returned by create/list/get sandbox calls. */
  sandboxId: string;
  command: string;
  /** Relative working directory inside the sandbox work directory. */
  cwd?: string;
  /** Command timeout in seconds. Values outside 1-600 are rejected with a 400 invalid_timeout error. */
  timeoutSeconds?: number;
  /** Start the command in the background and return a process id immediately instead of waiting for it to finish. Output goes to a log file on the sandbox; poll the status endpoint for it. */
  detached?: boolean;
}
export const ExecuteSandboxCommandRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandboxId: S.String.pipe(T.Label()),
    command: S.String,
    cwd: S.optional(S.String),
    timeoutSeconds: S.optional(S.Number),
    detached: S.optional(S.Boolean),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/sandboxes/{sandboxId}/commands",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ExecuteSandboxCommandRequest",
}) as any as S.Schema<ExecuteSandboxCommandRequest>;

export interface CommandResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  success: boolean;
  exitCode: number | null;
  signal?: string | null;
  stdout: string;
  stderr: string;
  stdoutTruncated?: boolean;
  stderrTruncated?: boolean;
  timedOut: boolean;
  cwd?: string;
  startedAt?: string;
  finishedAt?: string;
}
export const CommandResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    success: S.Boolean,
    exitCode: S.NullOr(S.Number),
    signal: S.optional(S.NullOr(S.String)),
    stdout: S.String,
    stderr: S.String,
    stdoutTruncated: S.optional(S.Boolean),
    stderrTruncated: S.optional(S.Boolean),
    timedOut: S.Boolean,
    cwd: S.optional(S.String),
    startedAt: S.optional(S.String),
    finishedAt: S.optional(S.String),
  }),
).annotate({
  identifier: "CommandResponse",
}) as any as S.Schema<CommandResponse>;

export interface CommandStartedResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  success: boolean;
  /** Process id to poll with the command status endpoint. */
  processId: number;
  pid: number;
  command: string;
  cwd?: string;
  startedAt: string;
  /** Stdout log file on the sandbox (~/.ascii/processes/<pid>.log). */
  logPath?: string;
  /** Stderr log file on the sandbox. */
  errLogPath?: string;
}
export const CommandStartedResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    success: S.Boolean,
    processId: S.Number,
    pid: S.Number,
    command: S.String,
    cwd: S.optional(S.String),
    startedAt: S.String,
    logPath: S.optional(S.String),
    errLogPath: S.optional(S.String),
  }),
).annotate({
  identifier: "CommandStartedResponse",
}) as any as S.Schema<CommandStartedResponse>;

export type ExecuteSandboxCommandResponseBody =
  | CommandResponse
  | CommandStartedResponse;
export const ExecuteSandboxCommandResponseBody =
  S.Unknown as any as S.Schema<ExecuteSandboxCommandResponseBody>;

export type ExecuteSandboxCommandResponse = ExecuteSandboxCommandResponseBody;
export const ExecuteSandboxCommandResponse = /*@__PURE__*/ S.suspend(() =>
  ExecuteSandboxCommandResponseBody.pipe(T.RawResponseRoot()),
).annotate({
  identifier: "ExecuteSandboxCommandResponse",
}) as any as S.Schema<ExecuteSandboxCommandResponse>;

/** Replaces the env the fork would otherwise inherit from the source sandbox. Same validation rules as `CreateSandboxRequest.env`. */
export type ForkSandboxRequestEnvMap = { [key: string]: string | undefined };
export const ForkSandboxRequestEnvMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<ForkSandboxRequestEnvMap>;

/** Machine size for the fork. Omit to inherit the source sandbox's type. The source sandbox is never modified. Shrinking is rejected with `type_too_small` when the source's data would not fit the smaller disk. `xlarge` requires the effective $100 plan or higher and an explicit bare-metal operator allocation. */
export type ForkSandboxRequestType = "small" | "default" | "large" | "xlarge";
export const ForkSandboxRequestType = S.String;

export interface ForkSandboxRequest {
  /** Public Sandbox id returned by create/list/get sandbox calls. */
  sandboxId: string;
  /** Optional exactly-once key for creating a sandbox. Send your own opaque, account-unique value (a UUID) to make `POST /sandboxes` safe to retry when the response is lost (network timeout, 5xx): the first request creates the sandbox and binds it to the key; every later request with the **same account, key, and request body** returns that same sandbox instead of creating a second, billable one. Behavior: keys are retained for **24 hours**; a concurrent or early retry while the first sandbox is still being minted returns `409` `idempotency_in_progress` (retry shortly, same key); reusing a key with a **different body** returns `409` `idempotency_key_reused`; timeouts and 5xx are safe to retry with the same key; a create that fails before the sandbox exists releases the key within ~2 minutes so a retry can create the sandbox. Omit the header to keep the default (non-idempotent) behavior. */
  idempotencyKey?: string;
  /** Replaces the env the fork would otherwise inherit from the source sandbox. Same validation rules as `CreateSandboxRequest.env`. */
  env?: ForkSandboxRequestEnvMap;
  /** Optionally pin the fork to a different named sandbox environment. Omit to inherit the source sandbox's environment. Unknown names are rejected with `unknown_environment`. */
  environment?: string;
  /** Make the fork no-env (see `CreateSandboxRequest.noEnv`). A fork of a no-env sandbox is always no-env regardless of this field. */
  noEnv?: boolean;
  /** Machine size for the fork. Omit to inherit the source sandbox's type. The source sandbox is never modified. Shrinking is rejected with `type_too_small` when the source's data would not fit the smaller disk. `xlarge` requires the effective $100 plan or higher and an explicit bare-metal operator allocation. */
  type?: ForkSandboxRequestType | (string & {});
  /** Auto-stop for the fork, in seconds. Omit for the 1 hour default; the fork does NOT inherit the source sandbox's TTL, so forking a sandbox that has auto-stop disabled still gives you a fork that stops itself. `null` disables auto-stop, which means nothing will ever stop this sandbox for you. */
  ttlSeconds?: number | null;
}
export const ForkSandboxRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandboxId: S.String.pipe(T.Label()),
    idempotencyKey: S.optional(S.String.pipe(T.Header("Idempotency-Key"))),
    env: S.optional(ForkSandboxRequestEnvMap),
    environment: S.optional(S.String),
    noEnv: S.optional(S.Boolean),
    type: S.optional(ForkSandboxRequestType),
    ttlSeconds: S.optional(S.NullOr(S.Number)),
  }).pipe(
    T.Http({ method: "POST", uri: "/sandboxes/{sandboxId}/fork", code: 200 }),
  ),
).annotate({
  identifier: "ForkSandboxRequest",
}) as any as S.Schema<ForkSandboxRequest>;

export interface ForkSandboxResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  id: string;
  status: string;
  sandbox?: Sandbox | null;
}
export const ForkSandboxResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    id: S.String,
    status: S.String,
    sandbox: S.optional(S.NullOr(Sandbox)),
  }),
).annotate({
  identifier: "ForkSandboxResponse",
}) as any as S.Schema<ForkSandboxResponse>;

export interface GetApiKeyUsageRequest {
  /** API key ID returned by `GET /api-keys`. */
  apiKeyId: string;
}
export const GetApiKeyUsageRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    apiKeyId: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({ method: "GET", uri: "/api-keys/{apiKeyId}/usage", code: 200 }),
  ),
).annotate({
  identifier: "GetApiKeyUsageRequest",
}) as any as S.Schema<GetApiKeyUsageRequest>;

/** Credential storage lane. Scoped secrets are never stored in the legacy hash column. */
export type GetApiKeyUsageResponseCredentialLane = "legacy" | "scoped-v1";
export const GetApiKeyUsageResponseCredentialLane = S.String;

export type GetApiKeyUsageResponseScopeActionsList = Array<string>;
export const GetApiKeyUsageResponseScopeActionsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<GetApiKeyUsageResponseScopeActionsList>;

export type GetApiKeyUsageResponseScopeSandboxesCase0 = "*";
export const GetApiKeyUsageResponseScopeSandboxesCase0 = S.String;

export type GetApiKeyUsageResponseScopeSandboxesCase1List = Array<string>;
export const GetApiKeyUsageResponseScopeSandboxesCase1List =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<GetApiKeyUsageResponseScopeSandboxesCase1List>;

export type GetApiKeyUsageResponseScopeSandboxes =
  | GetApiKeyUsageResponseScopeSandboxesCase0
  | GetApiKeyUsageResponseScopeSandboxesCase1List;
export const GetApiKeyUsageResponseScopeSandboxes =
  S.Unknown as any as S.Schema<GetApiKeyUsageResponseScopeSandboxes>;

export type GetApiKeyUsageResponseScopeEnvironmentsCase0 = "*";
export const GetApiKeyUsageResponseScopeEnvironmentsCase0 = S.String;

export type GetApiKeyUsageResponseScopeEnvironmentsCase1List = Array<string>;
export const GetApiKeyUsageResponseScopeEnvironmentsCase1List =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<GetApiKeyUsageResponseScopeEnvironmentsCase1List>;

export type GetApiKeyUsageResponseScopeEnvironments =
  | GetApiKeyUsageResponseScopeEnvironmentsCase0
  | GetApiKeyUsageResponseScopeEnvironmentsCase1List;
export const GetApiKeyUsageResponseScopeEnvironments =
  S.Unknown as any as S.Schema<GetApiKeyUsageResponseScopeEnvironments>;

export interface GetApiKeyUsageResponseScope {
  expiresAt?: string | null;
  actions?: GetApiKeyUsageResponseScopeActionsList;
  sandboxes?: GetApiKeyUsageResponseScopeSandboxes;
  environments?: GetApiKeyUsageResponseScopeEnvironments;
  grandfathered?: boolean;
}
export const GetApiKeyUsageResponseScope = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    expiresAt: S.optional(S.NullOr(S.String)),
    actions: S.optional(GetApiKeyUsageResponseScopeActionsList),
    sandboxes: S.optional(GetApiKeyUsageResponseScopeSandboxes),
    environments: S.optional(GetApiKeyUsageResponseScopeEnvironments),
    grandfathered: S.optional(S.Boolean),
  }),
).annotate({
  identifier: "GetApiKeyUsageResponseScope",
}) as any as S.Schema<GetApiKeyUsageResponseScope>;

export type ApiKeyCreatedResourceKind = "sandbox" | "agent";
export const ApiKeyCreatedResourceKind = S.String;

export interface ApiKeyCreatedResource {
  kind: ApiKeyCreatedResourceKind;
  id: string;
  name: string;
  state: string;
  createdAt: string | null;
}
export const ApiKeyCreatedResource = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    kind: ApiKeyCreatedResourceKind,
    id: S.String,
    name: S.String,
    state: S.String,
    createdAt: S.NullOr(S.String),
  }),
).annotate({
  identifier: "ApiKeyCreatedResource",
}) as any as S.Schema<ApiKeyCreatedResource>;

export type GetApiKeyUsageResponseCreatedResourcesList =
  Array<ApiKeyCreatedResource>;
export const GetApiKeyUsageResponseCreatedResourcesList = /*@__PURE__*/ S.Array(
  ApiKeyCreatedResource,
) as any as S.Schema<GetApiKeyUsageResponseCreatedResourcesList>;

export interface GetApiKeyUsageResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  id: string;
  name: string;
  /** Credential storage lane. Scoped secrets are never stored in the legacy hash column. */
  credentialLane: GetApiKeyUsageResponseCredentialLane;
  keyPrefix: string;
  keyLastFour: string;
  /** Sandbox ID for a platform-managed machine key, or null for a user-created key. */
  sandboxId: string | null;
  createdAt: string;
  lastUsedAt: string | null;
  usage: ApiKeyRequestUsage;
  resources: ApiKeyResourceTotals;
  expiresAt?: string | null;
  expired?: boolean;
  expiringSoon?: boolean;
  scope?: GetApiKeyUsageResponseScope;
  createdResources: GetApiKeyUsageResponseCreatedResourcesList;
}
export const GetApiKeyUsageResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    id: S.String,
    name: S.String,
    credentialLane: GetApiKeyUsageResponseCredentialLane,
    keyPrefix: S.String,
    keyLastFour: S.String,
    sandboxId: S.NullOr(S.String),
    createdAt: S.String,
    lastUsedAt: S.NullOr(S.String),
    usage: ApiKeyRequestUsage,
    resources: ApiKeyResourceTotals,
    expiresAt: S.optional(S.NullOr(S.String)),
    expired: S.optional(S.Boolean),
    expiringSoon: S.optional(S.Boolean),
    scope: S.optional(GetApiKeyUsageResponseScope),
    createdResources: GetApiKeyUsageResponseCreatedResourcesList,
  }),
).annotate({
  identifier: "GetApiKeyUsageResponse",
}) as any as S.Schema<GetApiKeyUsageResponse>;

export interface GetCommandStatusRequest {
  /** Public Sandbox id returned by create/list/get sandbox calls. */
  sandboxId: string;
  /** Process id returned by a detached command start. */
  processId: number;
  /** Cap each returned log to its last N bytes. Defaults to 524288 (512 KiB). */
  tailBytes?: number;
}
export const GetCommandStatusRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandboxId: S.String.pipe(T.Label()),
    processId: S.Number.pipe(T.Label()),
    tailBytes: S.optional(S.Number.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/sandboxes/{sandboxId}/commands/{processId}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "GetCommandStatusRequest",
}) as any as S.Schema<GetCommandStatusRequest>;

/** lost: the sandbox agent restarted and forgot the process; running/exitCode are then a best-effort probe and the logs come from the on-disk files. */
export type GetCommandStatusResponseStatus = "running" | "exited" | "lost";
export const GetCommandStatusResponseStatus = S.String;

export interface GetCommandStatusResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  success: boolean;
  processId: number;
  pid?: number;
  /** lost: the sandbox agent restarted and forgot the process; running/exitCode are then a best-effort probe and the logs come from the on-disk files. */
  status: GetCommandStatusResponseStatus;
  /** Whether the process is still tracked by the sandbox agent. */
  known?: boolean;
  running: boolean;
  exitCode: number | null;
  signal?: string | null;
  command?: string | null;
  cwd?: string | null;
  startedAt?: string | null;
  finishedAt?: string | null;
  /** Tail of the stdout log file. */
  stdout: string;
  /** Tail of the stderr log file. */
  stderr: string;
  stdoutTruncated?: boolean;
  stderrTruncated?: boolean;
  logPath?: string;
  errLogPath?: string;
}
export const GetCommandStatusResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    success: S.Boolean,
    processId: S.Number,
    pid: S.optional(S.Number),
    status: GetCommandStatusResponseStatus,
    known: S.optional(S.Boolean),
    running: S.Boolean,
    exitCode: S.NullOr(S.Number),
    signal: S.optional(S.NullOr(S.String)),
    command: S.optional(S.NullOr(S.String)),
    cwd: S.optional(S.NullOr(S.String)),
    startedAt: S.optional(S.NullOr(S.String)),
    finishedAt: S.optional(S.NullOr(S.String)),
    stdout: S.String,
    stderr: S.String,
    stdoutTruncated: S.optional(S.Boolean),
    stderrTruncated: S.optional(S.Boolean),
    logPath: S.optional(S.String),
    errLogPath: S.optional(S.String),
  }),
).annotate({
  identifier: "GetCommandStatusResponse",
}) as any as S.Schema<GetCommandStatusResponse>;

export interface GetCurrentUserRequest {}
export const GetCurrentUserRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}).pipe(T.Http({ method: "GET", uri: "/me", code: 200 })),
).annotate({
  identifier: "GetCurrentUserRequest",
}) as any as S.Schema<GetCurrentUserRequest>;

export interface GetCurrentUserResponseUser {
  login?: string;
  email?: string | null;
  /** Whether archived sandbox data is configured for deletion instead of retention. */
  zeroDataRetention?: boolean;
  zeroDataRetentionEnabledAt?: string | null;
}
export const GetCurrentUserResponseUser = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    login: S.optional(S.String),
    email: S.optional(S.NullOr(S.String)),
    zeroDataRetention: S.optional(S.Boolean),
    zeroDataRetentionEnabledAt: S.optional(S.NullOr(S.String)),
  }),
).annotate({
  identifier: "GetCurrentUserResponseUser",
}) as any as S.Schema<GetCurrentUserResponseUser>;

export interface GetCurrentUserResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  user: GetCurrentUserResponseUser;
}
export const GetCurrentUserResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    user: GetCurrentUserResponseUser,
  }),
).annotate({
  identifier: "GetCurrentUserResponse",
}) as any as S.Schema<GetCurrentUserResponse>;

export interface GetDataRetentionRequest {}
export const GetDataRetentionRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}).pipe(
    T.Http({ method: "GET", uri: "/account/data-retention", code: 200 }),
  ),
).annotate({
  identifier: "GetDataRetentionRequest",
}) as any as S.Schema<GetDataRetentionRequest>;

export interface GetDataRetentionResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  enabled: boolean;
  enabledAt: string | null;
  /** Archived Sandboxes newly queued for deletion by this policy update. */
  queuedSandboxes?: number;
  /** Always true on updates. Disabling the policy does not cancel accepted deletion operations. */
  acceptedDeletionOperationsIrreversible?: boolean;
}
export const GetDataRetentionResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    enabled: S.Boolean,
    enabledAt: S.NullOr(S.String),
    queuedSandboxes: S.optional(S.Number),
    acceptedDeletionOperationsIrreversible: S.optional(S.Boolean),
  }),
).annotate({
  identifier: "GetDataRetentionResponse",
}) as any as S.Schema<GetDataRetentionResponse>;

export interface GetDeletionOperationRequest {
  /** Deletion operation id returned by an accepted delete request. */
  operationId: string;
}
export const GetDeletionOperationRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    operationId: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/deletion-operations/{operationId}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "GetDeletionOperationRequest",
}) as any as S.Schema<GetDeletionOperationRequest>;

export interface GetDeletionOperationResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  operation: DeletionOperation;
}
export const GetDeletionOperationResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    operation: DeletionOperation,
  }),
).annotate({
  identifier: "GetDeletionOperationResponse",
}) as any as S.Schema<GetDeletionOperationResponse>;

export type GetDesktopStreamingUrlRequestVnc = 1;
export const GetDesktopStreamingUrlRequestVnc = S.Number;

export type GetDesktopStreamingUrlRequestTheme = "light" | "dark";
export const GetDesktopStreamingUrlRequestTheme = S.String;

export interface GetDesktopStreamingUrlRequest {
  /** Public Sandbox id returned by create/list/get sandbox calls. */
  sandboxId: string;
  /** Use VNC/noVNC streaming mode. */
  vnc?: GetDesktopStreamingUrlRequestVnc | (number & {});
  /** Desktop streaming theme for non-VNC mode. */
  theme?: GetDesktopStreamingUrlRequestTheme | (string & {});
  /** For `?vnc=1`, return a noVNC URL that does not require an access token. */
  publicAccess?: boolean;
}
export const GetDesktopStreamingUrlRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandboxId: S.String.pipe(T.Label()),
    vnc: S.optional(GetDesktopStreamingUrlRequestVnc.pipe(T.Query())),
    theme: S.optional(GetDesktopStreamingUrlRequestTheme.pipe(T.Query())),
    publicAccess: S.optional(S.Boolean),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/sandboxes/{sandboxId}/desktop",
      code: 200,
    }),
  ),
).annotate({
  identifier: "GetDesktopStreamingUrlRequest",
}) as any as S.Schema<GetDesktopStreamingUrlRequest>;

export interface GetDesktopStreamingUrlResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  success?: boolean;
  /** Secret-bearing desktop or noVNC URL. Redact from logs. */
  desktopUrl?: string | null;
  ip?: string | null;
  mode?: string;
  /** For `?vnc=1`, true means VNC is still being prepared; poll again. */
  provisioning?: boolean;
  message?: string;
}
export const GetDesktopStreamingUrlResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    success: S.optional(S.Boolean),
    desktopUrl: S.optional(S.NullOr(S.String)),
    ip: S.optional(S.NullOr(S.String)),
    mode: S.optional(S.String),
    provisioning: S.optional(S.Boolean),
    message: S.optional(S.String),
  }),
).annotate({
  identifier: "GetDesktopStreamingUrlResponse",
}) as any as S.Schema<GetDesktopStreamingUrlResponse>;

export interface GetLatestSandboxSnapshotRequest {
  /** Public Sandbox id returned by create/list/get sandbox calls. */
  sandboxId: string;
}
export const GetLatestSandboxSnapshotRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandboxId: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/sandboxes/{sandboxId}/snapshots/latest",
      code: 200,
    }),
  ),
).annotate({
  identifier: "GetLatestSandboxSnapshotRequest",
}) as any as S.Schema<GetLatestSandboxSnapshotRequest>;

export type SnapshotSummaryStatus = "completed";
export const SnapshotSummaryStatus = S.String;

/** `base` (full) or `incremental` (delta on a base). `null` for legacy snapshots. */
export type SnapshotSummaryKind = "base" | "incremental";
export const SnapshotSummaryKind = S.String;

export interface SnapshotSummary {
  id: string;
  /** Public Sandbox id this snapshot belongs to. */
  sandboxId: string;
  status: SnapshotSummaryStatus;
  /** `base` (full) or `incremental` (delta on a base). `null` for legacy snapshots. */
  kind?: SnapshotSummaryKind | null;
  /** Position in the incremental chain (0 = base). */
  generation: number;
  chainId?: string | null;
  createdAt: string;
  completedAt?: string | null;
  /** Bytes this snapshot added (its delta), not the full restored size. */
  sizeBytes: number;
  /** Inventory entries alive in the chain at this generation (includes base-image system entries). */
  fileCount: number;
  /** Total bytes of your data restored by this snapshot (what resume/download returns; base image excluded). `null` on legacy snapshots. */
  contentSizeBytes?: number | null;
  /** Number of your files restored by this snapshot (base image excluded). `null` on legacy snapshots. */
  contentFileCount?: number | null;
}
export const SnapshotSummary = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    sandboxId: S.String,
    status: SnapshotSummaryStatus,
    kind: S.optional(S.NullOr(SnapshotSummaryKind)),
    generation: S.Number,
    chainId: S.optional(S.NullOr(S.String)),
    createdAt: S.String,
    completedAt: S.optional(S.NullOr(S.String)),
    sizeBytes: S.Number,
    fileCount: S.Number,
    contentSizeBytes: S.optional(S.NullOr(S.Number)),
    contentFileCount: S.optional(S.NullOr(S.Number)),
  }),
).annotate({
  identifier: "SnapshotSummary",
}) as any as S.Schema<SnapshotSummary>;

export interface GetLatestSandboxSnapshotResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  snapshot: SnapshotSummary | null;
}
export const GetLatestSandboxSnapshotResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    snapshot: S.NullOr(SnapshotSummary),
  }),
).annotate({
  identifier: "GetLatestSandboxSnapshotResponse",
}) as any as S.Schema<GetLatestSandboxSnapshotResponse>;

export interface GetLimitsRequest {
  /** Billing wallet for this request. A team id you belong to reads that team's limits / bills a create to that team. Your own account id is personal. Sandboxes, snapshots, and environments stay creator-private. */
  org?: string;
  /** Legacy alias for `org`. Takes precedence over `org` / `X-Boat-Org` when set. */
  teamId?: string;
  /** Same as the `org` query parameter. Query wins when both are set. */
  xBoatOrg?: string;
}
export const GetLimitsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    org: S.optional(S.String.pipe(T.Query())),
    teamId: S.optional(S.String.pipe(T.Query())),
    xBoatOrg: S.optional(S.String.pipe(T.Header("X-Boat-Org"))),
  }).pipe(T.Http({ method: "GET", uri: "/limits", code: 200 })),
).annotate({
  identifier: "GetLimitsRequest",
}) as any as S.Schema<GetLimitsRequest>;

export interface GetLimitsResponseCurrentLimits {
  activeSandboxes?: number;
  creationRatePerMinute?: number;
  creationRequestsPerHour?: number | null;
  creationRequestsPerDay?: number | null;
}
export const GetLimitsResponseCurrentLimits = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    activeSandboxes: S.optional(S.Number),
    creationRatePerMinute: S.optional(S.Number),
    creationRequestsPerHour: S.optional(S.NullOr(S.Number)),
    creationRequestsPerDay: S.optional(S.NullOr(S.Number)),
  }),
).annotate({
  identifier: "GetLimitsResponseCurrentLimits",
}) as any as S.Schema<GetLimitsResponseCurrentLimits>;

export type GetLimitsResponseStandardLimits = GetLimitsResponseCurrentLimits;
export const GetLimitsResponseStandardLimits = GetLimitsResponseCurrentLimits;

export type GetLimitsResponseTrialLimits = GetLimitsResponseCurrentLimits;
export const GetLimitsResponseTrialLimits = GetLimitsResponseCurrentLimits;

export type GetLimitsResponseUpgradeEffectsMap = {
  [key: string]: unknown | undefined;
};
export const GetLimitsResponseUpgradeEffectsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<GetLimitsResponseUpgradeEffectsMap>;

export type GetLimitsResponseActiveStatesList = Array<string>;
export const GetLimitsResponseActiveStatesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<GetLimitsResponseActiveStatesList>;

/** Plan caps for machine starts. Null on unlimited accounts. Create, fork and resume each count as one start. */
export interface GetLimitsResponseStartLimits {
  perMinute?: number;
  perHour?: number;
  perDay?: number;
}
export const GetLimitsResponseStartLimits = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    perMinute: S.optional(S.Number),
    perHour: S.optional(S.Number),
    perDay: S.optional(S.Number),
  }),
).annotate({
  identifier: "GetLimitsResponseStartLimits",
}) as any as S.Schema<GetLimitsResponseStartLimits>;

/** One rolling start window. Null when the account is unlimited. */
export interface StartWindowUsage {
  limit?: number;
  used?: number;
  remaining?: number;
}
export const StartWindowUsage = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    limit: S.optional(S.Number),
    used: S.optional(S.Number),
    remaining: S.optional(S.Number),
  }),
).annotate({
  identifier: "StartWindowUsage",
}) as any as S.Schema<StartWindowUsage>;

/** Remaining machine starts in the rolling minute, hour and day windows. Null windows mean the account is unlimited. */
export interface GetLimitsResponseStarts {
  unlimited?: boolean;
  minute?: StartWindowUsage | null;
  hour?: StartWindowUsage | null;
  day?: StartWindowUsage | null;
}
export const GetLimitsResponseStarts = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    unlimited: S.optional(S.Boolean),
    minute: S.optional(S.NullOr(StartWindowUsage)),
    hour: S.optional(S.NullOr(StartWindowUsage)),
    day: S.optional(S.NullOr(StartWindowUsage)),
  }),
).annotate({
  identifier: "GetLimitsResponseStarts",
}) as any as S.Schema<GetLimitsResponseStarts>;

export type GetLimitsResponsePackageMap = {
  [key: string]: unknown | undefined;
};
export const GetLimitsResponsePackageMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<GetLimitsResponsePackageMap>;

export interface GetLimitsResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  accessTier?: string;
  blockedReason?: string | null;
  currentLimits?: GetLimitsResponseCurrentLimits;
  standardLimits?: GetLimitsResponseCurrentLimits;
  trialLimits?: GetLimitsResponseCurrentLimits;
  upgradeEffects?: GetLimitsResponseUpgradeEffectsMap;
  /** Whether the authenticated account can create or operate sandboxes right now. */
  canStart: boolean;
  checkoutRequired?: boolean;
  startBlockedReason?: string | null;
  contactMessage?: string | null;
  activeSandboxes: number;
  activeStates?: GetLimitsResponseActiveStatesList;
  maxActiveSandboxes: number;
  maxCreationRequestsPerMinute?: number;
  maxCreationRequestsPerDay?: number | null;
  /** Plan caps for machine starts. Null on unlimited accounts. Create, fork and resume each count as one start. */
  startLimits?: GetLimitsResponseStartLimits | null;
  /** Remaining machine starts in the rolling minute, hour and day windows. Null windows mean the account is unlimited. */
  starts?: GetLimitsResponseStarts;
  /** Remaining machine time in hours (`creditBalanceSeconds / 3600`). Null on unlimited accounts. */
  creditBalanceHours?: number | null;
  /** Remaining purchased credit packs in hours. */
  packBalanceHours?: number;
  /** Remaining purchased credit packs in dollars. */
  packBalanceDollars?: number;
  hasPaymentHistory?: boolean;
  package?: GetLimitsResponsePackageMap;
  subscriptionQuotaSeconds?: number;
  subscriptionRemainingSeconds?: number;
  packBalanceSeconds?: number;
  creditPurchasedSeconds?: number;
  creditUsedSeconds?: number;
  liveUsageSeconds?: number;
  creditSecondsPerDollar?: number;
  /** Account access state returned by the current backend. Billing endpoints are not part of v1. */
  billingStatus: string;
  subscriptionStatus?: string | null;
  subscriptionCancelAtPeriodEnd?: boolean;
  hasSubscription?: boolean;
  subscriptionTrialEndsAt?: string | null;
  subscriptionCurrentPeriodEnd?: string | null;
  creditBalanceSeconds?: number;
  /** Present when limits were read for a team wallet (`?teamId=`, `?org=`, or `X-Boat-Org`). */
  teamId?: string;
  /** Caller's role on that team when `teamId` is present. */
  teamRole?: string;
}
export const GetLimitsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    accessTier: S.optional(S.String),
    blockedReason: S.optional(S.NullOr(S.String)),
    currentLimits: S.optional(GetLimitsResponseCurrentLimits),
    standardLimits: S.optional(GetLimitsResponseCurrentLimits),
    trialLimits: S.optional(GetLimitsResponseCurrentLimits),
    upgradeEffects: S.optional(GetLimitsResponseUpgradeEffectsMap),
    canStart: S.Boolean,
    checkoutRequired: S.optional(S.Boolean),
    startBlockedReason: S.optional(S.NullOr(S.String)),
    contactMessage: S.optional(S.NullOr(S.String)),
    activeSandboxes: S.Number,
    activeStates: S.optional(GetLimitsResponseActiveStatesList),
    maxActiveSandboxes: S.Number,
    maxCreationRequestsPerMinute: S.optional(S.Number),
    maxCreationRequestsPerDay: S.optional(S.NullOr(S.Number)),
    startLimits: S.optional(S.NullOr(GetLimitsResponseStartLimits)),
    starts: S.optional(GetLimitsResponseStarts),
    creditBalanceHours: S.optional(S.NullOr(S.Number)),
    packBalanceHours: S.optional(S.Number),
    packBalanceDollars: S.optional(S.Number),
    hasPaymentHistory: S.optional(S.Boolean),
    package: S.optional(GetLimitsResponsePackageMap),
    subscriptionQuotaSeconds: S.optional(S.Number),
    subscriptionRemainingSeconds: S.optional(S.Number),
    packBalanceSeconds: S.optional(S.Number),
    creditPurchasedSeconds: S.optional(S.Number),
    creditUsedSeconds: S.optional(S.Number),
    liveUsageSeconds: S.optional(S.Number),
    creditSecondsPerDollar: S.optional(S.Number),
    billingStatus: S.String,
    subscriptionStatus: S.optional(S.NullOr(S.String)),
    subscriptionCancelAtPeriodEnd: S.optional(S.Boolean),
    hasSubscription: S.optional(S.Boolean),
    subscriptionTrialEndsAt: S.optional(S.NullOr(S.String)),
    subscriptionCurrentPeriodEnd: S.optional(S.NullOr(S.String)),
    creditBalanceSeconds: S.optional(S.Number),
    teamId: S.optional(S.String),
    teamRole: S.optional(S.String),
  }),
).annotate({
  identifier: "GetLimitsResponse",
}) as any as S.Schema<GetLimitsResponse>;

export interface GetNamedSnapshotRequest {
  name: string;
}
export const GetNamedSnapshotRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/named-snapshots/{name}", code: 200 })),
).annotate({
  identifier: "GetNamedSnapshotRequest",
}) as any as S.Schema<GetNamedSnapshotRequest>;

/** `saving` while the capture and pin are in flight (a live source sandbox takes a fresh snapshot first, which can run minutes), `ready` when deployable, `failed` if the save did not complete (see `error`; save again to retry). */
export type NamedSnapshotStatus = "saving" | "ready" | "failed";
export const NamedSnapshotStatus = S.String;

/** A named snapshot: a frozen copy of a sandbox's disk at one moment, saved under a name you pick. Independent of the source sandbox's later life: the sandbox can change, stop, or be deleted and the named snapshot still deploys. Named snapshots never expire; re-saving a name replaces its artifact. */
export interface NamedSnapshot {
  /** The user-chosen handle, unique within your account. */
  name: string;
  /** `saving` while the capture and pin are in flight (a live source sandbox takes a fresh snapshot first, which can run minutes), `ready` when deployable, `failed` if the save did not complete (see `error`; save again to retry). */
  status: NamedSnapshotStatus;
  /** Failure reason. Only present when `status` is `failed`. */
  error?: string;
  /** The sandbox this snapshot was saved from (display only). */
  sourceSandboxId: string;
  /** The frozen artifact behind the name. Present once `status` is `ready`. Accepted by `GET /api/boat/snapshots/{snapshotId}/tree` to browse its files. */
  snapshotId?: string;
  /** Sandbox type the snapshot was saved from. Deploys default to it. */
  type?: string;
  /** Restored content size of the frozen state, in bytes. */
  sizeBytes?: number;
  createdAt: string;
}
export const NamedSnapshot = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    name: S.String,
    status: NamedSnapshotStatus,
    error: S.optional(S.String),
    sourceSandboxId: S.String,
    snapshotId: S.optional(S.String),
    type: S.optional(S.String),
    sizeBytes: S.optional(S.Number),
    createdAt: S.String,
  }),
).annotate({ identifier: "NamedSnapshot" }) as any as S.Schema<NamedSnapshot>;

export interface GetNamedSnapshotResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  snapshot: NamedSnapshot;
}
export const GetNamedSnapshotResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    snapshot: NamedSnapshot,
  }),
).annotate({
  identifier: "GetNamedSnapshotResponse",
}) as any as S.Schema<GetNamedSnapshotResponse>;

export interface GetPromptRunStatusRequest {
  /** Public Sandbox id returned by create/list/get sandbox calls. */
  sandboxId: string;
  promptId: string;
}
export const GetPromptRunStatusRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandboxId: S.String.pipe(T.Label()),
    promptId: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/sandboxes/{sandboxId}/prompts/{promptId}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "GetPromptRunStatusRequest",
}) as any as S.Schema<GetPromptRunStatusRequest>;

export type PromptRunStatus =
  | "sending"
  | "queued"
  | "running"
  | "finished"
  | "failed"
  | "interrupted";
export const PromptRunStatus = S.String;

export interface PromptRun {
  id: string;
  promptId: string;
  sandboxId: string;
  status: PromptRunStatus;
  done: boolean;
  createdAt?: string | null;
  model?: string | null;
  reasoningEffort?: string | null;
  /** The conversation this prompt ran in. A Sandbox runs many conversations in parallel; see [Integrated agents](/integrated-agents). */
  conversationId?: string | null;
}
export const PromptRun = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    promptId: S.String,
    sandboxId: S.String,
    status: PromptRunStatus,
    done: S.Boolean,
    createdAt: S.optional(S.NullOr(S.String)),
    model: S.optional(S.NullOr(S.String)),
    reasoningEffort: S.optional(S.NullOr(S.String)),
    conversationId: S.optional(S.NullOr(S.String)),
  }),
).annotate({ identifier: "PromptRun" }) as any as S.Schema<PromptRun>;

export interface GetPromptRunStatusResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  id: string;
  promptRun: PromptRun;
}
export const GetPromptRunStatusResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    id: S.String,
    promptRun: PromptRun,
  }),
).annotate({
  identifier: "GetPromptRunStatusResponse",
}) as any as S.Schema<GetPromptRunStatusResponse>;

export interface GetSandboxRequest {
  /** Public Sandbox id returned by create/list/get sandbox calls. */
  sandboxId: string;
}
export const GetSandboxRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandboxId: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/sandboxes/{sandboxId}", code: 200 })),
).annotate({
  identifier: "GetSandboxRequest",
}) as any as S.Schema<GetSandboxRequest>;

export interface GetSandboxResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  sandbox: Sandbox;
}
export const GetSandboxResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    sandbox: Sandbox,
  }),
).annotate({
  identifier: "GetSandboxResponse",
}) as any as S.Schema<GetSandboxResponse>;

export interface GetSandboxUsageRequest {
  /** Public Sandbox id returned by create/list/get sandbox calls. */
  sandboxId: string;
  /** Count from this time, ISO 8601 or Unix epoch seconds. Default is the sandbox's creation. */
  since?: string;
  /** Count up to this time, ISO 8601 or Unix epoch seconds. Default is now. */
  until?: string;
}
export const GetSandboxUsageRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandboxId: S.String.pipe(T.Label()),
    since: S.optional(S.String.pipe(T.Query())),
    until: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({ method: "GET", uri: "/sandboxes/{sandboxId}/usage", code: 200 }),
  ),
).annotate({
  identifier: "GetSandboxUsageRequest",
}) as any as S.Schema<GetSandboxUsageRequest>;

/** The sandbox's current machine size, which sets `billingMultiplier`. */
export type GetSandboxUsageResponseSandboxType =
  | "small"
  | "default"
  | "large"
  | "xlarge";
export const GetSandboxUsageResponseSandboxType = S.String;

export interface GetSandboxUsageResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  sandboxId: string;
  /** The sandbox's current machine size, which sets `billingMultiplier`. */
  sandboxType: GetSandboxUsageResponseSandboxType;
  /** Rate at which this sandbox consumes machine time. 0.5 for `small`, 1 for `default`, 2 for `large`, and 50/9 for `xlarge`. */
  billingMultiplier: number;
  /** Start of the window the figures cover. The sandbox's creation time when the request did not pass `since`. */
  since: string;
  /** End of the window. Now when the request did not pass `until`, or passed one in the future. */
  until: string;
  /** Billable machine-seconds inside the window, the type multiplier already applied: a `large` sandbox that ran ten minutes reads 1200. Includes a running sandbox's time up to `until`. Time while stopped is never counted and time past a refused stop is excluded. */
  seconds: number;
  /** `seconds` at list price (`seconds / secondsPerDollar`), whatever plan, trial or gift actually paid for it. Up to six decimals. */
  dollars: number;
  /** How many billable seconds one dollar buys, so you can price `seconds` yourself. */
  secondsPerDollar: number;
  /** The meter is still moving: the sandbox is up and no refused stop is holding it paused. Read again after it stops for the final figure. `false` on a sandbox that is up but paused past a refused stop, whose figure will not change until it is used again. */
  running: boolean;
}
export const GetSandboxUsageResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    sandboxId: S.String,
    sandboxType: GetSandboxUsageResponseSandboxType,
    billingMultiplier: S.Number,
    since: S.String,
    until: S.String,
    seconds: S.Number,
    dollars: S.Number,
    secondsPerDollar: S.Number,
    running: S.Boolean,
  }),
).annotate({
  identifier: "GetSandboxUsageResponse",
}) as any as S.Schema<GetSandboxUsageResponse>;

export interface GetSecretsRequest {}
export const GetSecretsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}).pipe(T.Http({ method: "GET", uri: "/secrets", code: 200 })),
).annotate({
  identifier: "GetSecretsRequest",
}) as any as S.Schema<GetSecretsRequest>;

export type GetSecretsResponseSecretFilesList = Array<SecretFile>;
export const GetSecretsResponseSecretFilesList = /*@__PURE__*/ S.Array(
  SecretFile,
) as any as S.Schema<GetSecretsResponseSecretFilesList>;

/** Present on update; counts how many active sandboxes received the new environment. */
export type GetSecretsResponsePushedMap = {
  [key: string]: unknown | undefined;
};
export const GetSecretsResponsePushedMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<GetSecretsResponsePushedMap>;

export interface GetSecretsResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  success?: boolean;
  environmentId: string;
  envContents: string;
  secretFiles: GetSecretsResponseSecretFilesList;
  /** Present on update; counts how many active sandboxes received the new environment. */
  pushed?: GetSecretsResponsePushedMap;
}
export const GetSecretsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    success: S.optional(S.Boolean),
    environmentId: S.String,
    envContents: S.String,
    secretFiles: GetSecretsResponseSecretFilesList,
    pushed: S.optional(GetSecretsResponsePushedMap),
  }),
).annotate({
  identifier: "GetSecretsResponse",
}) as any as S.Schema<GetSecretsResponse>;

export interface GetSnapshotDownloadRequest {
  /** Snapshot id returned by the snapshot list/latest calls. */
  snapshotId: string;
}
export const GetSnapshotDownloadRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    snapshotId: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/snapshots/{snapshotId}/download",
      code: 200,
    }),
  ),
).annotate({
  identifier: "GetSnapshotDownloadRequest",
}) as any as S.Schema<GetSnapshotDownloadRequest>;

export type GetSnapshotDownloadResponseKind = "base" | "incremental" | "legacy";
export const GetSnapshotDownloadResponseKind = S.String;

export interface GetSnapshotDownloadResponseInventory {
  r2Key: string;
  signedUrl: string;
}
export const GetSnapshotDownloadResponseInventory = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      r2Key: S.String,
      signedUrl: S.String,
    }),
).annotate({
  identifier: "GetSnapshotDownloadResponseInventory",
}) as any as S.Schema<GetSnapshotDownloadResponseInventory>;

export interface SnapshotChunk {
  snapshotId: string;
  generation: number;
  chunkIndex: number;
  r2Key: string;
  sizeBytes: number;
  sha256: string;
  /** Time-limited download URL (see `expiresInSeconds`). */
  signedUrl: string;
}
export const SnapshotChunk = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    snapshotId: S.String,
    generation: S.Number,
    chunkIndex: S.Number,
    r2Key: S.String,
    sizeBytes: S.Number,
    sha256: S.String,
    signedUrl: S.String,
  }),
).annotate({ identifier: "SnapshotChunk" }) as any as S.Schema<SnapshotChunk>;

/** Every chunk across the chain (all generations up to and including this snapshot), ordered by `(generation, chunkIndex)`. */
export type GetSnapshotDownloadResponseChunksList = Array<SnapshotChunk>;
export const GetSnapshotDownloadResponseChunksList = /*@__PURE__*/ S.Array(
  SnapshotChunk,
) as any as S.Schema<GetSnapshotDownloadResponseChunksList>;

export interface GetSnapshotDownloadResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  snapshotId: string;
  sandboxId: string;
  kind: GetSnapshotDownloadResponseKind;
  generation: number;
  /** Lifetime of every `signedUrl` in this response. */
  expiresInSeconds: number;
  /** Human-readable note on how to reassemble the chunks into a filesystem. */
  reconstruct: string;
  inventory?: GetSnapshotDownloadResponseInventory | null;
  /** Every chunk across the chain (all generations up to and including this snapshot), ordered by `(generation, chunkIndex)`. */
  chunks: GetSnapshotDownloadResponseChunksList;
}
export const GetSnapshotDownloadResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    snapshotId: S.String,
    sandboxId: S.String,
    kind: GetSnapshotDownloadResponseKind,
    generation: S.Number,
    expiresInSeconds: S.Number,
    reconstruct: S.String,
    inventory: S.optional(S.NullOr(GetSnapshotDownloadResponseInventory)),
    chunks: GetSnapshotDownloadResponseChunksList,
  }),
).annotate({
  identifier: "GetSnapshotDownloadResponse",
}) as any as S.Schema<GetSnapshotDownloadResponse>;

export interface GetSnapshotFileRequest {
  /** Snapshot id returned by the snapshot list/latest calls. */
  snapshotId: string;
  /** File or folder path inside the snapshot. Empty for the whole snapshot. */
  path?: string;
}
export const GetSnapshotFileRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    snapshotId: S.String.pipe(T.Label()),
    path: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({ method: "GET", uri: "/snapshots/{snapshotId}/files", code: 200 }),
  ),
).annotate({
  identifier: "GetSnapshotFileRequest",
}) as any as S.Schema<GetSnapshotFileRequest>;

export interface GetSnapshotFileResponse {}
export const GetSnapshotFileResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "GetSnapshotFileResponse",
}) as any as S.Schema<GetSnapshotFileResponse>;

export interface GetSnapshotTreeRequest {
  /** Snapshot id returned by the snapshot list/latest calls. */
  snapshotId: string;
}
export const GetSnapshotTreeRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    snapshotId: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({ method: "GET", uri: "/snapshots/{snapshotId}/tree", code: 200 }),
  ),
).annotate({
  identifier: "GetSnapshotTreeRequest",
}) as any as S.Schema<GetSnapshotTreeRequest>;

export type SnapshotTreeEntryKind = "file" | "dir" | "symlink";
export const SnapshotTreeEntryKind = S.String;

export interface SnapshotTreeEntry {
  /** Path relative to the snapshot root. Docker named volumes appear under `__dockervol__/`. */
  path: string;
  kind: SnapshotTreeEntryKind;
  /** File size in bytes. Omitted for directories. */
  size?: number;
}
export const SnapshotTreeEntry = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    path: S.String,
    kind: SnapshotTreeEntryKind,
    size: S.optional(S.Number),
  }),
).annotate({
  identifier: "SnapshotTreeEntry",
}) as any as S.Schema<SnapshotTreeEntry>;

/** Exactly the files/dirs a resume or download returns, your data only; base-image system entries are not listed. */
export type GetSnapshotTreeResponseEntriesList = Array<SnapshotTreeEntry>;
export const GetSnapshotTreeResponseEntriesList = /*@__PURE__*/ S.Array(
  SnapshotTreeEntry,
) as any as S.Schema<GetSnapshotTreeResponseEntriesList>;

export interface GetSnapshotTreeResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  snapshotId: string;
  sandboxId: string;
  generation: number;
  /** `false` for legacy snapshots or inventories too large to expand; see `reason`. */
  treeAvailable: boolean;
  /** `true` when the file list was capped; not every entry is returned. */
  truncated: boolean;
  /** Number of your files in this snapshot (base-image system files excluded). */
  fileCount: number;
  /** Total bytes of your data in this snapshot (base-image system files excluded). */
  totalSizeBytes: number;
  /** Exactly the files/dirs a resume or download returns, your data only; base-image system entries are not listed. */
  entries: GetSnapshotTreeResponseEntriesList;
  /** Why the tree is unavailable, when `treeAvailable` is `false`. */
  reason?: string;
}
export const GetSnapshotTreeResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    snapshotId: S.String,
    sandboxId: S.String,
    generation: S.Number,
    treeAvailable: S.Boolean,
    truncated: S.Boolean,
    fileCount: S.Number,
    totalSizeBytes: S.Number,
    entries: GetSnapshotTreeResponseEntriesList,
    reason: S.optional(S.String),
  }),
).annotate({
  identifier: "GetSnapshotTreeResponse",
}) as any as S.Schema<GetSnapshotTreeResponse>;

export interface GetWebhookRequest {
  webhookId: string;
}
export const GetWebhookRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    webhookId: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/webhooks/{webhookId}", code: 200 })),
).annotate({
  identifier: "GetWebhookRequest",
}) as any as S.Schema<GetWebhookRequest>;

export interface GetWebhookResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  webhook: Webhook;
}
export const GetWebhookResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    webhook: Webhook,
  }),
).annotate({
  identifier: "GetWebhookResponse",
}) as any as S.Schema<GetWebhookResponse>;

export interface HostPortRequest {
  /** Public Sandbox id returned by create/list/get sandbox calls. */
  sandboxId: string;
  /** Port the service listens on inside the sandbox. */
  port: number;
  /** Return an ungated URL instead of one that requires the `_token` query parameter. */
  public?: boolean;
  /** Display title for the hosted port. */
  title?: string;
}
export const HostPortRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandboxId: S.String.pipe(T.Label()),
    port: S.Number,
    public: S.optional(S.Boolean),
    title: S.optional(S.String),
  }).pipe(
    T.Http({ method: "POST", uri: "/sandboxes/{sandboxId}/host", code: 200 }),
  ),
).annotate({
  identifier: "HostPortRequest",
}) as any as S.Schema<HostPortRequest>;

export type HostPortResponseAccess = "private" | "public";
export const HostPortResponseAccess = S.String;

export interface HostPortResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  success?: boolean;
  port?: number;
  /** Public HTTPS URL. Carries the `_token` query parameter unless the port is public; redact from logs. */
  url?: string;
  isProtected?: boolean;
  access?: HostPortResponseAccess;
}
export const HostPortResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    success: S.optional(S.Boolean),
    port: S.optional(S.Number),
    url: S.optional(S.String),
    isProtected: S.optional(S.Boolean),
    access: S.optional(HostPortResponseAccess),
  }),
).annotate({
  identifier: "HostPortResponse",
}) as any as S.Schema<HostPortResponse>;

export interface InterruptSandboxRequest {
  /** Public Sandbox id returned by create/list/get sandbox calls. */
  sandboxId: string;
  /** Interrupt only this conversation id. Omit to interrupt the whole sandbox. */
  conversation?: string;
}
export const InterruptSandboxRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandboxId: S.String.pipe(T.Label()),
    conversation: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/sandboxes/{sandboxId}/interrupt",
      code: 200,
    }),
  ),
).annotate({
  identifier: "InterruptSandboxRequest",
}) as any as S.Schema<InterruptSandboxRequest>;

export interface InterruptSandboxResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  id: string;
  status: string;
  sandbox?: Sandbox | null;
}
export const InterruptSandboxResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    id: S.String,
    status: S.String,
    sandbox: S.optional(S.NullOr(Sandbox)),
  }),
).annotate({
  identifier: "InterruptSandboxResponse",
}) as any as S.Schema<InterruptSandboxResponse>;

export interface ListApiKeysRequest {}
export const ListApiKeysRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}).pipe(T.Http({ method: "GET", uri: "/api-keys", code: 200 })),
).annotate({
  identifier: "ListApiKeysRequest",
}) as any as S.Schema<ListApiKeysRequest>;

export type ListApiKeysResponseApiKeysList = Array<ApiKey>;
export const ListApiKeysResponseApiKeysList = /*@__PURE__*/ S.Array(
  ApiKey,
) as any as S.Schema<ListApiKeysResponseApiKeysList>;

export type ApiKeyCatalogActionsList = Array<string>;
export const ApiKeyCatalogActionsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ApiKeyCatalogActionsList>;

export type ApiKeyCatalogPresetsValueList = Array<string>;
export const ApiKeyCatalogPresetsValueList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ApiKeyCatalogPresetsValueList>;

export type ApiKeyCatalogPresetsMap = {
  [key: string]: ApiKeyCatalogPresetsValueList | undefined;
};
export const ApiKeyCatalogPresetsMap = /*@__PURE__*/ S.Record(
  S.String,
  ApiKeyCatalogPresetsValueList,
) as any as S.Schema<ApiKeyCatalogPresetsMap>;

export interface ApiKeyScopeResource {
  id: string;
  name: string;
  organizationId: string;
  organizationName: string;
}
export const ApiKeyScopeResource = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    name: S.String,
    organizationId: S.String,
    organizationName: S.String,
  }),
).annotate({
  identifier: "ApiKeyScopeResource",
}) as any as S.Schema<ApiKeyScopeResource>;

export type ApiKeyCatalogScopeResourcesSandboxesList =
  Array<ApiKeyScopeResource>;
export const ApiKeyCatalogScopeResourcesSandboxesList = /*@__PURE__*/ S.Array(
  ApiKeyScopeResource,
) as any as S.Schema<ApiKeyCatalogScopeResourcesSandboxesList>;

export type ApiKeyCatalogScopeResourcesEnvironmentsList =
  Array<ApiKeyScopeResource>;
export const ApiKeyCatalogScopeResourcesEnvironmentsList =
  /*@__PURE__*/ S.Array(
    ApiKeyScopeResource,
  ) as any as S.Schema<ApiKeyCatalogScopeResourcesEnvironmentsList>;

/** Session-only selector resources. Omitted when the caller authenticates with an API key. */
export interface ApiKeyCatalogScopeResources {
  sandboxes: ApiKeyCatalogScopeResourcesSandboxesList;
  environments: ApiKeyCatalogScopeResourcesEnvironmentsList;
}
export const ApiKeyCatalogScopeResources = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandboxes: ApiKeyCatalogScopeResourcesSandboxesList,
    environments: ApiKeyCatalogScopeResourcesEnvironmentsList,
  }),
).annotate({
  identifier: "ApiKeyCatalogScopeResources",
}) as any as S.Schema<ApiKeyCatalogScopeResources>;

export interface ApiKeyCatalog {
  actions: ApiKeyCatalogActionsList;
  presets: ApiKeyCatalogPresetsMap;
  defaultTtl: string;
  maxTtl: string;
  /** Whether POST /api-keys/scoped currently accepts creation requests. */
  scopedCreationEnabled: boolean;
  /** Session-only selector resources. Omitted when the caller authenticates with an API key. */
  scopeResources?: ApiKeyCatalogScopeResources;
}
export const ApiKeyCatalog = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    actions: ApiKeyCatalogActionsList,
    presets: ApiKeyCatalogPresetsMap,
    defaultTtl: S.String,
    maxTtl: S.String,
    scopedCreationEnabled: S.Boolean,
    scopeResources: S.optional(ApiKeyCatalogScopeResources),
  }),
).annotate({ identifier: "ApiKeyCatalog" }) as any as S.Schema<ApiKeyCatalog>;

export interface ListApiKeysResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  apiKeys: ListApiKeysResponseApiKeysList;
  catalog: ApiKeyCatalog;
}
export const ListApiKeysResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    apiKeys: ListApiKeysResponseApiKeysList,
    catalog: ApiKeyCatalog,
  }),
).annotate({
  identifier: "ListApiKeysResponse",
}) as any as S.Schema<ListApiKeysResponse>;

export interface ListEnvironmentsRequest {}
export const ListEnvironmentsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}).pipe(T.Http({ method: "GET", uri: "/environments", code: 200 })),
).annotate({
  identifier: "ListEnvironmentsRequest",
}) as any as S.Schema<ListEnvironmentsRequest>;

export interface ListNamedSnapshotsRequest {}
export const ListNamedSnapshotsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}).pipe(
    T.Http({ method: "GET", uri: "/named-snapshots", code: 200 }),
  ),
).annotate({
  identifier: "ListNamedSnapshotsRequest",
}) as any as S.Schema<ListNamedSnapshotsRequest>;

export type ListNamedSnapshotsResponseSnapshotsList = Array<NamedSnapshot>;
export const ListNamedSnapshotsResponseSnapshotsList = /*@__PURE__*/ S.Array(
  NamedSnapshot,
) as any as S.Schema<ListNamedSnapshotsResponseSnapshotsList>;

export interface ListNamedSnapshotsResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  snapshots: ListNamedSnapshotsResponseSnapshotsList;
}
export const ListNamedSnapshotsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    snapshots: ListNamedSnapshotsResponseSnapshotsList,
  }),
).annotate({
  identifier: "ListNamedSnapshotsResponse",
}) as any as S.Schema<ListNamedSnapshotsResponse>;

export type ListReposRequestSort = "asc" | "desc";
export const ListReposRequestSort = S.String;

export interface ListReposRequest {
  /** When true, sync from GitHub before returning repository groups. */
  sync?: boolean;
  /** Maximum items to return. */
  limit?: number;
  /** Opaque pagination cursor returned as `pageInfo.nextCursor`. */
  cursor?: string;
  /** Sort direction for cursor pagination. */
  sort?: ListReposRequestSort | (string & {});
  /** Case-insensitive repository name/fullName filter. */
  q?: string;
  /** Filter to selected or unselected repositories. */
  selected?: boolean;
}
export const ListReposRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sync: S.optional(S.Boolean.pipe(T.Query())),
    limit: S.optional(S.Number.pipe(T.Query())),
    cursor: S.optional(S.String.pipe(T.Query())),
    sort: S.optional(ListReposRequestSort.pipe(T.Query())),
    q: S.optional(S.String.pipe(T.Query())),
    selected: S.optional(S.Boolean.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/repos", code: 200 })),
).annotate({
  identifier: "ListReposRequest",
}) as any as S.Schema<ListReposRequest>;

export interface Repository {
  /** GitHub repository id. */
  id?: number;
  /** Internal repository id used when selecting repositories. */
  databaseId?: string;
  name?: string;
  fullName?: string;
  private?: boolean;
  permissions?: string;
  pushedAt?: string | null;
}
export const Repository = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.optional(S.Number),
    databaseId: S.optional(S.String),
    name: S.optional(S.String),
    fullName: S.optional(S.String),
    private: S.optional(S.Boolean),
    permissions: S.optional(S.String),
    pushedAt: S.optional(S.NullOr(S.String)),
  }),
).annotate({ identifier: "Repository" }) as any as S.Schema<Repository>;

export type RepositoryInstallationRepositoriesList = Array<Repository>;
export const RepositoryInstallationRepositoriesList = /*@__PURE__*/ S.Array(
  Repository,
) as any as S.Schema<RepositoryInstallationRepositoriesList>;

export interface RepositoryInstallation {
  type?: string;
  accountLogin?: string;
  accountAvatarUrl?: string | null;
  repositories?: RepositoryInstallationRepositoriesList;
}
export const RepositoryInstallation = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    type: S.optional(S.String),
    accountLogin: S.optional(S.String),
    accountAvatarUrl: S.optional(S.NullOr(S.String)),
    repositories: S.optional(RepositoryInstallationRepositoriesList),
  }),
).annotate({
  identifier: "RepositoryInstallation",
}) as any as S.Schema<RepositoryInstallation>;

export type ListReposResponseInstallationsList = Array<RepositoryInstallation>;
export const ListReposResponseInstallationsList = /*@__PURE__*/ S.Array(
  RepositoryInstallation,
) as any as S.Schema<ListReposResponseInstallationsList>;

export type ListReposResponseSelectedRepositoriesList =
  Array<SelectedRepository>;
export const ListReposResponseSelectedRepositoriesList = /*@__PURE__*/ S.Array(
  SelectedRepository,
) as any as S.Schema<ListReposResponseSelectedRepositoriesList>;

export interface PageInfo {
  nextCursor: string | null;
  hasMore: boolean;
  limit: number;
}
export const PageInfo = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    nextCursor: S.NullOr(S.String),
    hasMore: S.Boolean,
    limit: S.Number,
  }),
).annotate({ identifier: "PageInfo" }) as any as S.Schema<PageInfo>;

export interface ListReposResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  installations: ListReposResponseInstallationsList;
  environmentId: string;
  selectedRepositories: ListReposResponseSelectedRepositoriesList;
  pageInfo?: PageInfo;
}
export const ListReposResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    installations: ListReposResponseInstallationsList,
    environmentId: S.String,
    selectedRepositories: ListReposResponseSelectedRepositoriesList,
    pageInfo: S.optional(PageInfo),
  }),
).annotate({
  identifier: "ListReposResponse",
}) as any as S.Schema<ListReposResponse>;

export interface ListSandboxConversationsRequest {
  /** Public Sandbox id returned by create/list/get sandbox calls. */
  sandboxId: string;
}
export const ListSandboxConversationsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandboxId: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/sandboxes/{sandboxId}/conversations",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ListSandboxConversationsRequest",
}) as any as S.Schema<ListSandboxConversationsRequest>;

export interface Conversation {
  /** Conversation id. Pass it as `conversationId` on `POST /prompt` to continue this thread. */
  id: string;
  /** When the first prompt of this conversation was queued. */
  createdAt: string | null;
  /** When its most recent prompt was queued. The list is sorted by this, newest first. */
  lastPromptAt: string | null;
  /** Number of prompts sent to this conversation. */
  prompts: number;
  /** `true` while a turn is queued or running in this conversation. */
  running: boolean;
  /** Harness the most recent prompt ran on (`claude`, `codex`, `pi`, `opencode`, `prime-agent`, `kimi`). */
  lastHarness: string | null;
  /** Model id of the most recent prompt that chose one, or `null` when every prompt used the harness default. */
  lastModel: string | null;
  /** The first 80 characters of the most recent prompt. */
  lastPromptPreview: string | null;
  /** `true` on the conversation a `POST /prompt` without `new` or `conversationId` would continue: the sandbox's most recently prompted one. Exactly one conversation is current unless the list is empty. */
  current: boolean;
}
export const Conversation = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
    createdAt: S.NullOr(S.String),
    lastPromptAt: S.NullOr(S.String),
    prompts: S.Number,
    running: S.Boolean,
    lastHarness: S.NullOr(S.String),
    lastModel: S.NullOr(S.String),
    lastPromptPreview: S.NullOr(S.String),
    current: S.Boolean,
  }),
).annotate({ identifier: "Conversation" }) as any as S.Schema<Conversation>;

export type ListSandboxConversationsResponseConversationsList =
  Array<Conversation>;
export const ListSandboxConversationsResponseConversationsList =
  /*@__PURE__*/ S.Array(
    Conversation,
  ) as any as S.Schema<ListSandboxConversationsResponseConversationsList>;

export interface ListSandboxConversationsResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  /** Sandbox id. */
  id: string;
  conversations: ListSandboxConversationsResponseConversationsList;
}
export const ListSandboxConversationsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    id: S.String,
    conversations: ListSandboxConversationsResponseConversationsList,
  }),
).annotate({
  identifier: "ListSandboxConversationsResponse",
}) as any as S.Schema<ListSandboxConversationsResponse>;

export type ListSandboxesRequestSort = "asc" | "desc";
export const ListSandboxesRequestSort = S.String;

export interface ListSandboxesRequest {
  /** Maximum items to return. */
  limit?: number;
  /** Opaque pagination cursor returned as `pageInfo.nextCursor`. */
  cursor?: string;
  /** Sort direction for cursor pagination. */
  sort?: ListSandboxesRequestSort | (string & {});
  /** Comma-separated sandbox state filter, for example `ready,idle,running`. */
  state?: string;
}
export const ListSandboxesRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    limit: S.optional(S.Number.pipe(T.Query())),
    cursor: S.optional(S.String.pipe(T.Query())),
    sort: S.optional(ListSandboxesRequestSort.pipe(T.Query())),
    state: S.optional(S.String.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/sandboxes", code: 200 })),
).annotate({
  identifier: "ListSandboxesRequest",
}) as any as S.Schema<ListSandboxesRequest>;

export type ListSandboxesResponseSandboxesList = Array<Sandbox>;
export const ListSandboxesResponseSandboxesList = /*@__PURE__*/ S.Array(
  Sandbox,
) as any as S.Schema<ListSandboxesResponseSandboxesList>;

export interface ListSandboxesResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  sandboxes: ListSandboxesResponseSandboxesList;
  pageInfo?: PageInfo;
}
export const ListSandboxesResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    sandboxes: ListSandboxesResponseSandboxesList,
    pageInfo: S.optional(PageInfo),
  }),
).annotate({
  identifier: "ListSandboxesResponse",
}) as any as S.Schema<ListSandboxesResponse>;

export type ListSandboxEventsRequestSort = "asc" | "desc";
export const ListSandboxEventsRequestSort = S.String;

export interface ListSandboxEventsRequest {
  /** Public Sandbox id returned by create/list/get sandbox calls. */
  sandboxId: string;
  /** Maximum items to return. */
  limit?: number;
  /** Opaque pagination cursor returned as `pageInfo.nextCursor`. */
  cursor?: string;
  /** Sort direction for cursor pagination. */
  sort?: ListSandboxEventsRequestSort | (string & {});
  /** Comma-separated event type filter, for example `prompt,response,steer`. */
  type?: string;
  /** Only return events for this conversation id. Repeat the parameter (or comma-separate) for several. Omit to stream every conversation on the sandbox. See [Integrated agents](/integrated-agents). */
  conversation?: string;
}
export const ListSandboxEventsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandboxId: S.String.pipe(T.Label()),
    limit: S.optional(S.Number.pipe(T.Query())),
    cursor: S.optional(S.String.pipe(T.Query())),
    sort: S.optional(ListSandboxEventsRequestSort.pipe(T.Query())),
    type: S.optional(S.String.pipe(T.Query())),
    conversation: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({ method: "GET", uri: "/sandboxes/{sandboxId}/events", code: 200 }),
  ),
).annotate({
  identifier: "ListSandboxEventsRequest",
}) as any as S.Schema<ListSandboxEventsRequest>;

export type SandboxEventDataMap = { [key: string]: unknown | undefined };
export const SandboxEventDataMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<SandboxEventDataMap>;

export interface SandboxEvent {
  id?: string;
  type: string;
  timestamp?: number;
  taskId?: string | null;
  /** The conversation this event belongs to. Filter the stream with `?conversation=<id>`. See [Integrated agents](/integrated-agents). */
  conversationId?: string | null;
  data?: SandboxEventDataMap;
}
export const SandboxEvent = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.optional(S.String),
    type: S.String,
    timestamp: S.optional(S.Number),
    taskId: S.optional(S.NullOr(S.String)),
    conversationId: S.optional(S.NullOr(S.String)),
    data: S.optional(SandboxEventDataMap),
  }),
).annotate({ identifier: "SandboxEvent" }) as any as S.Schema<SandboxEvent>;

/** Sandbox work and lifecycle event objects. Event shapes are intentionally extensible; branch on each event `type` when present. */
export type ListSandboxEventsResponseEventsList = Array<SandboxEvent>;
export const ListSandboxEventsResponseEventsList = /*@__PURE__*/ S.Array(
  SandboxEvent,
) as any as S.Schema<ListSandboxEventsResponseEventsList>;

export interface ListSandboxEventsResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  id: string;
  /** Sandbox work and lifecycle event objects. Event shapes are intentionally extensible; branch on each event `type` when present. */
  events: ListSandboxEventsResponseEventsList;
  pageInfo?: PageInfo;
}
export const ListSandboxEventsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    id: S.String,
    events: ListSandboxEventsResponseEventsList,
    pageInfo: S.optional(PageInfo),
  }),
).annotate({
  identifier: "ListSandboxEventsResponse",
}) as any as S.Schema<ListSandboxEventsResponse>;

export type ListSandboxSnapshotsRequestSort = "asc" | "desc";
export const ListSandboxSnapshotsRequestSort = S.String;

export interface ListSandboxSnapshotsRequest {
  /** Public Sandbox id returned by create/list/get sandbox calls. */
  sandboxId: string;
  /** Maximum items to return. */
  limit?: number;
  /** Opaque pagination cursor returned as `pageInfo.nextCursor`. */
  cursor?: string;
  /** Sort direction for cursor pagination. */
  sort?: ListSandboxSnapshotsRequestSort | (string & {});
}
export const ListSandboxSnapshotsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandboxId: S.String.pipe(T.Label()),
    limit: S.optional(S.Number.pipe(T.Query())),
    cursor: S.optional(S.String.pipe(T.Query())),
    sort: S.optional(ListSandboxSnapshotsRequestSort.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/sandboxes/{sandboxId}/snapshots",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ListSandboxSnapshotsRequest",
}) as any as S.Schema<ListSandboxSnapshotsRequest>;

export type ListSandboxSnapshotsResponseSnapshotsList = Array<SnapshotSummary>;
export const ListSandboxSnapshotsResponseSnapshotsList = /*@__PURE__*/ S.Array(
  SnapshotSummary,
) as any as S.Schema<ListSandboxSnapshotsResponseSnapshotsList>;

export interface ListSandboxSnapshotsResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  snapshots: ListSandboxSnapshotsResponseSnapshotsList;
  pageInfo?: PageInfo;
}
export const ListSandboxSnapshotsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    snapshots: ListSandboxSnapshotsResponseSnapshotsList,
    pageInfo: S.optional(PageInfo),
  }),
).annotate({
  identifier: "ListSandboxSnapshotsResponse",
}) as any as S.Schema<ListSandboxSnapshotsResponse>;

export type ListSnapshotsRequestSort = "asc" | "desc";
export const ListSnapshotsRequestSort = S.String;

export interface ListSnapshotsRequest {
  /** Maximum items to return. */
  limit?: number;
  /** Opaque pagination cursor returned as `pageInfo.nextCursor`. */
  cursor?: string;
  /** Sort direction for cursor pagination. */
  sort?: ListSnapshotsRequestSort | (string & {});
}
export const ListSnapshotsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    limit: S.optional(S.Number.pipe(T.Query())),
    cursor: S.optional(S.String.pipe(T.Query())),
    sort: S.optional(ListSnapshotsRequestSort.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/snapshots", code: 200 })),
).annotate({
  identifier: "ListSnapshotsRequest",
}) as any as S.Schema<ListSnapshotsRequest>;

export type ListSnapshotsResponseSnapshotsList = Array<SnapshotSummary>;
export const ListSnapshotsResponseSnapshotsList = /*@__PURE__*/ S.Array(
  SnapshotSummary,
) as any as S.Schema<ListSnapshotsResponseSnapshotsList>;

export interface ListSnapshotsResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  snapshots: ListSnapshotsResponseSnapshotsList;
  pageInfo?: PageInfo;
}
export const ListSnapshotsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    snapshots: ListSnapshotsResponseSnapshotsList,
    pageInfo: S.optional(PageInfo),
  }),
).annotate({
  identifier: "ListSnapshotsResponse",
}) as any as S.Schema<ListSnapshotsResponse>;

export interface ListWebhooksRequest {}
export const ListWebhooksRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}).pipe(T.Http({ method: "GET", uri: "/webhooks", code: 200 })),
).annotate({
  identifier: "ListWebhooksRequest",
}) as any as S.Schema<ListWebhooksRequest>;

export type ListWebhooksResponseWebhooksList = Array<Webhook>;
export const ListWebhooksResponseWebhooksList = /*@__PURE__*/ S.Array(
  Webhook,
) as any as S.Schema<ListWebhooksResponseWebhooksList>;

export interface ListWebhooksResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  webhooks: ListWebhooksResponseWebhooksList;
}
export const ListWebhooksResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    webhooks: ListWebhooksResponseWebhooksList,
  }),
).annotate({
  identifier: "ListWebhooksResponse",
}) as any as S.Schema<ListWebhooksResponse>;

export type PromptSandboxRequestProvider =
  | "codex"
  | "claude-code"
  | "claude"
  | "pi"
  | "opencode"
  | "prime-agent"
  | "prime"
  | "kimi"
  | "kimi-code";
export const PromptSandboxRequestProvider = S.String;

export interface PromptSandboxRequest {
  /** Public Sandbox id returned by create/list/get sandbox calls. */
  sandboxId: string;
  provider: PromptSandboxRequestProvider | (string & {});
  /** Optional provider model id from `GET /provider-models`. Omit to use the model selected for that harness on the Agents dashboard. Unknown explicit ids are currently forwarded rather than rejected by request validation. */
  model?: string | null;
  /** Optional reasoning/thinking level (e.g. `none`, `low`, `medium`, `high`, `xhigh`, `max`). Which levels a given model accepts is listed per model in `GET /provider-models`; some models accept none. */
  reasoningEffort?: string | null;
  /** Start a NEW conversation on the sandbox (runs in parallel with any existing ones) instead of continuing the most-recently-active one. Mutually exclusive with `conversationId`. */
  new?: boolean;
  /** Continue a specific conversation by id (as returned by a previous prompt). Omit (and omit `new`) to continue the sandbox's most-recently-active conversation. */
  conversationId?: string | null;
  /** Natural-language task for the sandbox, including repo, preview, or browser-use instructions. */
  prompt: string;
}
export const PromptSandboxRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandboxId: S.String.pipe(T.Label()),
    provider: PromptSandboxRequestProvider,
    model: S.optional(S.NullOr(S.String)),
    reasoningEffort: S.optional(S.NullOr(S.String)),
    new: S.optional(S.Boolean),
    conversationId: S.optional(S.NullOr(S.String)),
    prompt: S.String,
  }).pipe(
    T.Http({ method: "POST", uri: "/sandboxes/{sandboxId}/prompt", code: 200 }),
  ),
).annotate({
  identifier: "PromptSandboxRequest",
}) as any as S.Schema<PromptSandboxRequest>;

export type PromptSandboxResponseStatus = "queued";
export const PromptSandboxResponseStatus = S.String;

export interface PromptSandboxResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  /** Sandbox id. */
  id: string;
  promptId: string;
  /** The conversation this prompt was queued in. A new one when `new` was set, the one named by `conversationId`, or the sandbox's most-recently-active conversation. See [Integrated agents](/integrated-agents). */
  conversationId?: string | null;
  promptRun: PromptRun;
  status: PromptSandboxResponseStatus;
  provider: string;
  model?: string | null;
  reasoningEffort?: string | null;
}
export const PromptSandboxResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    id: S.String,
    promptId: S.String,
    conversationId: S.optional(S.NullOr(S.String)),
    promptRun: PromptRun,
    status: PromptSandboxResponseStatus,
    provider: S.String,
    model: S.optional(S.NullOr(S.String)),
    reasoningEffort: S.optional(S.NullOr(S.String)),
  }),
).annotate({
  identifier: "PromptSandboxResponse",
}) as any as S.Schema<PromptSandboxResponse>;

export type ReadSandboxFileRequestEncoding = "utf8" | "base64";
export const ReadSandboxFileRequestEncoding = S.String;

export interface ReadSandboxFileRequest {
  /** Public Sandbox id returned by create/list/get sandbox calls. */
  sandboxId: string;
  path: string;
  encoding?: ReadSandboxFileRequestEncoding | (string & {});
}
export const ReadSandboxFileRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandboxId: S.String.pipe(T.Label()),
    path: S.String.pipe(T.Query()),
    encoding: S.optional(ReadSandboxFileRequestEncoding.pipe(T.Query())),
  }).pipe(
    T.Http({ method: "GET", uri: "/sandboxes/{sandboxId}/files", code: 200 }),
  ),
).annotate({
  identifier: "ReadSandboxFileRequest",
}) as any as S.Schema<ReadSandboxFileRequest>;

export type ReadSandboxFileResponseEncoding = "utf8" | "base64";
export const ReadSandboxFileResponseEncoding = S.String;

export interface ReadSandboxFileResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  success: boolean;
  path: string;
  encoding: ReadSandboxFileResponseEncoding;
  size: number;
  content: string;
}
export const ReadSandboxFileResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    success: S.Boolean,
    path: S.String,
    encoding: ReadSandboxFileResponseEncoding,
    size: S.Number,
    content: S.String,
  }),
).annotate({
  identifier: "ReadSandboxFileResponse",
}) as any as S.Schema<ReadSandboxFileResponse>;

/** Resume onto a different machine size. Omit to keep the sandbox's current type. A resume already restores onto a fresh machine, so changing size costs nothing extra. Shrinking is rejected with `type_too_small` when the sandbox's data would not fit the smaller disk. `xlarge` requires the effective $100 plan or higher and an explicit bare-metal operator allocation. */
export type ResumeSandboxRequestType = "small" | "default" | "large" | "xlarge";
export const ResumeSandboxRequestType = S.String;

/** Replaces the sandbox's per-sandbox environment variables. Omit to keep the sandbox's current env. Same validation rules as `CreateSandboxRequest.env`. */
export type ResumeSandboxRequestEnvMap = { [key: string]: string | undefined };
export const ResumeSandboxRequestEnvMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<ResumeSandboxRequestEnvMap>;

export interface ResumeSandboxRequest {
  /** Public Sandbox id returned by create/list/get sandbox calls. */
  sandboxId: string;
  /** Resume onto a different machine size. Omit to keep the sandbox's current type. A resume already restores onto a fresh machine, so changing size costs nothing extra. Shrinking is rejected with `type_too_small` when the sandbox's data would not fit the smaller disk. `xlarge` requires the effective $100 plan or higher and an explicit bare-metal operator allocation. */
  type?: ResumeSandboxRequestType | (string & {});
  /** Replaces the sandbox's per-sandbox environment variables. Omit to keep the sandbox's current env. Same validation rules as `CreateSandboxRequest.env`. */
  env?: ResumeSandboxRequestEnvMap;
  /** Optionally re-pin the sandbox to a different named sandbox environment on resume. Omit to keep the sandbox's current environment. Unknown names are rejected with `unknown_environment`. */
  environment?: string;
  /** Resume into a no-env sandbox: withhold account secrets and scrub inherited owner secrets from the restored snapshot before the sandbox is exposed. One-way; once a sandbox is resumed as no-env it stays no-env on later resumes. */
  noEnv?: boolean;
  /** Auto-stop for the resumed sandbox, in seconds, stored on the sandbox. Omit to keep the TTL the sandbox already had. `null` disables auto-stop, which means nothing will ever stop this sandbox for you. */
  ttlSeconds?: number | null;
}
export const ResumeSandboxRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandboxId: S.String.pipe(T.Label()),
    type: S.optional(ResumeSandboxRequestType),
    env: S.optional(ResumeSandboxRequestEnvMap),
    environment: S.optional(S.String),
    noEnv: S.optional(S.Boolean),
    ttlSeconds: S.optional(S.NullOr(S.Number)),
  }).pipe(
    T.Http({ method: "POST", uri: "/sandboxes/{sandboxId}/resume", code: 200 }),
  ),
).annotate({
  identifier: "ResumeSandboxRequest",
}) as any as S.Schema<ResumeSandboxRequest>;

export interface ResumeSandboxResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  id: string;
  status: string;
  sandbox?: Sandbox | null;
}
export const ResumeSandboxResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    id: S.String,
    status: S.String,
    sandbox: S.optional(S.NullOr(Sandbox)),
  }),
).annotate({
  identifier: "ResumeSandboxResponse",
}) as any as S.Schema<ResumeSandboxResponse>;

export interface RevokeApiKeyRequest {
  apiKeyId: string;
}
export const RevokeApiKeyRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    apiKeyId: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "DELETE", uri: "/api-keys/{apiKeyId}", code: 200 })),
).annotate({
  identifier: "RevokeApiKeyRequest",
}) as any as S.Schema<RevokeApiKeyRequest>;

export type RevokeApiKeyResponseApiKeysList = Array<ApiKey>;
export const RevokeApiKeyResponseApiKeysList = /*@__PURE__*/ S.Array(
  ApiKey,
) as any as S.Schema<RevokeApiKeyResponseApiKeysList>;

export interface RevokeApiKeyResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  apiKeys: RevokeApiKeyResponseApiKeysList;
}
export const RevokeApiKeyResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    apiKeys: RevokeApiKeyResponseApiKeysList,
  }),
).annotate({
  identifier: "RevokeApiKeyResponse",
}) as any as S.Schema<RevokeApiKeyResponse>;

export interface RotateApiKeyRequest {
  apiKeyId: string;
}
export const RotateApiKeyRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    apiKeyId: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({ method: "POST", uri: "/api-keys/{apiKeyId}/rotate", code: 200 }),
  ),
).annotate({
  identifier: "RotateApiKeyRequest",
}) as any as S.Schema<RotateApiKeyRequest>;

export type RotateApiKeyResponseApiKeysList = Array<ApiKey>;
export const RotateApiKeyResponseApiKeysList = /*@__PURE__*/ S.Array(
  ApiKey,
) as any as S.Schema<RotateApiKeyResponseApiKeysList>;

export interface RotateApiKeyResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  apiKey: ApiKey;
  /** Raw secret returned once. Store it before closing the response. */
  secret: string | Redacted.Redacted<string>;
  apiKeys: RotateApiKeyResponseApiKeysList;
}
export const RotateApiKeyResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    apiKey: ApiKey,
    secret: S.String.pipe(T.SensitiveValue({})),
    apiKeys: RotateApiKeyResponseApiKeysList,
  }),
).annotate({
  identifier: "RotateApiKeyResponse",
}) as any as S.Schema<RotateApiKeyResponse>;

export interface RotateWebhookSigningSecretRequest {
  webhookId: string;
}
export const RotateWebhookSigningSecretRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    webhookId: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({ method: "POST", uri: "/webhooks/{webhookId}/rotate", code: 200 }),
  ),
).annotate({
  identifier: "RotateWebhookSigningSecretRequest",
}) as any as S.Schema<RotateWebhookSigningSecretRequest>;

export interface RotateWebhookSigningSecretResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  webhook: Webhook;
  /** Signing secret returned only when created or rotated. */
  secret: string | Redacted.Redacted<string>;
}
export const RotateWebhookSigningSecretResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    webhook: Webhook,
    secret: S.String.pipe(T.SensitiveValue({})),
  }),
).annotate({
  identifier: "RotateWebhookSigningSecretResponse",
}) as any as S.Schema<RotateWebhookSigningSecretResponse>;

export interface SaveNamedSnapshotRequest {
  /** The sandbox whose current state to freeze. */
  sandboxId: string;
  /** Name to save under. 1-63 lowercase letters, digits, or dashes, starting with a letter or digit. Reusing one of your existing names replaces that snapshot's artifact. `latest`, `tree`, `pull`, `rm`, `save`, `current`, `self`, and `new` are reserved. */
  name: string;
}
export const SaveNamedSnapshotRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandboxId: S.String,
    name: S.String,
  }).pipe(T.Http({ method: "POST", uri: "/named-snapshots", code: 200 })),
).annotate({
  identifier: "SaveNamedSnapshotRequest",
}) as any as S.Schema<SaveNamedSnapshotRequest>;

export interface SaveNamedSnapshotResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  status: string;
  snapshot: NamedSnapshot;
}
export const SaveNamedSnapshotResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    status: S.String,
    snapshot: NamedSnapshot,
  }),
).annotate({
  identifier: "SaveNamedSnapshotResponse",
}) as any as S.Schema<SaveNamedSnapshotResponse>;

export interface SelectRepoRequest {
  /** Internal repository `databaseId` returned by `GET /repos`. */
  repositoryId: string;
  baseBranch?: string;
}
export const SelectRepoRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    repositoryId: S.String,
    baseBranch: S.optional(S.String),
  }).pipe(T.Http({ method: "POST", uri: "/repos", code: 200 })),
).annotate({
  identifier: "SelectRepoRequest",
}) as any as S.Schema<SelectRepoRequest>;

export type SelectRepoResponseSelectedRepositoriesList =
  Array<SelectedRepository>;
export const SelectRepoResponseSelectedRepositoriesList = /*@__PURE__*/ S.Array(
  SelectedRepository,
) as any as S.Schema<SelectRepoResponseSelectedRepositoriesList>;

export interface SelectRepoResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  success: boolean;
  environmentId: string;
  selectedRepositories: SelectRepoResponseSelectedRepositoriesList;
}
export const SelectRepoResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    success: S.Boolean,
    environmentId: S.String,
    selectedRepositories: SelectRepoResponseSelectedRepositoriesList,
  }),
).annotate({
  identifier: "SelectRepoResponse",
}) as any as S.Schema<SelectRepoResponse>;

export interface SetEnvironmentSecretFileRequest {
  environmentId: string;
  /** In-sandbox path relative to the workspace, e.g. `.env` or `repo/.env`. */
  path: string;
  contents: string;
}
export const SetEnvironmentSecretFileRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    environmentId: S.String.pipe(T.Label()),
    path: S.String,
    contents: S.String,
  }).pipe(
    T.Http({
      method: "PUT",
      uri: "/environments/{environmentId}/secret-files",
      code: 200,
    }),
  ),
).annotate({
  identifier: "SetEnvironmentSecretFileRequest",
}) as any as S.Schema<SetEnvironmentSecretFileRequest>;

export interface SetEnvironmentVarRequest {
  environmentId: string;
  /** Environment variable name. */
  key: string;
  value: string;
}
export const SetEnvironmentVarRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    environmentId: S.String.pipe(T.Label()),
    key: S.String.pipe(T.Label()),
    value: S.String,
  }).pipe(
    T.Http({
      method: "PUT",
      uri: "/environments/{environmentId}/vars/{key}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "SetEnvironmentVarRequest",
}) as any as S.Schema<SetEnvironmentVarRequest>;

export interface SteerSandboxRequest {
  /** Public Sandbox id returned by create/list/get sandbox calls. */
  sandboxId: string;
  /** Conversation id to steer. Also accepted in the body. Omit to steer the sandbox's most-recently-active conversation. */
  conversation?: string;
  /** The extra instruction for the running turn. */
  message: string;
}
export const SteerSandboxRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandboxId: S.String.pipe(T.Label()),
    conversation: S.optional(S.String.pipe(T.Query())),
    message: S.String,
  }).pipe(
    T.Http({ method: "POST", uri: "/sandboxes/{sandboxId}/steer", code: 200 }),
  ),
).annotate({
  identifier: "SteerSandboxRequest",
}) as any as S.Schema<SteerSandboxRequest>;

/** How the message was delivered. `native` and `fallback` mirror the `native` field; `late` means the turn finished between the request and its delivery, so the message ran as an ordinary new turn on the conversation. A steer reported `native` here can settle as `native-continued` on the `steer` event: the harness accepted it but its turn ended without acting on it, so Boat continued it immediately as its own turn on the same session. Read the event for the settled mode. */
export type SteerSandboxResponseMode = "native" | "fallback" | "late";
export const SteerSandboxResponseMode = S.String;

export type SteerSandboxResponseStatus = "steered";
export const SteerSandboxResponseStatus = S.String;

export interface SteerSandboxResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  /** Sandbox id. */
  id: string;
  /** The conversation whose running turn was steered. */
  conversationId: string;
  /** Id of the steer record. It appears in `GET /events` as a `steer` event; it is not a prompt run and has no lifecycle to poll. */
  promptId: string;
  /** `true` when the harness accepted the message into the turn that was already running, so nothing was interrupted. `false` when Boat interrupted that turn and immediately continued the SAME conversation with the instruction, which keeps the harness session and all of its memory but may repeat a little of the work in flight. */
  native: boolean;
  /** How the message was delivered. `native` and `fallback` mirror the `native` field; `late` means the turn finished between the request and its delivery, so the message ran as an ordinary new turn on the conversation. A steer reported `native` here can settle as `native-continued` on the `steer` event: the harness accepted it but its turn ended without acting on it, so Boat continued it immediately as its own turn on the same session. Read the event for the settled mode. */
  mode?: SteerSandboxResponseMode;
  status: SteerSandboxResponseStatus;
}
export const SteerSandboxResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    id: S.String,
    conversationId: S.String,
    promptId: S.String,
    native: S.Boolean,
    mode: S.optional(SteerSandboxResponseMode),
    status: SteerSandboxResponseStatus,
  }),
).annotate({
  identifier: "SteerSandboxResponse",
}) as any as S.Schema<SteerSandboxResponse>;

export interface StopSandboxRequest {
  /** Public Sandbox id returned by create/list/get sandbox calls. */
  sandboxId: string;
  /** Stop the sandbox even if its disk cannot be snapshotted. Everything written since the last successful snapshot is permanently LOST. Without this, a sandbox whose snapshot pipeline is failing is left running rather than discarding your work, and you are not billed for that time. Only use this after a stop has already been refused. */
  force?: boolean;
}
export const StopSandboxRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandboxId: S.String.pipe(T.Label()),
    force: S.optional(S.Boolean),
  }).pipe(
    T.Http({ method: "POST", uri: "/sandboxes/{sandboxId}/stop", code: 200 }),
  ),
).annotate({
  identifier: "StopSandboxRequest",
}) as any as S.Schema<StopSandboxRequest>;

export interface StopSandboxResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  id: string;
  status: string;
  sandbox?: Sandbox | null;
}
export const StopSandboxResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    id: S.String,
    status: S.String,
    sandbox: S.optional(S.NullOr(Sandbox)),
  }),
).annotate({
  identifier: "StopSandboxResponse",
}) as any as S.Schema<StopSandboxResponse>;

export interface UpdateDataRetentionRequest {
  enabled: boolean;
  /** Required when enabling and must exactly equal `delete archived sandbox data`. */
  confirmation?: string;
}
export const UpdateDataRetentionRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    enabled: S.Boolean,
    confirmation: S.optional(S.String),
  }).pipe(
    T.Http({ method: "PATCH", uri: "/account/data-retention", code: 200 }),
  ),
).annotate({
  identifier: "UpdateDataRetentionRequest",
}) as any as S.Schema<UpdateDataRetentionRequest>;

export interface UpdateDataRetentionResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  enabled: boolean;
  enabledAt: string | null;
  /** Archived Sandboxes newly queued for deletion by this policy update. */
  queuedSandboxes?: number;
  /** Always true on updates. Disabling the policy does not cancel accepted deletion operations. */
  acceptedDeletionOperationsIrreversible?: boolean;
}
export const UpdateDataRetentionResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    enabled: S.Boolean,
    enabledAt: S.NullOr(S.String),
    queuedSandboxes: S.optional(S.Number),
    acceptedDeletionOperationsIrreversible: S.optional(S.Boolean),
  }),
).annotate({
  identifier: "UpdateDataRetentionResponse",
}) as any as S.Schema<UpdateDataRetentionResponse>;

export type UpdateEnvironmentRequestSecretFilesList = Array<SecretFile>;
export const UpdateEnvironmentRequestSecretFilesList = /*@__PURE__*/ S.Array(
  SecretFile,
) as any as S.Schema<UpdateEnvironmentRequestSecretFilesList>;

export interface UpdateEnvironmentRequestRepositoriesItem {
  /** Internal repository databaseId. */
  repositoryId: string;
  baseBranch?: string;
  setupScript?: string;
  setupBlocking?: boolean;
}
export const UpdateEnvironmentRequestRepositoriesItem = /*@__PURE__*/ S.suspend(
  () =>
    S.Struct({
      repositoryId: S.String,
      baseBranch: S.optional(S.String),
      setupScript: S.optional(S.String),
      setupBlocking: S.optional(S.Boolean),
    }),
).annotate({
  identifier: "UpdateEnvironmentRequestRepositoriesItem",
}) as any as S.Schema<UpdateEnvironmentRequestRepositoriesItem>;

/** Full replacement of the version's repository selection. Each item selects one repository by its internal `databaseId` (from `GET /repos`). */
export type UpdateEnvironmentRequestRepositoriesList =
  Array<UpdateEnvironmentRequestRepositoriesItem>;
export const UpdateEnvironmentRequestRepositoriesList = /*@__PURE__*/ S.Array(
  UpdateEnvironmentRequestRepositoriesItem,
) as any as S.Schema<UpdateEnvironmentRequestRepositoriesList>;

export interface UpdateEnvironmentRequest {
  /** Environment id returned by `GET /environments`. */
  environmentId: string;
  /** New environment name. */
  name?: string;
  /** Set to true to make this the default environment (clears the flag on the previous default). */
  isDefault?: boolean;
  safeForThirdParties?: boolean;
  passGithub?: boolean;
  passSecrets?: boolean;
  passSandboxCredentials?: boolean;
  passAgentsCredentials?: boolean;
  /** Full .env-style content for the new version. */
  envContents?: string;
  secretFiles?: UpdateEnvironmentRequestSecretFilesList;
  /** Full replacement of the version's repository selection. Each item selects one repository by its internal `databaseId` (from `GET /repos`). */
  repositories?: UpdateEnvironmentRequestRepositoriesList;
}
export const UpdateEnvironmentRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    environmentId: S.String.pipe(T.Label()),
    name: S.optional(S.String),
    isDefault: S.optional(S.Boolean),
    safeForThirdParties: S.optional(S.Boolean),
    passGithub: S.optional(S.Boolean),
    passSecrets: S.optional(S.Boolean),
    passSandboxCredentials: S.optional(S.Boolean),
    passAgentsCredentials: S.optional(S.Boolean),
    envContents: S.optional(S.String),
    secretFiles: S.optional(UpdateEnvironmentRequestSecretFilesList),
    repositories: S.optional(UpdateEnvironmentRequestRepositoriesList),
  }).pipe(
    T.Http({ method: "PUT", uri: "/environments/{environmentId}", code: 200 }),
  ),
).annotate({
  identifier: "UpdateEnvironmentRequest",
}) as any as S.Schema<UpdateEnvironmentRequest>;

export interface UpdateSandboxRequest {
  /** Public Sandbox id returned by create/list/get sandbox calls. */
  sandboxId: string;
  /** New display name. Empty strings are rejected; longer names are truncated to 120 chars by the backend. */
  name?: string;
  /** New archival TTL. `null` disables auto-stop. */
  ttlSeconds?: number | null;
  /** Rename the sandbox's stable subdomain (the `<subdomain>.on.boat.dev` label). Lowercase letters, digits and hyphens; no leading/trailing/double hyphens; cannot end in `-desktop` or `-<number>` (reserved for the desktop and hosted-port URLs). Must be globally unique. The base URL, desktop URL and every live `host <port>` URL are re-pointed to the new name with no downtime and their access tokens preserved; the old URLs stop resolving. On a transient routing error the rename is saved but returns 502 `gateway_error` - retry with the same value to finish activating routes. */
  subdomain?: string;
}
export const UpdateSandboxRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandboxId: S.String.pipe(T.Label()),
    name: S.optional(S.String),
    ttlSeconds: S.optional(S.NullOr(S.Number)),
    subdomain: S.optional(S.String),
  }).pipe(
    T.Http({ method: "PATCH", uri: "/sandboxes/{sandboxId}", code: 200 }),
  ),
).annotate({
  identifier: "UpdateSandboxRequest",
}) as any as S.Schema<UpdateSandboxRequest>;

export interface UpdateSandboxResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  sandbox: Sandbox;
}
export const UpdateSandboxResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    sandbox: Sandbox,
  }),
).annotate({
  identifier: "UpdateSandboxResponse",
}) as any as S.Schema<UpdateSandboxResponse>;

/** Full list of secret files to keep configured. Send existing files again if they should remain. */
export type UpdateSecretsRequestSecretFilesList = Array<SecretFile>;
export const UpdateSecretsRequestSecretFilesList = /*@__PURE__*/ S.Array(
  SecretFile,
) as any as S.Schema<UpdateSecretsRequestSecretFilesList>;

export interface UpdateSecretsRequest {
  /** Full .env-style content to sync into sandboxes. Send the complete desired file contents, not a patch. */
  envContents?: string;
  /** Full list of secret files to keep configured. Send existing files again if they should remain. */
  secretFiles?: UpdateSecretsRequestSecretFilesList;
}
export const UpdateSecretsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    envContents: S.optional(S.String),
    secretFiles: S.optional(UpdateSecretsRequestSecretFilesList),
  }).pipe(T.Http({ method: "POST", uri: "/secrets", code: 200 })),
).annotate({
  identifier: "UpdateSecretsRequest",
}) as any as S.Schema<UpdateSecretsRequest>;

export type UpdateSecretsResponseSecretFilesList = Array<SecretFile>;
export const UpdateSecretsResponseSecretFilesList = /*@__PURE__*/ S.Array(
  SecretFile,
) as any as S.Schema<UpdateSecretsResponseSecretFilesList>;

/** Present on update; counts how many active sandboxes received the new environment. */
export type UpdateSecretsResponsePushedMap = {
  [key: string]: unknown | undefined;
};
export const UpdateSecretsResponsePushedMap = /*@__PURE__*/ S.Record(
  S.String,
  S.Unknown,
) as any as S.Schema<UpdateSecretsResponsePushedMap>;

export interface UpdateSecretsResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  success?: boolean;
  environmentId: string;
  envContents: string;
  secretFiles: UpdateSecretsResponseSecretFilesList;
  /** Present on update; counts how many active sandboxes received the new environment. */
  pushed?: UpdateSecretsResponsePushedMap;
}
export const UpdateSecretsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    success: S.optional(S.Boolean),
    environmentId: S.String,
    envContents: S.String,
    secretFiles: UpdateSecretsResponseSecretFilesList,
    pushed: S.optional(UpdateSecretsResponsePushedMap),
  }),
).annotate({
  identifier: "UpdateSecretsResponse",
}) as any as S.Schema<UpdateSecretsResponse>;

export type UpdateWebhookRequestEventsList = Array<
  WebhookEventType | (string & {})
>;
export const UpdateWebhookRequestEventsList = /*@__PURE__*/ S.Array(
  WebhookEventType,
) as any as S.Schema<UpdateWebhookRequestEventsList>;

export interface UpdateWebhookRequest {
  webhookId: string;
  name?: string | null;
  url?: string;
  events?: UpdateWebhookRequestEventsList;
}
export const UpdateWebhookRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    webhookId: S.String.pipe(T.Label()),
    name: S.optional(S.NullOr(S.String)),
    url: S.optional(S.String),
    events: S.optional(UpdateWebhookRequestEventsList),
  }).pipe(T.Http({ method: "PATCH", uri: "/webhooks/{webhookId}", code: 200 })),
).annotate({
  identifier: "UpdateWebhookRequest",
}) as any as S.Schema<UpdateWebhookRequest>;

export interface UpdateWebhookResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  webhook: Webhook;
}
export const UpdateWebhookResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    webhook: Webhook,
  }),
).annotate({
  identifier: "UpdateWebhookResponse",
}) as any as S.Schema<UpdateWebhookResponse>;

/** Restrict the upgrade to these sandbox (agent) ids. Omit to upgrade all of the caller's active sandboxes that are on an older version of this environment. */
export type UpgradeEnvironmentRequestAgentIdsList = Array<string>;
export const UpgradeEnvironmentRequestAgentIdsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<UpgradeEnvironmentRequestAgentIdsList>;

export interface UpgradeEnvironmentRequest {
  environmentId: string;
  /** Restrict the upgrade to these sandbox (agent) ids. Omit to upgrade all of the caller's active sandboxes that are on an older version of this environment. */
  agentIds?: UpgradeEnvironmentRequestAgentIdsList;
}
export const UpgradeEnvironmentRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    environmentId: S.String.pipe(T.Label()),
    agentIds: S.optional(UpgradeEnvironmentRequestAgentIdsList),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/environments/{environmentId}/upgrade",
      code: 200,
    }),
  ),
).annotate({
  identifier: "UpgradeEnvironmentRequest",
}) as any as S.Schema<UpgradeEnvironmentRequest>;

export interface UpgradeSandboxEnvironmentResponse {
  success: boolean;
  upgraded: number;
  failed: number;
}
export const UpgradeSandboxEnvironmentResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    success: S.Boolean,
    upgraded: S.Number,
    failed: S.Number,
  }),
).annotate({
  identifier: "UpgradeSandboxEnvironmentResponse",
}) as any as S.Schema<UpgradeSandboxEnvironmentResponse>;

export type WriteSandboxFileRequestEncoding = "utf8" | "base64";
export const WriteSandboxFileRequestEncoding = S.String;

export interface WriteSandboxFileRequest {
  /** Public Sandbox id returned by create/list/get sandbox calls. */
  sandboxId: string;
  /** Absolute path, or path relative to the sandbox work directory (/home/user). The canonicalized path must resolve under /home/user or /tmp; anything else is rejected with a 400 invalid_path error. */
  path: string;
  content: string;
  encoding?: WriteSandboxFileRequestEncoding | (string & {});
}
export const WriteSandboxFileRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sandboxId: S.String.pipe(T.Label()),
    path: S.String,
    content: S.String,
    encoding: S.optional(WriteSandboxFileRequestEncoding),
  }).pipe(
    T.Http({ method: "PUT", uri: "/sandboxes/{sandboxId}/files", code: 200 }),
  ),
).annotate({
  identifier: "WriteSandboxFileRequest",
}) as any as S.Schema<WriteSandboxFileRequest>;

export type WriteSandboxFileResponseEncoding = "utf8" | "base64";
export const WriteSandboxFileResponseEncoding = S.String;

export interface WriteSandboxFileResponse {
  ok: boolean;
  /** Stable success envelope discriminator added by v1. */
  type: string;
  success: boolean;
  path: string;
  encoding: WriteSandboxFileResponseEncoding;
  size: number;
}
export const WriteSandboxFileResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ok: S.Boolean,
    type: S.String,
    success: S.Boolean,
    path: S.String,
    encoding: WriteSandboxFileResponseEncoding,
    size: S.Number,
  }),
).annotate({
  identifier: "WriteSandboxFileResponse",
}) as any as S.Schema<WriteSandboxFileResponse>;

export type AddEnvironmentRepoError = BadRequest | NotFound | BoatOpError;
/** Add a repository to an environment Adds a repository (or updates its base branch) by database id from the repository list. Mints a new immutable version. */
export const addEnvironmentRepo: API.OperationMethod<
  AddEnvironmentRepoRequest,
  EnvironmentItemChangeResponse,
  AddEnvironmentRepoError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AddEnvironmentRepoRequest,
  output: EnvironmentItemChangeResponse,
  errors: [BadRequest, NotFound, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type ConfigureSshKeyError = BadRequest | NotFound | BoatOpError;
/** Configure sandbox SSH key Adds an OpenSSH public key to the running sandbox so the caller can SSH as user `user`. The response carries `machineIp`, which is the host to connect to on port 22. This is also how you reach your own machine from inside a sandbox without the CLI: authorize the key, then open a reverse tunnel with stock OpenSSH, and a port on your machine answers at `127.0.0.1:<port>` inside the sandbox for as long as the ssh process runs. ```bash ssh -o ExitOnForwardFailure=yes -i ~/.ssh/id_ed25519 -N -R 7777:127.0.0.1:7777 user@<machineIp> ``` `boat forward <id> --reverse --local 7777` is the same tunnel with the key handling and redial done for you. */
export const configureSshKey: API.OperationMethod<
  ConfigureSshKeyRequest,
  ConfigureSshKeyResponse,
  ConfigureSshKeyError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ConfigureSshKeyRequest,
  output: ConfigureSshKeyResponse,
  errors: [BadRequest, NotFound, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type CreateEnvironmentError = BadRequest | Conflict | BoatOpError;
/** Create a sandbox environment Create a new named environment. It starts on version 1 with all fine-grained flags on and `safeForThirdParties` off. */
export const createEnvironment: API.OperationMethod<
  CreateEnvironmentRequest,
  SandboxEnvironmentListResponse,
  CreateEnvironmentError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateEnvironmentRequest,
  output: SandboxEnvironmentListResponse,
  errors: [BadRequest, Conflict, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type CreateSandboxError = Conflict | BoatOpError;
/** Create sandbox Provision a new cloud computer. Store the returned `sandbox.id` with your product job/session record. Send an `Idempotency-Key` header to make this call safe to retry after a lost response without creating a second billable sandbox. */
export const createSandbox: API.OperationMethod<
  CreateSandboxRequest,
  CreateSandboxResponse,
  CreateSandboxError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateSandboxRequest,
  output: CreateSandboxResponse,
  errors: [Conflict, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type CreateScopedApiKeyError = BadRequest | Forbidden | BoatOpError;
/** Create a scoped API key Creates a scoped, expiring API key. Requires a dashboard/CLI session or an admin-scoped token. The secret is returned once. This dedicated path returns a typed 503 until scoped credential creation is activated. */
export const createScopedApiKey: API.OperationMethod<
  CreateScopedApiKeyRequest,
  CreateScopedApiKeyResponse,
  CreateScopedApiKeyError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateScopedApiKeyRequest,
  output: CreateScopedApiKeyResponse,
  errors: [BadRequest, Forbidden, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type CreateWebhookError = BadRequest | Conflict | BoatOpError;
/** Create webhook Registers an account-wide endpoint for sandbox lifecycle events. The endpoint must use HTTPS on port 443 and resolve only to public addresses. Redirects are not followed during delivery. The signing secret is returned only in this response. An account can register at most 10 unique endpoint URLs. */
export const createWebhook: API.OperationMethod<
  CreateWebhookRequest,
  CreateWebhookResponse,
  CreateWebhookError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateWebhookRequest,
  output: CreateWebhookResponse,
  errors: [BadRequest, Conflict, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type DeleteEnvironmentError = NotFound | Conflict | BoatOpError;
/** Delete a sandbox environment Soft-deletes an environment. The default environment cannot be deleted, and at least one environment is always kept. */
export const deleteEnvironment: API.OperationMethod<
  DeleteEnvironmentRequest,
  SandboxEnvironmentResponse,
  DeleteEnvironmentError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteEnvironmentRequest,
  output: SandboxEnvironmentResponse,
  errors: [NotFound, Conflict, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type DeleteEnvironmentRepoError = NotFound | BoatOpError;
/** Remove a repository from an environment Removes one repository from the environment's selection. Mints a new immutable version. */
export const deleteEnvironmentRepo: API.OperationMethod<
  DeleteEnvironmentRepoRequest,
  EnvironmentItemChangeResponse,
  DeleteEnvironmentRepoError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteEnvironmentRepoRequest,
  output: EnvironmentItemChangeResponse,
  errors: [NotFound, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type DeleteEnvironmentSecretFileError = NotFound | BoatOpError;
/** Remove one secret file Removes the secret file at `path`. Mints a new immutable version. */
export const deleteEnvironmentSecretFile: API.OperationMethod<
  DeleteEnvironmentSecretFileRequest,
  EnvironmentItemChangeResponse,
  DeleteEnvironmentSecretFileError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteEnvironmentSecretFileRequest,
  output: EnvironmentItemChangeResponse,
  errors: [NotFound, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type DeleteEnvironmentVarError = NotFound | BoatOpError;
/** Remove one environment variable Removes a single variable. Mints a new immutable version. */
export const deleteEnvironmentVar: API.OperationMethod<
  DeleteEnvironmentVarRequest,
  EnvironmentItemChangeResponse,
  DeleteEnvironmentVarError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteEnvironmentVarRequest,
  output: EnvironmentItemChangeResponse,
  errors: [NotFound, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type DeleteNamedSnapshotError = NotFound | Conflict | BoatOpError;
/** Remove a named snapshot Remove a named snapshot and release its storage (`boat snapshot rm <name>` in the CLI). Sandboxes already deployed from it are unaffected. */
export const deleteNamedSnapshot: API.OperationMethod<
  DeleteNamedSnapshotRequest,
  DeleteNamedSnapshotResponse,
  DeleteNamedSnapshotError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteNamedSnapshotRequest,
  output: DeleteNamedSnapshotResponse,
  errors: [NotFound, Conflict, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type DeleteSandboxError = Forbidden | NotFound | Conflict | BoatOpError;
/** Permanently delete sandbox data Accept irreversible background deletion of the sandbox machine, snapshots, and content-bearing records. This is not archive: the sandbox cannot be resumed. Named snapshots and other shared artifacts remain independent. Poll the returned operation until `completed`. */
export const deleteSandbox: API.OperationMethod<
  DeleteSandboxRequest,
  DeleteSandboxResponse,
  DeleteSandboxError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteSandboxRequest,
  output: DeleteSandboxResponse,
  errors: [Forbidden, NotFound, Conflict, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type DeleteSnapshotError = Forbidden | NotFound | Conflict | BoatOpError;
/** Permanently delete snapshot data Accept irreversible background deletion of an unpinned snapshot owned by the authenticated account. Snapshots required by another incremental snapshot or active restore return `409`. Poll the returned operation until `completed`. */
export const deleteSnapshot: API.OperationMethod<
  DeleteSnapshotRequest,
  DeleteSnapshotResponse,
  DeleteSnapshotError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteSnapshotRequest,
  output: DeleteSnapshotResponse,
  errors: [Forbidden, NotFound, Conflict, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type DeleteWebhookError = NotFound | BoatOpError;
/** Delete webhook Deletes the endpoint and its queued deliveries. An attempt already in flight can still arrive. */
export const deleteWebhook: API.OperationMethod<
  DeleteWebhookRequest,
  DeleteWebhookResponse,
  DeleteWebhookError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteWebhookRequest,
  output: DeleteWebhookResponse,
  errors: [NotFound, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type DownloadSandboxArtifactError = BoatOpError;
/** Download a sandbox artifact */
export const downloadSandboxArtifact: API.OperationMethod<
  DownloadSandboxArtifactRequest,
  DownloadSandboxArtifactResponse,
  DownloadSandboxArtifactError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DownloadSandboxArtifactRequest,
  output: DownloadSandboxArtifactResponse,
  errors: [UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type ExecuteSandboxCommandError =
  | BadRequest
  | NotFound
  | Conflict
  | BoatOpError;
/** Execute a command in a sandbox Runs the command synchronously by default (timeout configurable via `timeoutSeconds`, 600s cap). With `detached: true` the command starts in the background and a process id is returned immediately; poll `/sandboxes/{sandboxId}/commands/{processId}` for status and logs. Returns 400 invalid_timeout when `timeoutSeconds` is not an integer in 1-600. Returns 409 boat_starting (retryable) while the sandbox is still provisioning -- wait until the sandbox state is ready before running commands. Command execution is never retried automatically: a 502 boat_direct_failed means the command may already be running on the sandbox. */
export const executeSandboxCommand: API.OperationMethod<
  ExecuteSandboxCommandRequest,
  ExecuteSandboxCommandResponse,
  ExecuteSandboxCommandError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ExecuteSandboxCommandRequest,
  output: ExecuteSandboxCommandResponse,
  errors: [BadRequest, NotFound, Conflict, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type ForkSandboxError = NotFound | Conflict | BoatOpError;
/** Fork sandbox Provision a new sandbox from an existing one. Send an `Idempotency-Key` header to make this call safe to retry after a lost response without creating a second billable fork. */
export const forkSandbox: API.OperationMethod<
  ForkSandboxRequest,
  ForkSandboxResponse,
  ForkSandboxError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ForkSandboxRequest,
  output: ForkSandboxResponse,
  errors: [NotFound, Conflict, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type GetApiKeyUsageError = NotFound | BoatOpError;
/** Get API key usage Returns the key's request total for the 30-day UTC window and the sandboxes and Agents it created that still exist, including sandboxes that never got a sandbox row. Works for revoked keys owned by the authenticated account. */
export const getApiKeyUsage: API.OperationMethod<
  GetApiKeyUsageRequest,
  GetApiKeyUsageResponse,
  GetApiKeyUsageError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetApiKeyUsageRequest,
  output: GetApiKeyUsageResponse,
  errors: [NotFound, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type GetCommandStatusError = BadRequest | NotFound | BoatOpError;
/** Get detached command status and logs */
export const getCommandStatus: API.OperationMethod<
  GetCommandStatusRequest,
  GetCommandStatusResponse,
  GetCommandStatusError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetCommandStatusRequest,
  output: GetCommandStatusResponse,
  errors: [BadRequest, NotFound, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type GetCurrentUserError = BoatOpError;
/** Get current Boat user Returns GitHub identity for the authenticated Boat account. */
export const getCurrentUser: API.OperationMethod<
  GetCurrentUserRequest,
  GetCurrentUserResponse,
  GetCurrentUserError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetCurrentUserRequest,
  output: GetCurrentUserResponse,
  errors: [UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type GetDataRetentionError = Forbidden | BoatOpError;
/** Get account data-retention policy Returns the current zero-data-retention policy. Per-Boat API keys cannot access this account-wide setting. Responses are never cached. */
export const getDataRetention: API.OperationMethod<
  GetDataRetentionRequest,
  GetDataRetentionResponse,
  GetDataRetentionError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetDataRetentionRequest,
  output: GetDataRetentionResponse,
  errors: [Forbidden, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type GetDeletionOperationError = NotFound | BoatOpError;
/** Get deletion operation Poll an accepted Boat or snapshot deletion. Only operations owned by the authenticated account are returned. Responses are never cached. */
export const getDeletionOperation: API.OperationMethod<
  GetDeletionOperationRequest,
  GetDeletionOperationResponse,
  GetDeletionOperationError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetDeletionOperationRequest,
  output: GetDeletionOperationResponse,
  errors: [NotFound, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type GetDesktopStreamingUrlError = BadRequest | NotFound | BoatOpError;
/** Get desktop streaming URL Create or fetch a secret-bearing desktop/noVNC URL for live computer-use visibility. Use `?vnc=1` for noVNC; a `provisioning: true` response means poll again. */
export const getDesktopStreamingUrl: API.OperationMethod<
  GetDesktopStreamingUrlRequest,
  GetDesktopStreamingUrlResponse,
  GetDesktopStreamingUrlError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetDesktopStreamingUrlRequest,
  output: GetDesktopStreamingUrlResponse,
  errors: [BadRequest, NotFound, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type GetLatestSandboxSnapshotError = NotFound | BoatOpError;
/** Get latest boat snapshot Return the most recent completed snapshot for this sandbox, or `null` if it has none. */
export const getLatestSandboxSnapshot: API.OperationMethod<
  GetLatestSandboxSnapshotRequest,
  GetLatestSandboxSnapshotResponse,
  GetLatestSandboxSnapshotError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetLatestSandboxSnapshotRequest,
  output: GetLatestSandboxSnapshotResponse,
  errors: [NotFound, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type GetLimitsError = BoatOpError;
/** Get Boat limits Check remaining machine starts, compute time, credits, access readiness, and concurrent-sandbox capacity for the authenticated account. Pass `org` / `X-Boat-Org` (or `teamId`) to read a team wallet you belong to. */
export const getLimits: API.OperationMethod<
  GetLimitsRequest,
  GetLimitsResponse,
  GetLimitsError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetLimitsRequest,
  output: GetLimitsResponse,
  errors: [UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type GetNamedSnapshotError = NotFound | BoatOpError;
/** Get a named snapshot */
export const getNamedSnapshot: API.OperationMethod<
  GetNamedSnapshotRequest,
  GetNamedSnapshotResponse,
  GetNamedSnapshotError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetNamedSnapshotRequest,
  output: GetNamedSnapshotResponse,
  errors: [NotFound, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type GetPromptRunStatusError = NotFound | BoatOpError;
/** Get prompt run status Returns first-class status for a queued/running/finished prompt so clients do not infer completion from sandbox state and events. */
export const getPromptRunStatus: API.OperationMethod<
  GetPromptRunStatusRequest,
  GetPromptRunStatusResponse,
  GetPromptRunStatusError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetPromptRunStatusRequest,
  output: GetPromptRunStatusResponse,
  errors: [NotFound, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type GetSandboxError = NotFound | BoatOpError;
/** Get sandbox Poll this endpoint after create, stop, resume, or fork operations. */
export const getSandbox: API.OperationMethod<
  GetSandboxRequest,
  GetSandboxResponse,
  GetSandboxError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetSandboxRequest,
  output: GetSandboxResponse,
  errors: [NotFound, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type GetSandboxUsageError = BadRequest | NotFound | BoatOpError;
/** Get boat usage Machine time one sandbox consumed, and what it costs, so you can bill your own users per sandbox. Same meter as `GET /limits`: billable seconds with the sandbox type's multiplier applied, paused past a refused stop, never counting time stopped. Narrow it to a billing period with `since` and `until`; a period boundary that falls inside a running stretch splits that stretch pro rata. Works while the sandbox runs and after it stops. */
export const getSandboxUsage: API.OperationMethod<
  GetSandboxUsageRequest,
  GetSandboxUsageResponse,
  GetSandboxUsageError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetSandboxUsageRequest,
  output: GetSandboxUsageResponse,
  errors: [BadRequest, NotFound, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type GetSecretsError = BoatOpError;
/** Get Boat secrets setup Returns the environment variables and secret files configured for sandboxes. */
export const getSecrets: API.OperationMethod<
  GetSecretsRequest,
  GetSecretsResponse,
  GetSecretsError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetSecretsRequest,
  output: GetSecretsResponse,
  errors: [UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type GetSnapshotDownloadError = NotFound | BoatOpError;
/** Get snapshot download Return time-limited signed URLs for every chunk needed to reconstruct the sandbox filesystem at this snapshot (the full chain, base through this generation), plus the inventory. Download the chunks and reassemble them as described by `reconstruct`. The `boat snapshot pull` CLI command does this for you. */
export const getSnapshotDownload: API.OperationMethod<
  GetSnapshotDownloadRequest,
  GetSnapshotDownloadResponse,
  GetSnapshotDownloadError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetSnapshotDownloadRequest,
  output: GetSnapshotDownloadResponse,
  errors: [NotFound, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type GetSnapshotFileError = NotFound | Conflict | BoatOpError;
/** Download a file or folder from a snapshot Stream a single file's bytes, or a folder subtree as a tar archive, directly out of a snapshot, the sandbox can be stopped or archived; the machine is never contacted. Use `GET /snapshots/{snapshotId}/tree` to list paths, then pass one here. Paths are relative to the snapshot root (same space as `tree` entries; docker volumes under `__dockervol__/`). An empty or `/` path downloads the whole snapshot as a tar. Folder responses set `X-Snapshot-File-Count`, `X-Snapshot-Total-Size-Bytes` and `X-Snapshot-Skipped-Base-Image-Files` headers; both response shapes set `X-Snapshot-Entry-Kind` (`file` or `dir`). */
export const getSnapshotFile: API.OperationMethod<
  GetSnapshotFileRequest,
  GetSnapshotFileResponse,
  GetSnapshotFileError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetSnapshotFileRequest,
  output: GetSnapshotFileResponse,
  errors: [NotFound, Conflict, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type GetSnapshotTreeError = NotFound | BoatOpError;
/** Get snapshot file tree List the files and folders captured in a snapshot, with sizes. Returns a flat list of entries you can render as a tree. */
export const getSnapshotTree: API.OperationMethod<
  GetSnapshotTreeRequest,
  GetSnapshotTreeResponse,
  GetSnapshotTreeError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetSnapshotTreeRequest,
  output: GetSnapshotTreeResponse,
  errors: [NotFound, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type GetWebhookError = NotFound | BoatOpError;
/** Get webhook Returns webhook metadata without the signing secret. */
export const getWebhook: API.OperationMethod<
  GetWebhookRequest,
  GetWebhookResponse,
  GetWebhookError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetWebhookRequest,
  output: GetWebhookResponse,
  errors: [NotFound, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type HostPortError = BadRequest | NotFound | BoatOpError;
/** Expose a sandbox port on a public HTTPS URL Registers a stable `https://<sandbox-subdomain>-<port>.on.boat.dev` route for a port inside the sandbox and opens the sandbox firewall for it. Idempotent: calling it again for the same sandbox and port returns the same URL and token. */
export const hostPort: API.OperationMethod<
  HostPortRequest,
  HostPortResponse,
  HostPortError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: HostPortRequest,
  output: HostPortResponse,
  errors: [BadRequest, NotFound, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type InterruptSandboxError = NotFound | Conflict | BoatOpError;
/** Interrupt running work Interrupt running agent work. By default this stops **every** conversation on the sandbox. Pass `conversation` to stop just one, leaving the others running. See [Integrated agents](/integrated-agents). */
export const interruptSandbox: API.OperationMethod<
  InterruptSandboxRequest,
  InterruptSandboxResponse,
  InterruptSandboxError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: InterruptSandboxRequest,
  output: InterruptSandboxResponse,
  errors: [NotFound, Conflict, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type ListApiKeysError = Forbidden | BoatOpError;
/** List API keys Lists API key metadata only. Raw key secrets are not returned after creation/rotation. Results include expiry and scope. A restricted API-key caller receives `apiKeys: []`; only an unrestricted account key or browser/CLI session receives the account inventory. Bearer header is unchanged; scope lives on the key. */
export const listApiKeys: API.OperationMethod<
  ListApiKeysRequest,
  ListApiKeysResponse,
  ListApiKeysError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListApiKeysRequest,
  output: ListApiKeysResponse,
  errors: [Forbidden, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type ListEnvironmentsError = BoatOpError;
/** List Sandbox environments Returns every non-deleted sandbox environment with its latest-version flags, contents, and per-version sandbox usage. A default environment named `base` is created on first access if none exists. */
export const listEnvironments: API.OperationMethod<
  ListEnvironmentsRequest,
  SandboxEnvironmentListResponse,
  ListEnvironmentsError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListEnvironmentsRequest,
  output: SandboxEnvironmentListResponse,
  errors: [UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type ListNamedSnapshotsError = BoatOpError;
/** List named snapshots List your named snapshots, newest first. */
export const listNamedSnapshots: API.OperationMethod<
  ListNamedSnapshotsRequest,
  ListNamedSnapshotsResponse,
  ListNamedSnapshotsError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListNamedSnapshotsRequest,
  output: ListNamedSnapshotsResponse,
  errors: [UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type ListReposError = Conflict | BoatOpError;
/** List GitHub repositories available to Boat Returns GitHub repositories grouped by installation plus the current selected repositories for new Sandboxes. */
export const listRepos: API.PaginatedOperationMethod<
  ListReposRequest,
  ListReposResponse,
  ListReposError,
  BoatOpContext,
  RepositoryInstallation
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListReposRequest,
    output: ListReposResponse,
    errors: [Conflict, UnknownBoatError],
    protocol: BoatProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "cursor",
      inputToken: "cursor",
      outputToken: "pageInfo.nextCursor",
      items: "installations",
      hasNextPage: "pageInfo.hasMore",
      pageSize: "limit",
    } as const,
  }),
  paginateCursor,
) as any;

export type ListSandboxConversationsError = NotFound | BoatOpError;
/** List boat conversations Every conversation on the sandbox, most recently prompted first, with what you need to pick one: how many prompts it holds, whether a turn is running in it right now, the last harness and model it ran on, a preview of its last prompt, and `current`, the one a `POST /prompt` without `new` or `conversationId` would continue. Pass an `id` back as `conversationId` on `POST /prompt` to resume that thread, or as `conversation` on `POST /steer`, `POST /interrupt` and `GET /events` to scope those. See [Integrated agents](/integrated-agents). */
export const listSandboxConversations: API.OperationMethod<
  ListSandboxConversationsRequest,
  ListSandboxConversationsResponse,
  ListSandboxConversationsError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListSandboxConversationsRequest,
  output: ListSandboxConversationsResponse,
  errors: [NotFound, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type ListSandboxesError = BoatOpError;
/** List sandboxes */
export const listSandboxes: API.PaginatedOperationMethod<
  ListSandboxesRequest,
  ListSandboxesResponse,
  ListSandboxesError,
  BoatOpContext,
  Sandbox
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListSandboxesRequest,
    output: ListSandboxesResponse,
    errors: [UnknownBoatError],
    protocol: BoatProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "cursor",
      inputToken: "cursor",
      outputToken: "pageInfo.nextCursor",
      items: "sandboxes",
      hasNextPage: "pageInfo.hasMore",
      pageSize: "limit",
    } as const,
  }),
  paginateCursor,
) as any;

export type ListSandboxEventsError = NotFound | BoatOpError;
/** List boat events Return Sandbox work, lifecycle, and progress events so product UIs can show what the sandbox is doing. A Sandbox runs many conversations in parallel; this streams **all** of them by default and every event carries a `conversationId`. Filter to one or more with `conversation`. Event payloads are extensible. Clients can long-poll this endpoint with `sort=asc` and a cursor to stream responses. Response events expose agent text, streaming partials via `data.is_streaming`, and tool calls/results via `data.tools`. */
export const listSandboxEvents: API.PaginatedOperationMethod<
  ListSandboxEventsRequest,
  ListSandboxEventsResponse,
  ListSandboxEventsError,
  BoatOpContext,
  SandboxEvent
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListSandboxEventsRequest,
    output: ListSandboxEventsResponse,
    errors: [NotFound, UnknownBoatError],
    protocol: BoatProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "cursor",
      inputToken: "cursor",
      outputToken: "pageInfo.nextCursor",
      items: "events",
      hasNextPage: "pageInfo.hasMore",
      pageSize: "limit",
    } as const,
  }),
  paginateCursor,
) as any;

export type ListSandboxSnapshotsError = BadRequest | NotFound | BoatOpError;
/** List boat snapshots List visible completed snapshots for one sandbox with uncapped cursor pagination. */
export const listSandboxSnapshots: API.PaginatedOperationMethod<
  ListSandboxSnapshotsRequest,
  ListSandboxSnapshotsResponse,
  ListSandboxSnapshotsError,
  BoatOpContext,
  SnapshotSummary
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListSandboxSnapshotsRequest,
    output: ListSandboxSnapshotsResponse,
    errors: [BadRequest, NotFound, UnknownBoatError],
    protocol: BoatProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "cursor",
      inputToken: "cursor",
      outputToken: "pageInfo.nextCursor",
      items: "snapshots",
      hasNextPage: "pageInfo.hasMore",
      pageSize: "limit",
    } as const,
  }),
  paginateCursor,
) as any;

export type ListSnapshotsError = BadRequest | BoatOpError;
/** List snapshots List visible completed snapshots across all of your sandboxes using immutable ownership attribution. Pinned, deletion-scheduled, and privacy-fenced sandbox snapshots are omitted. Follow `nextCursor` through the complete history; there is no hidden 500-snapshot cap. */
export const listSnapshots: API.PaginatedOperationMethod<
  ListSnapshotsRequest,
  ListSnapshotsResponse,
  ListSnapshotsError,
  BoatOpContext,
  SnapshotSummary
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListSnapshotsRequest,
    output: ListSnapshotsResponse,
    errors: [BadRequest, UnknownBoatError],
    protocol: BoatProtocol,
    retry: Retry.Retry,
    pagination: {
      mode: "cursor",
      inputToken: "cursor",
      outputToken: "pageInfo.nextCursor",
      items: "snapshots",
      hasNextPage: "pageInfo.hasMore",
      pageSize: "limit",
    } as const,
  }),
  paginateCursor,
) as any;

export type ListWebhooksError = BoatOpError;
/** List webhooks Lists account-wide sandbox lifecycle webhook endpoints. Signing secrets are never included. */
export const listWebhooks: API.OperationMethod<
  ListWebhooksRequest,
  ListWebhooksResponse,
  ListWebhooksError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListWebhooksRequest,
  output: ListWebhooksResponse,
  errors: [UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type PromptSandboxError = BadRequest | NotFound | Conflict | BoatOpError;
/** Prompt Sandbox Queue a natural-language work item for an agent harness (Codex, Claude Code, pi, OpenCode, or Prime Agent) inside the sandbox. Observe progress with `GET /sandboxes/{sandboxId}/events`. A Sandbox runs many conversations in parallel. Set `new: true` to start a fresh one, `conversationId` to continue a specific one, or omit both to continue the most-recently-active conversation; the response returns the `conversationId`. See [Integrated agents](/integrated-agents). */
export const promptSandbox: API.OperationMethod<
  PromptSandboxRequest,
  PromptSandboxResponse,
  PromptSandboxError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PromptSandboxRequest,
  output: PromptSandboxResponse,
  errors: [BadRequest, NotFound, Conflict, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type ReadSandboxFileError = BadRequest | BoatOpError;
/** Read a file from a sandbox */
export const readSandboxFile: API.OperationMethod<
  ReadSandboxFileRequest,
  ReadSandboxFileResponse,
  ReadSandboxFileError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ReadSandboxFileRequest,
  output: ReadSandboxFileResponse,
  errors: [BadRequest, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type ResumeSandboxError = NotFound | Conflict | BoatOpError;
/** Resume sandbox */
export const resumeSandbox: API.OperationMethod<
  ResumeSandboxRequest,
  ResumeSandboxResponse,
  ResumeSandboxError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ResumeSandboxRequest,
  output: ResumeSandboxResponse,
  errors: [NotFound, Conflict, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type RevokeApiKeyError = Forbidden | NotFound | BoatOpError;
/** Revoke an API key Revokes the key immediately. Requires a dashboard or CLI session. */
export const revokeApiKey: API.OperationMethod<
  RevokeApiKeyRequest,
  RevokeApiKeyResponse,
  RevokeApiKeyError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RevokeApiKeyRequest,
  output: RevokeApiKeyResponse,
  errors: [Forbidden, NotFound, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type RotateApiKeyError = Forbidden | NotFound | Conflict | BoatOpError;
/** Rotate an API key Replaces the raw secret without changing expiry or scope. Requires a dashboard or CLI session. */
export const rotateApiKey: API.OperationMethod<
  RotateApiKeyRequest,
  RotateApiKeyResponse,
  RotateApiKeyError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RotateApiKeyRequest,
  output: RotateApiKeyResponse,
  errors: [Forbidden, NotFound, Conflict, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type RotateWebhookSigningSecretError = NotFound | BoatOpError;
/** Rotate webhook signing secret Replaces the endpoint's signing secret. The new secret is returned only in this response; briefly accept the old secret for attempts already in flight. */
export const rotateWebhookSigningSecret: API.OperationMethod<
  RotateWebhookSigningSecretRequest,
  RotateWebhookSigningSecretResponse,
  RotateWebhookSigningSecretError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RotateWebhookSigningSecretRequest,
  output: RotateWebhookSigningSecretResponse,
  errors: [NotFound, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type SaveNamedSnapshotError =
  | BadRequest
  | NotFound
  | Conflict
  | BoatOpError;
/** Save a named snapshot Freeze a sandbox's current state under a name (`boat snapshot <id> <name>` in the CLI). Answers `202` immediately with the snapshot in `saving`; a running sandbox takes a fresh capture first, which can run minutes. Poll `GET /named-snapshots/{name}` until the status settles at `ready` or `failed`. Reusing one of your existing names replaces that snapshot's artifact once the new save is ready. */
export const saveNamedSnapshot: API.OperationMethod<
  SaveNamedSnapshotRequest,
  SaveNamedSnapshotResponse,
  SaveNamedSnapshotError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: SaveNamedSnapshotRequest,
  output: SaveNamedSnapshotResponse,
  errors: [BadRequest, NotFound, Conflict, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type SelectRepoError = BadRequest | Conflict | BoatOpError;
/** Select repository for sandboxes Idempotently selects one repository for the sandbox environment. Use `databaseId` from `GET /repos` as `repositoryId`; selecting an already-selected repository updates its `baseBranch`. */
export const selectRepo: API.OperationMethod<
  SelectRepoRequest,
  SelectRepoResponse,
  SelectRepoError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: SelectRepoRequest,
  output: SelectRepoResponse,
  errors: [BadRequest, Conflict, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type SetEnvironmentSecretFileError = BadRequest | NotFound | BoatOpError;
/** Write one secret file Adds or replaces a single secret file by path. Mints a new immutable version. */
export const setEnvironmentSecretFile: API.OperationMethod<
  SetEnvironmentSecretFileRequest,
  EnvironmentItemChangeResponse,
  SetEnvironmentSecretFileError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: SetEnvironmentSecretFileRequest,
  output: EnvironmentItemChangeResponse,
  errors: [BadRequest, NotFound, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type SetEnvironmentVarError = BadRequest | NotFound | BoatOpError;
/** Set one environment variable Sets or replaces a single variable without reading or rewriting the rest. Mints a new immutable version. */
export const setEnvironmentVar: API.OperationMethod<
  SetEnvironmentVarRequest,
  EnvironmentItemChangeResponse,
  SetEnvironmentVarError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: SetEnvironmentVarRequest,
  output: EnvironmentItemChangeResponse,
  errors: [BadRequest, NotFound, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type SteerSandboxError = BadRequest | NotFound | Conflict | BoatOpError;
/** Steer a running turn Send a new message to a turn that is already running, without losing what the agent is doing. This is what typing into a coding agent while it works does: the instruction is taken into account and the work continues. Where the harness has a native mid-turn primitive (Claude Code, Codex, pi, Prime Agent) nothing is interrupted and the response has `native: true`. Where it does not (OpenCode), Boat interrupts that turn and immediately continues the SAME conversation with the instruction, keeping the session and its memory, and the response has `native: false`. Refused with **409 `no_running_turn`** when the conversation has nothing in flight; queue work with `POST /prompt` instead. The steer shows up in `GET /events` as a `steer` event. See [Integrated agents](/integrated-agents). */
export const steerSandbox: API.OperationMethod<
  SteerSandboxRequest,
  SteerSandboxResponse,
  SteerSandboxError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: SteerSandboxRequest,
  output: SteerSandboxResponse,
  errors: [BadRequest, NotFound, Conflict, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type StopSandboxError = BadRequest | NotFound | BoatOpError;
/** Stop and archive sandbox Stop active work and archive/snapshot the sandbox for later resume or fork. A stop always saves the sandbox's disk first. If that save is failing, the stop is refused and the sandbox keeps running so your work is not discarded, we retry automatically and email you. You are not billed for time spent in that state. Set `force: true` to stop anyway, accepting the loss of everything written since the last successful snapshot. This is irreversible; only reach for it after a stop has already failed. */
export const stopSandbox: API.OperationMethod<
  StopSandboxRequest,
  StopSandboxResponse,
  StopSandboxError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: StopSandboxRequest,
  output: StopSandboxResponse,
  errors: [BadRequest, NotFound, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type UpdateDataRetentionError =
  | BadRequest
  | Forbidden
  | Conflict
  | BoatOpError;
/** Update account data-retention policy Requires an interactive sandbox session; API keys and legacy permanent tokens are refused. Enabling requires the exact confirmation phrase `delete archived sandbox data`. Disabling affects future archives only and cannot cancel deletion operations already accepted. */
export const updateDataRetention: API.OperationMethod<
  UpdateDataRetentionRequest,
  UpdateDataRetentionResponse,
  UpdateDataRetentionError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateDataRetentionRequest,
  output: UpdateDataRetentionResponse,
  errors: [BadRequest, Forbidden, Conflict, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type UpdateEnvironmentError = BadRequest | NotFound | BoatOpError;
/** Update a sandbox environment Rename, set as default, and/or edit flags and contents. Any flag or content change mints a new immutable version; existing sandboxes stay on their pinned version until you call upgrade. */
export const updateEnvironment: API.OperationMethod<
  UpdateEnvironmentRequest,
  SandboxEnvironmentResponse,
  UpdateEnvironmentError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateEnvironmentRequest,
  output: SandboxEnvironmentResponse,
  errors: [BadRequest, NotFound, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type UpdateSandboxError = BadRequest | NotFound | BoatOpError;
/** Update sandbox */
export const updateSandbox: API.OperationMethod<
  UpdateSandboxRequest,
  UpdateSandboxResponse,
  UpdateSandboxError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateSandboxRequest,
  output: UpdateSandboxResponse,
  errors: [BadRequest, NotFound, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type UpdateSecretsError = BoatOpError;
/** Update Boat secrets setup */
export const updateSecrets: API.OperationMethod<
  UpdateSecretsRequest,
  UpdateSecretsResponse,
  UpdateSecretsError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateSecretsRequest,
  output: UpdateSecretsResponse,
  errors: [UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type UpdateWebhookError = BadRequest | NotFound | Conflict | BoatOpError;
/** Update webhook Updates the name, endpoint URL, or subscribed events without changing the signing secret. */
export const updateWebhook: API.OperationMethod<
  UpdateWebhookRequest,
  UpdateWebhookResponse,
  UpdateWebhookError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateWebhookRequest,
  output: UpdateWebhookResponse,
  errors: [BadRequest, NotFound, Conflict, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type UpgradeEnvironmentError = NotFound | BoatOpError;
/** Upgrade sandboxes to an environment's latest version Repoints the caller's active sandboxes from an older version of this environment to its latest version, scrubbing any owner secrets the new version drops and hot-pushing the new config. */
export const upgradeEnvironment: API.OperationMethod<
  UpgradeEnvironmentRequest,
  UpgradeSandboxEnvironmentResponse,
  UpgradeEnvironmentError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpgradeEnvironmentRequest,
  output: UpgradeSandboxEnvironmentResponse,
  errors: [NotFound, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));

export type WriteSandboxFileError = BadRequest | BoatOpError;
/** Write a file in a sandbox */
export const writeSandboxFile: API.OperationMethod<
  WriteSandboxFileRequest,
  WriteSandboxFileResponse,
  WriteSandboxFileError,
  BoatOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: WriteSandboxFileRequest,
  output: WriteSandboxFileResponse,
  errors: [BadRequest, UnknownBoatError],
  protocol: BoatProtocol,
  retry: Retry.Retry,
}));
