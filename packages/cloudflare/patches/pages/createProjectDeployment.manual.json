{
  "description": "Hand-authored: Pages direct-upload deployment is multipart/form-data (branch/manifest fields + named file parts). The docs now describe every body field, so only the content type has to be supplied here.",
  "patches": [
    {
      "op": "add",
      "path": "/shapes/com.cloudflare.pages#CreateProjectDeployment/traits/smithy.api#http/contentType",
      "value": "multipart"
    }
  ]
}
