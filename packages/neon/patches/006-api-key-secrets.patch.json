{
  "description": "Mark the reveal-once API key token sensitive on the shared personal and organization create response; the upstream parent x-sensitive array is not a member annotation.",
  "patches": [
    {
      "op": "add",
      "path": "/components/schemas/ApiKeyCreateResponse/properties/key/x-sensitive",
      "value": true
    }
  ]
}
