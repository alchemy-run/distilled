import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/spec-mirror-stackit). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import { UnknownStackitError } from "../errors.ts";
import { StackitProtocol, type StackitOpError, type StackitOpContext } from "../protocol.ts";
import * as Retry from "../retry.ts";
import * as T from "../traits.ts";

export type { StackitOpError, StackitOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Conflict
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Conflict>()("Conflict", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withConflictError),
    [{ status: 409 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

/** Key-value string pairs attached to an existing resource container. Certain labels may be enforced via organizational policies. * **Key:** Must match the regex `[A-ZÄÜÖa-zäüöß0-9_-]{1,64}` * **Value:** Must match the regex `^$|[A-ZÄÜÖa-zäüöß0-9_-]{1,64}` > Note: Additional naming restrictions may apply depending on your specific organization.* */
export type Labels = { [key: string]: string | undefined };
export const Labels = /*@__PURE__*/ S.Record(S.String, S.String) as any as S.Schema<Labels>;

export interface Member {
  /** A valid role defined for the resource. */
  role: string;
  /** Unique identifier of the user, service account or client. */
  subject: string;
}
export const Member = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    role: S.String,
    subject: S.String,
  }),
).annotate({ identifier: "Member" }) as any as S.Schema<Member>;

/** The initial members assigned to the project. At least one subject needs to be a user, and not a client or service account. */
export type CreateFolderRequestMembersList = Array<Member>;
export const CreateFolderRequestMembersList = /*@__PURE__*/ S.Array(
  Member,
) as any as S.Schema<CreateFolderRequestMembersList>;

export interface CreateFolderRequest {
  /** Identifier of the parent resource container - containerId as well as UUID identifier is supported. */
  containerParentId: string;
  labels?: Labels;
  /** The initial members assigned to the project. At least one subject needs to be a user, and not a client or service account. */
  members?: CreateFolderRequestMembersList;
  /** The name of the folder matching the regex `^[a-zA-ZäüöÄÜÖ0-9]( ?[a-zA-ZäüöÄÜÖß0-9_+&-]){0,39}$`. */
  name: string;
}
export const CreateFolderRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    containerParentId: S.String,
    labels: S.optional(Labels),
    members: S.optional(CreateFolderRequestMembersList),
    name: S.String,
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v2/folders",
      code: 200,
      baseUrl: "https://resource-manager.api.stackit.cloud",
    }),
  ),
).annotate({
  identifier: "CreateFolderRequest",
}) as any as S.Schema<CreateFolderRequest>;

/** Container type of parent container. */
export type ParentType = "ORGANIZATION" | "FOLDER";
export const ParentType = /*@__PURE__*/ S.String;

/** Parent container. */
export interface Parent {
  /** User-friendly identifier of either organization or folder (will replace id). */
  containerId: string;
  /** Identifier of either organization or folder. */
  id: string;
  /** Container type of parent container. */
  type: ParentType;
}
export const Parent = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    containerId: S.String,
    id: S.String,
    type: ParentType,
  }),
).annotate({ identifier: "Parent" }) as any as S.Schema<Parent>;

export interface FolderResponse {
  /** Globally unique, user-friendly identifier. */
  containerId: string;
  /** Timestamp at which the folder was created. */
  creationTime: string;
  /** Globally unique folder identifier. */
  folderId: string;
  labels?: Labels;
  /** Folder name. */
  name: string;
  parent: Parent;
  /** Timestamp at which the folder was last modified. */
  updateTime: string;
}
export const FolderResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    containerId: S.String,
    creationTime: S.String,
    folderId: S.String,
    labels: S.optional(Labels),
    name: S.String,
    parent: Parent,
    updateTime: S.String,
  }),
).annotate({ identifier: "FolderResponse" }) as any as S.Schema<FolderResponse>;

/** Key-value string pairs attached to a resource container during creation. Certain labels may be enforced via organizational policies. * **Key:** Must match the regex `[A-ZÄÜÖa-zäüöß0-9_-]{1,64}` * **Value:** Must match the regex `^$|[A-ZÄÜÖa-zäüöß0-9_-]{1,64}` * Either a `"scope": "PUBLIC"` label or a valid `networkArea` label must be provided for project creation. * To create a project within a STACKIT Network Area, you must set the label `"networkArea": "<networkAreaID>"`. > **Important:** The `networkArea` label is immutable and **cannot be changed** after project creation. */
export type CreateProjectLabels = { [key: string]: string | undefined };
export const CreateProjectLabels = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<CreateProjectLabels>;

/** The initial members assigned to the project. At least one subject needs to be a user, and not a client or service account. */
export type CreateProjectRequestMembersList = Array<Member>;
export const CreateProjectRequestMembersList = /*@__PURE__*/ S.Array(
  Member,
) as any as S.Schema<CreateProjectRequestMembersList>;

export interface CreateProjectRequest {
  /** Identifier of the parent resource container - containerId as well as UUID identifier is supported. */
  containerParentId: string;
  labels?: CreateProjectLabels;
  /** The initial members assigned to the project. At least one subject needs to be a user, and not a client or service account. */
  members: CreateProjectRequestMembersList;
  /** Project name matching the regex `^[a-zA-ZäüöÄÜÖ0-9]( ?[a-zA-ZäüöÄÜÖß0-9_+&-]){0,39}$`. */
  name: string;
}
export const CreateProjectRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    containerParentId: S.String,
    labels: S.optional(CreateProjectLabels),
    members: CreateProjectRequestMembersList,
    name: S.String,
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v2/projects",
      code: 200,
      baseUrl: "https://resource-manager.api.stackit.cloud",
    }),
  ),
).annotate({
  identifier: "CreateProjectRequest",
}) as any as S.Schema<CreateProjectRequest>;

/** Lifecycle state of the resource container. | LIFECYCLE STATE | DESCRIPTION | |----------|--------------------| | CREATING | The creation process has been triggered. The state remains until resource manager gets notified about successful process completion. | | ACTIVE | Resource container can be fully used. | | INACTIVE | Resource container usage has been disabled. | | DELETING | The deletion process has been triggered. The state remains until resource manager gets notified about successful process completion. Afterwards, the record will be deleted. | */
export type LifecycleState = "CREATING" | "ACTIVE" | "DELETING" | "INACTIVE";
export const LifecycleState = /*@__PURE__*/ S.String;

export interface Project {
  /** Globally unique, user-friendly identifier. */
  containerId: string;
  /** Timestamp at which the project was created. */
  creationTime: string;
  labels?: Labels;
  lifecycleState: LifecycleState;
  /** Project name. */
  name: string;
  parent: Parent;
  /** Globally unique, project identifier. */
  projectId: string;
  /** Timestamp at which the project was last modified. */
  updateTime: string;
}
export const Project = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    containerId: S.String,
    creationTime: S.String,
    labels: S.optional(Labels),
    lifecycleState: LifecycleState,
    name: S.String,
    parent: Parent,
    projectId: S.String,
    updateTime: S.String,
  }),
).annotate({ identifier: "Project" }) as any as S.Schema<Project>;

export interface DeleteFolderRequest {
  /** Folder identifier - containerId as well as UUID identifier is supported. */
  containerId: string;
  /** If true, all nested, empty folders are deleted recursively - if no project is attached! */
  force?: boolean;
}
export const DeleteFolderRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    containerId: S.String.pipe(T.Label()),
    force: S.optional(S.Boolean.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/v2/folders/{containerId}",
      code: 200,
      baseUrl: "https://resource-manager.api.stackit.cloud",
    }),
  ),
).annotate({
  identifier: "DeleteFolderRequest",
}) as any as S.Schema<DeleteFolderRequest>;

export interface DeleteFolderResponse {}
export const DeleteFolderResponse = /*@__PURE__*/ S.suspend(() => S.Struct({})).annotate({
  identifier: "DeleteFolderResponse",
}) as any as S.Schema<DeleteFolderResponse>;

export type DeleteFolderLabelsRequestKeysList = Array<string>;
export const DeleteFolderLabelsRequestKeysList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<DeleteFolderLabelsRequestKeysList>;

export interface DeleteFolderLabelsRequest {
  /** Folder identifier - containerId as well as UUID identifier is supported. */
  containerId: string;
  /** Label name. */
  keys?: DeleteFolderLabelsRequestKeysList;
}
export const DeleteFolderLabelsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    containerId: S.String.pipe(T.Label()),
    keys: S.optional(DeleteFolderLabelsRequestKeysList.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/v2/folders/{containerId}/labels",
      code: 200,
      baseUrl: "https://resource-manager.api.stackit.cloud",
    }),
  ),
).annotate({
  identifier: "DeleteFolderLabelsRequest",
}) as any as S.Schema<DeleteFolderLabelsRequest>;

export interface DeleteFolderLabelsResponse {}
export const DeleteFolderLabelsResponse = /*@__PURE__*/ S.suspend(() => S.Struct({})).annotate({
  identifier: "DeleteFolderLabelsResponse",
}) as any as S.Schema<DeleteFolderLabelsResponse>;

export type DeleteOrganizationLabelsRequestKeysList = Array<string>;
export const DeleteOrganizationLabelsRequestKeysList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<DeleteOrganizationLabelsRequestKeysList>;

export interface DeleteOrganizationLabelsRequest {
  /** Organization identifier - containerId as well as UUID identifier is supported. */
  containerId: string;
  /** Label name. */
  keys?: DeleteOrganizationLabelsRequestKeysList;
}
export const DeleteOrganizationLabelsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    containerId: S.String.pipe(T.Label()),
    keys: S.optional(DeleteOrganizationLabelsRequestKeysList.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/v2/organizations/{containerId}/labels",
      code: 200,
      baseUrl: "https://resource-manager.api.stackit.cloud",
    }),
  ),
).annotate({
  identifier: "DeleteOrganizationLabelsRequest",
}) as any as S.Schema<DeleteOrganizationLabelsRequest>;

export interface DeleteOrganizationLabelsResponse {}
export const DeleteOrganizationLabelsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "DeleteOrganizationLabelsResponse",
}) as any as S.Schema<DeleteOrganizationLabelsResponse>;

export interface DeleteProjectRequest {
  /** Project identifier - containerId as well as UUID identifier is supported. */
  id: string;
}
export const DeleteProjectRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/v2/projects/{id}",
      code: 200,
      baseUrl: "https://resource-manager.api.stackit.cloud",
    }),
  ),
).annotate({
  identifier: "DeleteProjectRequest",
}) as any as S.Schema<DeleteProjectRequest>;

export interface DeleteProjectResponse {}
export const DeleteProjectResponse = /*@__PURE__*/ S.suspend(() => S.Struct({})).annotate({
  identifier: "DeleteProjectResponse",
}) as any as S.Schema<DeleteProjectResponse>;

export type DeleteProjectLabelsRequestKeysList = Array<string>;
export const DeleteProjectLabelsRequestKeysList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<DeleteProjectLabelsRequestKeysList>;

export interface DeleteProjectLabelsRequest {
  /** Project identifier - containerId as well as UUID identifier is supported. */
  containerId: string;
  /** Label name. */
  keys?: DeleteProjectLabelsRequestKeysList;
}
export const DeleteProjectLabelsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    containerId: S.String.pipe(T.Label()),
    keys: S.optional(DeleteProjectLabelsRequestKeysList.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/v2/projects/{containerId}/labels",
      code: 200,
      baseUrl: "https://resource-manager.api.stackit.cloud",
    }),
  ),
).annotate({
  identifier: "DeleteProjectLabelsRequest",
}) as any as S.Schema<DeleteProjectLabelsRequest>;

export interface DeleteProjectLabelsResponse {}
export const DeleteProjectLabelsResponse = /*@__PURE__*/ S.suspend(() => S.Struct({})).annotate({
  identifier: "DeleteProjectLabelsResponse",
}) as any as S.Schema<DeleteProjectLabelsResponse>;

export interface GetFolderDetailsRequest {
  /** Folder identifier - containerId as well as UUID identifier is supported. */
  containerId: string;
  includeParents?: boolean;
}
export const GetFolderDetailsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    containerId: S.String.pipe(T.Label()),
    includeParents: S.optional(S.Boolean.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/v2/folders/{containerId}",
      code: 200,
      baseUrl: "https://resource-manager.api.stackit.cloud",
    }),
  ),
).annotate({
  identifier: "GetFolderDetailsRequest",
}) as any as S.Schema<GetFolderDetailsRequest>;

/** Parent container type. */
export type ParentListItemType = "FOLDER" | "ORGANIZATION";
export const ParentListItemType = /*@__PURE__*/ S.String;

export interface ParentListItem {
  /** User-friendly identifier of either organization or folder (will replace id). */
  containerId: string;
  /** User-friendly parent identifier of either organization or folder (will replace parentId). */
  containerParentId?: string;
  /** Identifier. */
  id: string;
  /** Parent container name. */
  name: string;
  /** Identifier of the parent resource container. */
  parentId?: string;
  /** Parent container type. */
  type: ParentListItemType;
}
export const ParentListItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    containerId: S.String,
    containerParentId: S.optional(S.String),
    id: S.String,
    name: S.String,
    parentId: S.optional(S.String),
    type: ParentListItemType,
  }),
).annotate({ identifier: "ParentListItem" }) as any as S.Schema<ParentListItem>;

export type ParentList = Array<ParentListItem>;
export const ParentList = /*@__PURE__*/ S.Array(ParentListItem) as any as S.Schema<ParentList>;

export interface GetFolderDetailsResponse {
  /** Globally unique user-friendly identifier. */
  containerId: string;
  /** Timestamp at which the folder was created. */
  creationTime: string;
  /** Globally unique folder identifier. */
  folderId: string;
  labels?: Labels;
  /** Folder name. */
  name: string;
  parent: Parent;
  parents?: ParentList;
  /** Timestamp at which the folder was last modified. */
  updateTime: string;
}
export const GetFolderDetailsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    containerId: S.String,
    creationTime: S.String,
    folderId: S.String,
    labels: S.optional(Labels),
    name: S.String,
    parent: Parent,
    parents: S.optional(ParentList),
    updateTime: S.String,
  }),
).annotate({
  identifier: "GetFolderDetailsResponse",
}) as any as S.Schema<GetFolderDetailsResponse>;

export interface GetOrganizationRequest {
  /** Organization identifier - containerId as well as UUID identifier is supported. */
  id: string;
}
export const GetOrganizationRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/v2/organizations/{id}",
      code: 200,
      baseUrl: "https://resource-manager.api.stackit.cloud",
    }),
  ),
).annotate({
  identifier: "GetOrganizationRequest",
}) as any as S.Schema<GetOrganizationRequest>;

export interface OrganizationResponse {
  /** Globally unique, user-friendly identifier. */
  containerId: string;
  /** Timestamp at which the organization was created. */
  creationTime: string;
  labels?: Labels;
  lifecycleState: LifecycleState;
  /** Organization name. */
  name: string;
  /** Globally unique, organization identifier. */
  organizationId: string;
  /** Timestamp at which the organization was last modified. */
  updateTime: string;
}
export const OrganizationResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    containerId: S.String,
    creationTime: S.String,
    labels: S.optional(Labels),
    lifecycleState: LifecycleState,
    name: S.String,
    organizationId: S.String,
    updateTime: S.String,
  }),
).annotate({
  identifier: "OrganizationResponse",
}) as any as S.Schema<OrganizationResponse>;

export interface GetProjectRequest {
  /** Project identifier - containerId as well as UUID identifier is supported. */
  id: string;
  includeParents?: boolean;
}
export const GetProjectRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    includeParents: S.optional(S.Boolean.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/v2/projects/{id}",
      code: 200,
      baseUrl: "https://resource-manager.api.stackit.cloud",
    }),
  ),
).annotate({
  identifier: "GetProjectRequest",
}) as any as S.Schema<GetProjectRequest>;

export interface GetProjectResponse {
  /** Globally unique identifier. */
  containerId: string;
  /** Timestamp at which the project was created. */
  creationTime: string;
  labels?: Labels;
  lifecycleState: LifecycleState;
  /** Project name. */
  name: string;
  parent: Parent;
  parents?: ParentList;
  /** Globally unique identifier. */
  projectId: string;
  /** Timestamp at which the project was last modified. */
  updateTime: string;
}
export const GetProjectResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    containerId: S.String,
    creationTime: S.String,
    labels: S.optional(Labels),
    lifecycleState: LifecycleState,
    name: S.String,
    parent: Parent,
    parents: S.optional(ParentList),
    projectId: S.String,
    updateTime: S.String,
  }),
).annotate({
  identifier: "GetProjectResponse",
}) as any as S.Schema<GetProjectResponse>;

export type ListFoldersRequestContainerIdsList = Array<string>;
export const ListFoldersRequestContainerIdsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ListFoldersRequestContainerIdsList>;

export interface ListFoldersRequest {
  /** Identifier of the parent resource container - containerId as well as UUID identifier is supported. */
  containerParentId?: string;
  /** List of container identifiers - containerId as well as UUID identifier is supported. */
  containerIds?: ListFoldersRequestContainerIdsList;
  /** E-Mail address of the user for whom the visible resource containers should be filtered. */
  member?: string;
  /** The maximum number of projects to return in the response. If not present, an appropriate default will be used. If maximum is exceeded, maximum is used. */
  limit?: number;
  /** The offset of the first item in the collection to return. */
  offset?: number;
  /** A timestamp to specify the beginning of the creationTime from which entries should be returned. If not given, defaults to the beginning of time. */
  creation_time_start?: string;
}
export const ListFoldersRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    containerParentId: S.optional(S.String.pipe(T.Query())),
    containerIds: S.optional(ListFoldersRequestContainerIdsList.pipe(T.Query())),
    member: S.optional(S.String.pipe(T.Query())),
    limit: S.optional(S.Number.pipe(T.Query())),
    offset: S.optional(S.Number.pipe(T.Query())),
    creation_time_start: S.optional(S.String.pipe(T.Query("creation-time-start"))),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/v2/folders",
      code: 200,
      baseUrl: "https://resource-manager.api.stackit.cloud",
    }),
  ),
).annotate({
  identifier: "ListFoldersRequest",
}) as any as S.Schema<ListFoldersRequest>;

export interface ListFoldersResponseItemsItem {
  /** Globally unique folder identifier. */
  containerId: string;
  /** Timestamp at which the folder was created. */
  creationTime: string;
  /** Globally unique folder identifier. */
  folderId: string;
  labels?: Labels;
  /** Name of the folder. */
  name: string;
  parent: Parent;
  /** Timestamp at which the folder was created. */
  updateTime: string;
}
export const ListFoldersResponseItemsItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    containerId: S.String,
    creationTime: S.String,
    folderId: S.String,
    labels: S.optional(Labels),
    name: S.String,
    parent: Parent,
    updateTime: S.String,
  }),
).annotate({
  identifier: "ListFoldersResponseItemsItem",
}) as any as S.Schema<ListFoldersResponseItemsItem>;

export type ListFoldersResponseItemsList = Array<ListFoldersResponseItemsItem>;
export const ListFoldersResponseItemsList = /*@__PURE__*/ S.Array(
  ListFoldersResponseItemsItem,
) as any as S.Schema<ListFoldersResponseItemsList>;

export interface ListFoldersResponse {
  items: ListFoldersResponseItemsList;
  limit: number;
  offset: number;
}
export const ListFoldersResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    items: ListFoldersResponseItemsList,
    limit: S.Number,
    offset: S.Number,
  }),
).annotate({
  identifier: "ListFoldersResponse",
}) as any as S.Schema<ListFoldersResponse>;

export type ListOrganizationsRequestContainerIdsList = Array<string>;
export const ListOrganizationsRequestContainerIdsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ListOrganizationsRequestContainerIdsList>;

export interface ListOrganizationsRequest {
  /** Organization identifiers - containerId as well as UUID identifier is supported. A combination of both is not allowed. */
  containerIds?: ListOrganizationsRequestContainerIdsList;
  /** E-Mail address of the user for whom the visible resource containers should be filtered. */
  member?: string;
  /** The maximum number of projects to return in the response. If not present, an appropriate default will be used. If maximum is exceeded, maximum is used. */
  limit?: number;
  /** The offset of the first item in the collection to return. */
  offset?: number;
  /** A timestamp to specify the beginning of the creationTime from which entries should be returned. If not given, defaults to the beginning of time. */
  creation_time_start?: string;
}
export const ListOrganizationsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    containerIds: S.optional(ListOrganizationsRequestContainerIdsList.pipe(T.Query())),
    member: S.optional(S.String.pipe(T.Query())),
    limit: S.optional(S.Number.pipe(T.Query())),
    offset: S.optional(S.Number.pipe(T.Query())),
    creation_time_start: S.optional(S.String.pipe(T.Query("creation-time-start"))),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/v2/organizations",
      code: 200,
      baseUrl: "https://resource-manager.api.stackit.cloud",
    }),
  ),
).annotate({
  identifier: "ListOrganizationsRequest",
}) as any as S.Schema<ListOrganizationsRequest>;

export interface ListOrganizationsResponseItemsItem {
  /** Globally unique, user-friendly identifier. */
  containerId: string;
  /** Timestamp at which the organization was created. */
  creationTime: string;
  labels?: Labels;
  lifecycleState: LifecycleState;
  /** Name of the organization. */
  name: string;
  /** Globally unique, organization identifier. */
  organizationId: string;
  /** Timestamp at which the organization was last modified. */
  updateTime: string;
}
export const ListOrganizationsResponseItemsItem = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    containerId: S.String,
    creationTime: S.String,
    labels: S.optional(Labels),
    lifecycleState: LifecycleState,
    name: S.String,
    organizationId: S.String,
    updateTime: S.String,
  }),
).annotate({
  identifier: "ListOrganizationsResponseItemsItem",
}) as any as S.Schema<ListOrganizationsResponseItemsItem>;

export type ListOrganizationsResponseItemsList = Array<ListOrganizationsResponseItemsItem>;
export const ListOrganizationsResponseItemsList = /*@__PURE__*/ S.Array(
  ListOrganizationsResponseItemsItem,
) as any as S.Schema<ListOrganizationsResponseItemsList>;

export interface ListOrganizationsResponse {
  items: ListOrganizationsResponseItemsList;
  limit: number;
  offset: number;
}
export const ListOrganizationsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    items: ListOrganizationsResponseItemsList,
    limit: S.Number,
    offset: S.Number,
  }),
).annotate({
  identifier: "ListOrganizationsResponse",
}) as any as S.Schema<ListOrganizationsResponse>;

export type ListProjectsRequestContainerIdsList = Array<string>;
export const ListProjectsRequestContainerIdsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<ListProjectsRequestContainerIdsList>;

export interface ListProjectsRequest {
  /** Identifier of the parent resource container - containerId as well as UUID identifier is supported. */
  containerParentId?: string;
  /** List of container identifiers - containerId as well as UUID identifier is supported. */
  containerIds?: ListProjectsRequestContainerIdsList;
  /** E-Mail address of the user for whom the visible resource containers should be filtered. */
  member?: string;
  /** The offset of the first item in the collection to return. */
  offset?: number;
  /** The maximum number of projects to return in the response. If not present, an appropriate default will be used. If maximum is exceeded, maximum is used. */
  limit?: number;
  /** A timestamp to specify the beginning of the creationTime from which entries should be returned. If not given, defaults to the beginning of time. */
  creation_time_start?: string;
}
export const ListProjectsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    containerParentId: S.optional(S.String.pipe(T.Query())),
    containerIds: S.optional(ListProjectsRequestContainerIdsList.pipe(T.Query())),
    member: S.optional(S.String.pipe(T.Query())),
    offset: S.optional(S.Number.pipe(T.Query())),
    limit: S.optional(S.Number.pipe(T.Query())),
    creation_time_start: S.optional(S.String.pipe(T.Query("creation-time-start"))),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/v2/projects",
      code: 200,
      baseUrl: "https://resource-manager.api.stackit.cloud",
    }),
  ),
).annotate({
  identifier: "ListProjectsRequest",
}) as any as S.Schema<ListProjectsRequest>;

export type ListProjectsResponseItemsList = Array<Project>;
export const ListProjectsResponseItemsList = /*@__PURE__*/ S.Array(
  Project,
) as any as S.Schema<ListProjectsResponseItemsList>;

export interface ListProjectsResponse {
  items: ListProjectsResponseItemsList;
  limit: number;
  offset: number;
}
export const ListProjectsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    items: ListProjectsResponseItemsList,
    limit: S.Number,
    offset: S.Number,
  }),
).annotate({
  identifier: "ListProjectsResponse",
}) as any as S.Schema<ListProjectsResponse>;

/** Key-value string pairs attached to an existing resource container. Certain labels may be enforced via organizational policies. Setting an individual label's value to `null` removes/deletes that label from the resource. * **Key:** Must match the regex `[A-ZÄÜÖa-zäüöß0-9_-]{1,64}` * **Value:** Must match the regex `^$|[A-ZÄÜÖa-zäüöß0-9_-]{1,64}`, or be `null` to delete the label > Note: Additional naming restrictions may apply depending on your specific organization.* */
export type UpdateLabels = { [key: string]: string | undefined };
export const UpdateLabels = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<UpdateLabels>;

export interface PartialUpdateFolderRequest {
  /** Folder identifier - containerId as well as UUID identifier is supported. */
  containerId: string;
  /** New parent identifier for the resource container - containerId as well as UUID identifier is supported. */
  containerParentId?: string;
  labels?: UpdateLabels;
  /** New name for the resource container matching the regex `^[a-zA-ZäüöÄÜÖ0-9]( ?[a-zA-ZäüöÄÜÖß0-9_+&-]){0,39}$`. */
  name?: string;
}
export const PartialUpdateFolderRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    containerId: S.String.pipe(T.Label()),
    containerParentId: S.optional(S.String),
    labels: S.optional(UpdateLabels),
    name: S.optional(S.String),
  }).pipe(
    T.Http({
      method: "PATCH",
      uri: "/v2/folders/{containerId}",
      code: 200,
      baseUrl: "https://resource-manager.api.stackit.cloud",
    }),
  ),
).annotate({
  identifier: "PartialUpdateFolderRequest",
}) as any as S.Schema<PartialUpdateFolderRequest>;

export interface PartialUpdateOrganizationRequest {
  /** Organization identifier - containerId as well as UUID identifier is supported. */
  id: string;
  labels?: UpdateLabels;
  /** The new name of the organization matching the regex `^[a-zA-ZäüöÄÜÖ0-9]( ?[a-zA-ZäüöÄÜÖß0-9_+&-]){0,39}$`. */
  name?: string;
}
export const PartialUpdateOrganizationRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    labels: S.optional(UpdateLabels),
    name: S.optional(S.String),
  }).pipe(
    T.Http({
      method: "PATCH",
      uri: "/v2/organizations/{id}",
      code: 200,
      baseUrl: "https://resource-manager.api.stackit.cloud",
    }),
  ),
).annotate({
  identifier: "PartialUpdateOrganizationRequest",
}) as any as S.Schema<PartialUpdateOrganizationRequest>;

export interface PartialUpdateProjectRequest {
  /** Project identifier - containerId as well as UUID identifier is supported. */
  id: string;
  /** New parent identifier for the resource container - containerId as well as UUID identifier is supported. */
  containerParentId?: string;
  labels?: UpdateLabels;
  /** New name for the resource container matching the regex `^[a-zA-ZäüöÄÜÖ0-9]( ?[a-zA-ZäüöÄÜÖß0-9_+&-]){0,39}$`. */
  name?: string;
}
export const PartialUpdateProjectRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String.pipe(T.Label()),
    containerParentId: S.optional(S.String),
    labels: S.optional(UpdateLabels),
    name: S.optional(S.String),
  }).pipe(
    T.Http({
      method: "PATCH",
      uri: "/v2/projects/{id}",
      code: 200,
      baseUrl: "https://resource-manager.api.stackit.cloud",
    }),
  ),
).annotate({
  identifier: "PartialUpdateProjectRequest",
}) as any as S.Schema<PartialUpdateProjectRequest>;

export type CreateFolderError = BadRequest | Forbidden | Conflict | StackitOpError;
/** Create Folder Creates a new folder within a parent organization or folder. Authorization & Permissions: - Evaluated on Target Parent Container: resource-manager.folder.create (required permission on the target parent organization or folder to create a folder). */
export const createFolder: API.OperationMethod<
  CreateFolderRequest,
  FolderResponse,
  CreateFolderError,
  StackitOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateFolderRequest,
  output: FolderResponse,
  errors: [BadRequest, Forbidden, Conflict, UnknownStackitError],
  protocol: StackitProtocol,
  retry: Retry.Retry,
}));

export type CreateProjectError = BadRequest | Forbidden | Conflict | StackitOpError;
/** Create Project Creates a new project within a parent container. - Creation request returns synchronously, while underlying resource provisioning runs asynchronously. - Initial lifecycle state is CREATING until provisioning completes. Authorization & Permissions: - Evaluated on Target Parent Container: resource-manager.project.create (required permission on the target parent organization or folder to create a project). */
export const createProject: API.OperationMethod<
  CreateProjectRequest,
  Project,
  CreateProjectError,
  StackitOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateProjectRequest,
  output: Project,
  errors: [BadRequest, Forbidden, Conflict, UnknownStackitError],
  protocol: StackitProtocol,
  retry: Retry.Retry,
}));

export type DeleteFolderError = NotFound | Conflict | StackitOpError;
/** Delete Folder Delete a folder and its metadata. - Folder must not contain active child containers unless force deletion is enabled without attached projects. Authorization & Permissions: - Evaluated on Target Folder: resource-manager.folder.delete (required permission on the target folder to delete it). */
export const deleteFolder: API.OperationMethod<
  DeleteFolderRequest,
  DeleteFolderResponse,
  DeleteFolderError,
  StackitOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteFolderRequest,
  output: DeleteFolderResponse,
  errors: [NotFound, Conflict, UnknownStackitError],
  protocol: StackitProtocol,
  retry: Retry.Retry,
}));

export type DeleteFolderLabelsError = Conflict | StackitOpError;
/** Delete Folder Labels Deletes folder labels by specified keys, or removes all labels if no key is provided. Authorization & Permissions: - Evaluated on Target Folder: resource-manager.folder.edit (required permission on the target folder to delete its labels). */
export const deleteFolderLabels: API.OperationMethod<
  DeleteFolderLabelsRequest,
  DeleteFolderLabelsResponse,
  DeleteFolderLabelsError,
  StackitOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteFolderLabelsRequest,
  output: DeleteFolderLabelsResponse,
  errors: [Conflict, UnknownStackitError],
  protocol: StackitProtocol,
  retry: Retry.Retry,
}));

export type DeleteOrganizationLabelsError = Conflict | StackitOpError;
/** Delete Organization Labels Deletes organization labels by specified keys, or removes all labels if no key is provided. Authorization & Permissions: - Evaluated on Target Organization: resource-manager.organization.edit (required permission on the organization to delete labels). */
export const deleteOrganizationLabels: API.OperationMethod<
  DeleteOrganizationLabelsRequest,
  DeleteOrganizationLabelsResponse,
  DeleteOrganizationLabelsError,
  StackitOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteOrganizationLabelsRequest,
  output: DeleteOrganizationLabelsResponse,
  errors: [Conflict, UnknownStackitError],
  protocol: StackitProtocol,
  retry: Retry.Retry,
}));

export type DeleteProjectError = Conflict | StackitOpError;
/** Delete Project Triggers the deletion of a project. - Request returns synchronously, while deletion process runs asynchronously. - Initial lifecycle state is DELETING until workflow completes. Authorization & Permissions: - Evaluated on Target Project: resource-manager.project.delete (required permission on the project to delete it). */
export const deleteProject: API.OperationMethod<
  DeleteProjectRequest,
  DeleteProjectResponse,
  DeleteProjectError,
  StackitOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteProjectRequest,
  output: DeleteProjectResponse,
  errors: [Conflict, UnknownStackitError],
  protocol: StackitProtocol,
  retry: Retry.Retry,
}));

export type DeleteProjectLabelsError = Conflict | StackitOpError;
/** Delete Project Labels Deletes project labels by specified keys, or removes all labels if no key is provided. Authorization & Permissions: - Evaluated on Target Project: resource-manager.project.edit (required permission on the project to delete its labels). */
export const deleteProjectLabels: API.OperationMethod<
  DeleteProjectLabelsRequest,
  DeleteProjectLabelsResponse,
  DeleteProjectLabelsError,
  StackitOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteProjectLabelsRequest,
  output: DeleteProjectLabelsResponse,
  errors: [Conflict, UnknownStackitError],
  protocol: StackitProtocol,
  retry: Retry.Retry,
}));

export type GetFolderDetailsError = Forbidden | NotFound | StackitOpError;
/** Get Folder Details Returns metadata for a specific folder. Authorization & Permissions: - Evaluated on Target Folder: resource-manager.folder.get (required primary permission to retrieve folder details). - Evaluated on Parent Containers: resource-manager.organization.get and resource-manager.folder.get implicitly to resolve parent hierarchy details. */
export const getFolderDetails: API.OperationMethod<
  GetFolderDetailsRequest,
  GetFolderDetailsResponse,
  GetFolderDetailsError,
  StackitOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetFolderDetailsRequest,
  output: GetFolderDetailsResponse,
  errors: [Forbidden, NotFound, UnknownStackitError],
  protocol: StackitProtocol,
  retry: Retry.Retry,
}));

export type GetOrganizationError = NotFound | StackitOpError;
/** Get Organization Details Returns metadata for a specific organization. Authorization & Permissions: - Evaluated on Target Organization: resource-manager.organization.get (required permission on the organization to view its details). */
export const getOrganization: API.OperationMethod<
  GetOrganizationRequest,
  OrganizationResponse,
  GetOrganizationError,
  StackitOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetOrganizationRequest,
  output: OrganizationResponse,
  errors: [NotFound, UnknownStackitError],
  protocol: StackitProtocol,
  retry: Retry.Retry,
}));

export type GetProjectError = Forbidden | NotFound | StackitOpError;
/** Get Project Details Returns the project and its metadata. Authorization & Permissions: - Evaluated on Target Project: resource-manager.project.get (required primary permission to view project details). - Evaluated on Parent Containers: resource-manager.organization.get and resource-manager.folder.get on parent organizations/folders implicitly to resolve and include parent metadata in the response hierarchy. */
export const getProject: API.OperationMethod<
  GetProjectRequest,
  GetProjectResponse,
  GetProjectError,
  StackitOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetProjectRequest,
  output: GetProjectResponse,
  errors: [Forbidden, NotFound, UnknownStackitError],
  protocol: StackitProtocol,
  retry: Retry.Retry,
}));

export type ListFoldersError = BadRequest | Forbidden | Conflict | StackitOpError;
/** Get All Folders Returns all folders and their metadata that: - Are children of the specific containerParentId - Match the given containerIds - User is member of <br /> Filter: - Either containerParentId OR containerIds OR member must be passed - If containerId and containerParentId are given, both are used for filtering - containers must point to the same parent - If member and containerParentId are given, both are used for filtering - If member is given, containers must not point to the same container parent Authorization & Permissions: - When filtering by member: resource-manager.folder.direct.get is evaluated on target folders for the member; if caller differs from target member, system-level resource-manager.folder.get is required. - When filtering by containerParentId: resource-manager.folder.list is evaluated on the parent container. - When filtering by containerIds: resource-manager.folder.get is evaluated on the parent container of requested folders. */
export const listFolders: API.OperationMethod<
  ListFoldersRequest,
  ListFoldersResponse,
  ListFoldersError,
  StackitOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListFoldersRequest,
  output: ListFoldersResponse,
  errors: [BadRequest, Forbidden, Conflict, UnknownStackitError],
  protocol: StackitProtocol,
  retry: Retry.Retry,
}));

export type ListOrganizationsError = BadRequest | Forbidden | StackitOpError;
/** Get all organizations of member Returns all organizations and their metadata accessible to the caller. - Filterable by containerIds or member. Authorization & Permissions: - Evaluated on Target Organizations: resource-manager.organization.list and/or (depending if containerIDs was used in the request) resource-manager.organization.get to filter and list accessible organizations. */
export const listOrganizations: API.OperationMethod<
  ListOrganizationsRequest,
  ListOrganizationsResponse,
  ListOrganizationsError,
  StackitOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListOrganizationsRequest,
  output: ListOrganizationsResponse,
  errors: [BadRequest, Forbidden, UnknownStackitError],
  protocol: StackitProtocol,
  retry: Retry.Retry,
}));

export type ListProjectsError = BadRequest | Forbidden | StackitOpError;
/** Get All Projects of a member. Returns all projects and their metadata that: - Are children of the specific containerParentId - Match the given containerIds - User is member of Filter: - Either containerParentId OR containerIds OR member must be passed - If containerId and containerParentId are given, both are used for filtering - containers must point to the same parent - If member and containerParentId are given, both are used for filtering - If member is given, containers must not point to the same container parent Authorization & Permissions: - Evaluated on Target Projects: resource-manager.project.get, resource-manager.project.list, or resource-manager.project.direct.get to filter and return accessible projects. */
export const listProjects: API.OperationMethod<
  ListProjectsRequest,
  ListProjectsResponse,
  ListProjectsError,
  StackitOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListProjectsRequest,
  output: ListProjectsResponse,
  errors: [BadRequest, Forbidden, UnknownStackitError],
  protocol: StackitProtocol,
  retry: Retry.Retry,
}));

export type PartialUpdateFolderError = Forbidden | NotFound | Conflict | StackitOpError;
/** Update Folder Update the folder and its metadata (name, labels, parent container). Authorization & Permissions: - Evaluated on Target Folder: resource-manager.folder.edit (required permission on the target folder to modify its details, labels, or parent). */
export const partialUpdateFolder: API.OperationMethod<
  PartialUpdateFolderRequest,
  FolderResponse,
  PartialUpdateFolderError,
  StackitOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PartialUpdateFolderRequest,
  output: FolderResponse,
  errors: [Forbidden, NotFound, Conflict, UnknownStackitError],
  protocol: StackitProtocol,
  retry: Retry.Retry,
}));

export type PartialUpdateOrganizationError = NotFound | Conflict | StackitOpError;
/** Update Organization Updates organization metadata (name, labels). Authorization & Permissions: - Evaluated on Target Organization: resource-manager.organization.edit (required permission on the organization to update metadata and labels). */
export const partialUpdateOrganization: API.OperationMethod<
  PartialUpdateOrganizationRequest,
  OrganizationResponse,
  PartialUpdateOrganizationError,
  StackitOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PartialUpdateOrganizationRequest,
  output: OrganizationResponse,
  errors: [NotFound, Conflict, UnknownStackitError],
  protocol: StackitProtocol,
  retry: Retry.Retry,
}));

export type PartialUpdateProjectError = Forbidden | NotFound | Conflict | StackitOpError;
/** Update Project Updates project metadata (name, labels, or parent hierarchy). Authorization & Permissions: - Evaluated on Target Project: resource-manager.project.edit (required permission on the project to modify its details, labels, or parent). */
export const partialUpdateProject: API.OperationMethod<
  PartialUpdateProjectRequest,
  Project,
  PartialUpdateProjectError,
  StackitOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PartialUpdateProjectRequest,
  output: Project,
  errors: [Forbidden, NotFound, Conflict, UnknownStackitError],
  protocol: StackitProtocol,
  retry: Retry.Retry,
}));
