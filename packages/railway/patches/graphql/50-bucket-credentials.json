{
  "description": "Live Bucket lifecycle on 2026-09-12 failed immediately after creation at Query.bucketS3Credentials with exact message BucketInstanceCredential not found. The aggregate log did not retain the wire code, so match only the observed message. Credentials may become visible after the bucket; callers may bounded-retry this read-only query.",
  "patches": [
    {
      "op": "add",
      "path": "/errors/RailwayBucketCredentialsNotReady",
      "value": {
        "description": "The bucket exists but its S3-compatible credentials are not available yet. Retry the credentials query with a bounded policy.",
        "category": "notFound",
        "retryable": true,
        "matchers": [{ "message": "BucketInstanceCredential not found" }]
      }
    },
    {
      "op": "add",
      "path": "/types/Query/fields/bucketS3Credentials/errors/-",
      "value": "RailwayBucketCredentialsNotReady"
    }
  ]
}
