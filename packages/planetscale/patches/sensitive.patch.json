{
  "description": "ServiceToken.token is a plaintext secret but the name does not match convert's sensitive-field patterns (plain_text and plain_text_refresh_token already do).",
  "patches": [
    {
      "op": "add",
      "path": "/definitions/ServiceToken/properties/token/x-sensitive",
      "value": true
    }
  ]
}
