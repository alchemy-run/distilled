// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/spec-mirror-freestyle/specs/openapi.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  FreestyleProtocol,
  type FreestyleOpError,
  type FreestyleOpContext,
} from "../protocol.ts";
import { UnknownFreestyleError } from "../errors.ts";
import * as Retry from "../retry.ts";

export type { FreestyleOpError, FreestyleOpContext };

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export interface GetBackgroundRequestRequest {
  /** The id from the 202's `requestId` field / `x-freestyle-background-request-id` header */
  requestId: string;
}
export const GetBackgroundRequestRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    requestId: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/v5/background-requests/{requestId}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "GetBackgroundRequestRequest",
}) as any as S.Schema<GetBackgroundRequestRequest>;

export interface GetBackgroundRequestResponse {}
export const GetBackgroundRequestResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "GetBackgroundRequestResponse",
}) as any as S.Schema<GetBackgroundRequestResponse>;

export type GetBackgroundRequestError = NotFound | FreestyleOpError;
/** Poll a background request. A request started with the `x-freestyle-background-after-secs` header answers `202 Accepted` with a `requestId` once that many seconds elapse; poll this route with it. While the request is still running this answers `202` again; once it finishes, the request's own response — its status, headers, and body, success or error — is returned exactly as the original call would have. Results expire ten minutes after completion; a result that was delivered inline (the original call returned before the window elapsed) is not kept here. */
export const getBackgroundRequest: API.OperationMethod<
  GetBackgroundRequestRequest,
  GetBackgroundRequestResponse,
  GetBackgroundRequestError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetBackgroundRequestRequest,
  output: GetBackgroundRequestResponse,
  errors: [NotFound, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));
