// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/spec-mirror-freestyle/specs/openapi.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  FreestyleProtocol,
  type FreestyleOpError,
  type FreestyleOpContext,
} from "../protocol.ts";
import { UnknownFreestyleError } from "../errors.ts";
import * as Retry from "../retry.ts";

export type { FreestyleOpError, FreestyleOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Conflict
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Conflict>()("Conflict", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withConflictError),
    [{ status: 409 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export interface MakeDirRequest {
  /** VM id, or your slug for it */
  vmIdOrSlug: string;
  /** Absolute path inside the guest. */
  path: string;
}
export const MakeDirRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    vmIdOrSlug: S.String.pipe(T.Label()),
    path: S.String,
  }).pipe(
    T.Http({ method: "POST", uri: "/v5/vms/{vmIdOrSlug}/fs/mkdir", code: 200 }),
  ),
).annotate({ identifier: "MakeDirRequest" }) as any as S.Schema<MakeDirRequest>;

export interface MakeDirResponse {}
export const MakeDirResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "MakeDirResponse",
}) as any as S.Schema<MakeDirResponse>;

export interface PathExistsRequest {
  /** VM id, or your slug for it */
  vmIdOrSlug: string;
  /** Absolute path inside the guest. */
  path: string;
}
export const PathExistsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    vmIdOrSlug: S.String.pipe(T.Label()),
    path: S.String.pipe(T.Query()),
  }).pipe(
    T.Http({ method: "GET", uri: "/v5/vms/{vmIdOrSlug}/fs/exists", code: 200 }),
  ),
).annotate({
  identifier: "PathExistsRequest",
}) as any as S.Schema<PathExistsRequest>;

export interface PathExistsResponse {
  exists: boolean;
}
export const PathExistsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    exists: S.Boolean,
  }),
).annotate({
  identifier: "PathExistsResponse",
}) as any as S.Schema<PathExistsResponse>;

export interface ReadDirRequest {
  /** VM id, or your slug for it */
  vmIdOrSlug: string;
  /** Absolute path inside the guest. */
  path: string;
}
export const ReadDirRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    vmIdOrSlug: S.String.pipe(T.Label()),
    path: S.String.pipe(T.Query()),
  }).pipe(
    T.Http({ method: "GET", uri: "/v5/vms/{vmIdOrSlug}/fs/dir", code: 200 }),
  ),
).annotate({ identifier: "ReadDirRequest" }) as any as S.Schema<ReadDirRequest>;

export interface DirEntry {
  /** `file`, `directory`, or `symlink`. */
  kind: string;
  name: string;
}
export const DirEntry = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    kind: S.String,
    name: S.String,
  }),
).annotate({ identifier: "DirEntry" }) as any as S.Schema<DirEntry>;

export type ReadDirResponseEntriesList = Array<DirEntry>;
export const ReadDirResponseEntriesList = /*@__PURE__*/ S.Array(
  DirEntry,
) as any as S.Schema<ReadDirResponseEntriesList>;

export interface ReadDirResponse {
  entries: ReadDirResponseEntriesList;
}
export const ReadDirResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    entries: ReadDirResponseEntriesList,
  }),
).annotate({
  identifier: "ReadDirResponse",
}) as any as S.Schema<ReadDirResponse>;

export interface ReadFileRequest {
  /** VM id, or your slug for it */
  vmIdOrSlug: string;
  /** Absolute path inside the guest. */
  path: string;
  /** Optional single byte range, e.g. `bytes=0-1048575` */
  range?: string;
}
export const ReadFileRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    vmIdOrSlug: S.String.pipe(T.Label()),
    path: S.String.pipe(T.Query()),
    range: S.optional(S.String.pipe(T.Header("Range"))),
  }).pipe(
    T.Http({ method: "GET", uri: "/v5/vms/{vmIdOrSlug}/fs/read", code: 200 }),
  ),
).annotate({
  identifier: "ReadFileRequest",
}) as any as S.Schema<ReadFileRequest>;

export interface ReadFileResponse {}
export const ReadFileResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "ReadFileResponse",
}) as any as S.Schema<ReadFileResponse>;

export interface RemovePathRequest {
  /** VM id, or your slug for it */
  vmIdOrSlug: string;
  /** Absolute path inside the guest. */
  path: string;
}
export const RemovePathRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    vmIdOrSlug: S.String.pipe(T.Label()),
    path: S.String.pipe(T.Query()),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/v5/vms/{vmIdOrSlug}/fs/remove",
      code: 200,
    }),
  ),
).annotate({
  identifier: "RemovePathRequest",
}) as any as S.Schema<RemovePathRequest>;

export interface RemovePathResponse {}
export const RemovePathResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "RemovePathResponse",
}) as any as S.Schema<RemovePathResponse>;

export interface StatPathRequest {
  /** VM id, or your slug for it */
  vmIdOrSlug: string;
  /** Absolute path inside the guest. */
  path: string;
}
export const StatPathRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    vmIdOrSlug: S.String.pipe(T.Label()),
    path: S.String.pipe(T.Query()),
  }).pipe(
    T.Http({ method: "GET", uri: "/v5/vms/{vmIdOrSlug}/fs/stat", code: 200 }),
  ),
).annotate({
  identifier: "StatPathRequest",
}) as any as S.Schema<StatPathRequest>;

export interface FileStat {
  group: string;
  isDirectory: boolean;
  isFile: boolean;
  isSymlink: boolean;
  modified: string;
  owner: string;
  /** Octal mode, e.g. `0644`. */
  permissions: string;
  /** Size in bytes. */
  size: number;
}
export const FileStat = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    group: S.String,
    isDirectory: S.Boolean,
    isFile: S.Boolean,
    isSymlink: S.Boolean,
    modified: S.String,
    owner: S.String,
    permissions: S.String,
    size: S.Number,
  }),
).annotate({ identifier: "FileStat" }) as any as S.Schema<FileStat>;

export interface UploadAbortRequest {
  /** VM id, or your slug for it */
  vmIdOrSlug: string;
  /** Upload session id */
  uploadId: string;
}
export const UploadAbortRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    vmIdOrSlug: S.String.pipe(T.Label()),
    uploadId: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/v5/vms/{vmIdOrSlug}/fs/uploads/{uploadId}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "UploadAbortRequest",
}) as any as S.Schema<UploadAbortRequest>;

export interface UploadAbortResponse {}
export const UploadAbortResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "UploadAbortResponse",
}) as any as S.Schema<UploadAbortResponse>;

export interface UploadBeginRequest {
  /** VM id, or your slug for it */
  vmIdOrSlug: string;
  /** Optional final file mode bits (e.g. 493 for `0o755`). Defaults to the target's existing mode, or `0o600` for a new file. */
  mode?: number | null;
  /** Absolute path inside the guest the finished file is committed to. */
  path: string;
  /** Optional sha256 of the full file as 64 hex characters; the upload is rejected at commit if the received bytes do not match. */
  sha256?: string | null;
  /** Total file size in bytes, up to 16 GiB. */
  size: number;
}
export const UploadBeginRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    vmIdOrSlug: S.String.pipe(T.Label()),
    mode: S.optional(S.NullOr(S.Number)),
    path: S.String,
    sha256: S.optional(S.NullOr(S.String)),
    size: S.Number,
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v5/vms/{vmIdOrSlug}/fs/uploads",
      code: 200,
    }),
  ),
).annotate({
  identifier: "UploadBeginRequest",
}) as any as S.Schema<UploadBeginRequest>;

export interface UploadSessionStatus {
  /** Absolute guest path the file will be committed to. */
  path: string;
  /** Bytes stored so far — the offset the next chunk must start at. */
  receivedBytes: number;
  /** Total declared file size in bytes. */
  size: number;
  uploadId: string;
}
export const UploadSessionStatus = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    path: S.String,
    receivedBytes: S.Number,
    size: S.Number,
    uploadId: S.String,
  }),
).annotate({
  identifier: "UploadSessionStatus",
}) as any as S.Schema<UploadSessionStatus>;

export interface UploadChunkRequest {
  /** VM id, or your slug for it */
  vmIdOrSlug: string;
  /** Upload session id */
  uploadId: string;
  /** Byte offset this chunk starts at. Must equal the session's current `receivedBytes`; chunks at an already-received offset are acknowledged without rewriting (safe retries), and offsets beyond it are rejected. */
  offset: number;
}
export const UploadChunkRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    vmIdOrSlug: S.String.pipe(T.Label()),
    uploadId: S.String.pipe(T.Label()),
    offset: S.Number.pipe(T.Query()),
  }).pipe(
    T.Http({
      method: "PUT",
      uri: "/v5/vms/{vmIdOrSlug}/fs/uploads/{uploadId}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "UploadChunkRequest",
}) as any as S.Schema<UploadChunkRequest>;

export interface UploadCommitRequest {
  /** VM id, or your slug for it */
  vmIdOrSlug: string;
  /** Upload session id */
  uploadId: string;
}
export const UploadCommitRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    vmIdOrSlug: S.String.pipe(T.Label()),
    uploadId: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v5/vms/{vmIdOrSlug}/fs/uploads/{uploadId}/commit",
      code: 200,
    }),
  ),
).annotate({
  identifier: "UploadCommitRequest",
}) as any as S.Schema<UploadCommitRequest>;

export interface UploadCommitResponse {}
export const UploadCommitResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "UploadCommitResponse",
}) as any as S.Schema<UploadCommitResponse>;

export interface UploadStatusRequest {
  /** VM id, or your slug for it */
  vmIdOrSlug: string;
  /** Upload session id */
  uploadId: string;
}
export const UploadStatusRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    vmIdOrSlug: S.String.pipe(T.Label()),
    uploadId: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/v5/vms/{vmIdOrSlug}/fs/uploads/{uploadId}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "UploadStatusRequest",
}) as any as S.Schema<UploadStatusRequest>;

/** How `content` is encoded in the JSON form of a write. */
export type ContentEncoding = "utf8" | "base64";
export const ContentEncoding = S.String;

export interface WriteFileRequest {
  /** VM id, or your slug for it */
  vmIdOrSlug: string;
  /** Absolute path inside the guest. Required when the body is raw bytes; omitted when sending a JSON body, which carries its own `path`. */
  path?: string;
  /** Final file mode bits (e.g. 493 for `0o755`). Defaults to the target's existing mode, or `0o600` for a new file. */
  mode?: number;
  /** Optional sha256 of the file as 64 hex characters. Supplied, the write is rejected unless the received bytes match. */
  sha256?: string;
  /** The file's content, interpreted per `encoding`. */
  content: string;
  encoding?: ContentEncoding | (string & {}) | null;
}
export const WriteFileRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    vmIdOrSlug: S.String.pipe(T.Label()),
    path: S.optional(S.String.pipe(T.Query())),
    mode: S.optional(S.Number.pipe(T.Query())),
    sha256: S.optional(S.String.pipe(T.Query())),
    content: S.String,
    encoding: S.optional(S.NullOr(ContentEncoding)),
  }).pipe(
    T.Http({ method: "PUT", uri: "/v5/vms/{vmIdOrSlug}/fs/write", code: 200 }),
  ),
).annotate({
  identifier: "WriteFileRequest",
}) as any as S.Schema<WriteFileRequest>;

export interface WriteFileResponse {}
export const WriteFileResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "WriteFileResponse",
}) as any as S.Schema<WriteFileResponse>;

export type MakeDirError = BadRequest | NotFound | FreestyleOpError;
/** Create a directory inside the VM. */
export const makeDir: API.OperationMethod<
  MakeDirRequest,
  MakeDirResponse,
  MakeDirError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: MakeDirRequest,
  output: MakeDirResponse,
  errors: [BadRequest, NotFound, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));

export type PathExistsError = BadRequest | NotFound | FreestyleOpError;
/** Check whether a path exists inside the VM. */
export const pathExists: API.OperationMethod<
  PathExistsRequest,
  PathExistsResponse,
  PathExistsError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PathExistsRequest,
  output: PathExistsResponse,
  errors: [BadRequest, NotFound, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));

export type ReadDirError = BadRequest | NotFound | FreestyleOpError;
/** List the entries of a directory inside the VM. */
export const readDir: API.OperationMethod<
  ReadDirRequest,
  ReadDirResponse,
  ReadDirError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ReadDirRequest,
  output: ReadDirResponse,
  errors: [BadRequest, NotFound, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));

export type ReadFileError = BadRequest | NotFound | Conflict | FreestyleOpError;
/** Read a file from inside the VM. The response body is the file's raw bytes, streamed from the guest, so there is no size limit and nothing to decode. Send a `Range: bytes=…` header to fetch part of a file — which is also how you resume a download that failed partway. */
export const readFile: API.OperationMethod<
  ReadFileRequest,
  ReadFileResponse,
  ReadFileError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ReadFileRequest,
  output: ReadFileResponse,
  errors: [BadRequest, NotFound, Conflict, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));

export type RemovePathError = BadRequest | NotFound | FreestyleOpError;
/** Delete a file or directory inside the VM. */
export const removePath: API.OperationMethod<
  RemovePathRequest,
  RemovePathResponse,
  RemovePathError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RemovePathRequest,
  output: RemovePathResponse,
  errors: [BadRequest, NotFound, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));

export type StatPathError = BadRequest | NotFound | FreestyleOpError;
/** Fetch metadata for a path inside the VM. */
export const statPath: API.OperationMethod<
  StatPathRequest,
  FileStat,
  StatPathError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: StatPathRequest,
  output: FileStat,
  errors: [BadRequest, NotFound, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));

export type UploadAbortError = NotFound | Conflict | FreestyleOpError;
/** Abandon an upload session and free its server-side spool space. */
export const uploadAbort: API.OperationMethod<
  UploadAbortRequest,
  UploadAbortResponse,
  UploadAbortError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UploadAbortRequest,
  output: UploadAbortResponse,
  errors: [NotFound, Conflict, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));

export type UploadBeginError =
  | BadRequest
  | NotFound
  | Conflict
  | FreestyleOpError;
/** Start a chunked upload of a large file (up to 16 GiB) into the VM. Send the bytes with `PUT /fs/uploads/{uploadId}`, then finish with `commit`. */
export const uploadBegin: API.OperationMethod<
  UploadBeginRequest,
  UploadSessionStatus,
  UploadBeginError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UploadBeginRequest,
  output: UploadSessionStatus,
  errors: [BadRequest, NotFound, Conflict, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));

export type UploadChunkError =
  | BadRequest
  | NotFound
  | Conflict
  | FreestyleOpError;
/** Append one chunk of raw bytes to an upload session at the given offset. Chunks must arrive in order; on any transport error, re-fetch the session and resume from its `receivedBytes`. Keep chunks small enough to retry cheaply (the SDK uses 16 MiB; the hard cap is 256 MiB). */
export const uploadChunk: API.OperationMethod<
  UploadChunkRequest,
  UploadSessionStatus,
  UploadChunkError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UploadChunkRequest,
  output: UploadSessionStatus,
  errors: [BadRequest, NotFound, Conflict, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));

export type UploadCommitError =
  | BadRequest
  | NotFound
  | Conflict
  | FreestyleOpError;
/** Finish an upload: the file is verified (size, and sha256 when declared) and renamed into place inside the guest atomically — the target path never holds a partial file. On success the session is gone; on failure it remains and commit may be retried. */
export const uploadCommit: API.OperationMethod<
  UploadCommitRequest,
  UploadCommitResponse,
  UploadCommitError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UploadCommitRequest,
  output: UploadCommitResponse,
  errors: [BadRequest, NotFound, Conflict, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));

export type UploadStatusError = NotFound | FreestyleOpError;
/** Fetch an upload session's progress — use `receivedBytes` as the next chunk's offset when resuming after a failed chunk. */
export const uploadStatus: API.OperationMethod<
  UploadStatusRequest,
  UploadSessionStatus,
  UploadStatusError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UploadStatusRequest,
  output: UploadSessionStatus,
  errors: [NotFound, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));

export type WriteFileError =
  | BadRequest
  | NotFound
  | Conflict
  | FreestyleOpError;
/** Write a file inside the VM, atomically: the path holds the old file until the new one has arrived intact, never a half-written one. Send the file as the raw request body (with `?path=`) and it can be as large as 16 GiB — nothing buffers it whole. Send a JSON body instead (up to 32 MiB) when it is easier to type than to upload, as from this page. This is a single attempt: if the connection drops, the write starts over. To upload something big over a link you do not trust, open an upload session (`POST /fs/uploads`) and send retryable chunks instead. */
export const writeFile: API.OperationMethod<
  WriteFileRequest,
  WriteFileResponse,
  WriteFileError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: WriteFileRequest,
  output: WriteFileResponse,
  errors: [BadRequest, NotFound, Conflict, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));
