// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/spec-mirror-freestyle/specs/openapi.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as Redacted from "effect/Redacted";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  FreestyleProtocol,
  type FreestyleOpError,
  type FreestyleOpContext,
} from "../protocol.ts";
import { paginateOffset } from "../pagination.ts";
import { UnknownFreestyleError } from "../errors.ts";
import * as Retry from "../retry.ts";

export type { FreestyleOpError, FreestyleOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Conflict
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Conflict>()("Conflict", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withConflictError),
    [{ status: 409 }],
  ) {}

export class Forbidden
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Forbidden>()("Forbidden", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withAuthError),
    [{ status: 403 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export interface AttachPtyRequest {
  /** VM id, or your slug for it */
  vmIdOrSlug: string;
  /** Session id, or the slug the session was opened with */
  sessionId: string;
  /** Guest Linux user that must own the session */
  linuxUser?: string;
}
export const AttachPtyRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    vmIdOrSlug: S.String.pipe(T.Label()),
    sessionId: S.String.pipe(T.Label()),
    linuxUser: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/v5/vms/{vmIdOrSlug}/pty/sessions/{sessionId}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "AttachPtyRequest",
}) as any as S.Schema<AttachPtyRequest>;

export interface AttachPtyResponse {}
export const AttachPtyResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "AttachPtyResponse",
}) as any as S.Schema<AttachPtyResponse>;

export interface ClosePtySessionRequest {
  /** VM id, or your slug for it */
  vmIdOrSlug: string;
  /** Session id, or the slug the session was opened with */
  sessionId: string;
  /** Guest Linux user that must own the session */
  linuxUser?: string;
}
export const ClosePtySessionRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    vmIdOrSlug: S.String.pipe(T.Label()),
    sessionId: S.String.pipe(T.Label()),
    linuxUser: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/v5/vms/{vmIdOrSlug}/pty/sessions/{sessionId}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ClosePtySessionRequest",
}) as any as S.Schema<ClosePtySessionRequest>;

export interface ClosePtySessionResponse {
  /** Exit code if the session had already exited. Absent when it was still running: it is sent `SIGHUP` and torn down asynchronously. */
  exitCode?: number | null;
  sessionId: number;
}
export const ClosePtySessionResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    exitCode: S.optional(S.NullOr(S.Number)),
    sessionId: S.Number,
  }),
).annotate({
  identifier: "ClosePtySessionResponse",
}) as any as S.Schema<ClosePtySessionResponse>;

/** What a rule does with the traffic it matches. Only `allow` exists today, and it is required on every rule rather than defaulted: a default would mean that the day `deny` arrives, every rule written before it silently acquires an action it never stated. */
export type FirewallAction = "allow";
export const FirewallAction = S.String;

/** The transport a matcher is restricted to. */
export type FirewallProtocol = "tcp" | "udp" | "icmp";
export const FirewallProtocol = S.String;

/** One end of a rule: which traffic this side matches. Fields intersect — `{ vpcId, port, protocol }` is "traffic in that network, on that port, over that protocol". `vmId`/`vpcId`/`tunnelId` select by identity (the VM however it is addressed); `cidr`/`public` select by address. Unknown fields are rejected rather than ignored: a matcher is a security statement, and a typo'd `porrt: 22` that quietly became "every port" is the wrong failure. */
export interface FirewallEndpoint {
  /** An address range, IPv4 or IPv6, in canonical `network/prefix` form. */
  cidr?: string | null;
  /** A single port, 1–65535. Requires `protocol`. */
  port?: number | null;
  protocol?: FirewallProtocol | (string & {}) | null;
  /** Every publicly routable address, whoever owns it — including another Freestyle VM reached at its public address. Only `true` is meaningful; omit the field otherwise. Not combined with `vmId` or `vpcId`. */
  public?: boolean | null;
  /** Whatever is on the far side of one tunnel — the client that dials it, and any remote range routed over it. Tunnels have no slug, so this is a tunnel id: `tun-…`, or `wg-…` for one carried over from the per-VPC VPN records tunnels replaced. */
  tunnelId?: string | null;
  /** A single VM, by id or by your slug for it. Answers hold its id. */
  vmId?: string | null;
  /** Everything on one private network, by id or by your slug for it. Answers hold its id. */
  vpcId?: string | null;
}
export const FirewallEndpoint = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    cidr: S.optional(S.NullOr(S.String)),
    port: S.optional(S.NullOr(S.Number)),
    protocol: S.optional(S.NullOr(FirewallProtocol)),
    public: S.optional(S.NullOr(S.Boolean)),
    tunnelId: S.optional(S.NullOr(S.String)),
    vmId: S.optional(S.NullOr(S.String)),
    vpcId: S.optional(S.NullOr(S.String)),
  }),
).annotate({
  identifier: "FirewallEndpoint",
}) as any as S.Schema<FirewallEndpoint>;

export interface CreateFirewallRuleRequest {
  /** Required, and `allow` is the only accepted value. */
  action: FirewallAction | (string & {});
  /** A free-form note, up to 1024 characters. */
  description?: string | null;
  destination: FirewallEndpoint;
  source: FirewallEndpoint;
}
export const CreateFirewallRuleRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    action: FirewallAction,
    description: S.optional(S.NullOr(S.String)),
    destination: FirewallEndpoint,
    source: FirewallEndpoint,
  }),
).annotate({
  identifier: "CreateFirewallRuleRequest",
}) as any as S.Schema<CreateFirewallRuleRequest>;

export type FirewallSpecRulesList = Array<CreateFirewallRuleRequest>;
export const FirewallSpecRulesList = /*@__PURE__*/ S.Array(
  CreateFirewallRuleRequest,
) as any as S.Schema<FirewallSpecRulesList>;

/** The `firewall` block of a create: rules created with the resource and deleted with it. Inside it, an endpoint with no identity means the resource being created — which is how a rule names something whose id does not exist yet. */
export interface FirewallSpec {
  rules?: FirewallSpecRulesList;
}
export const FirewallSpec = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    rules: S.optional(FirewallSpecRulesList),
  }),
).annotate({ identifier: "FirewallSpec" }) as any as S.Schema<FirewallSpec>;

/** Up to 64 metadata entries; keys and values are limited to 63 characters. */
export type CreateVmRequestMetadataMap = { [key: string]: string | undefined };
export const CreateVmRequestMetadataMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<CreateVmRequestMetadataMap>;

/** An in-guest static route: reach `cidr` via the gateway `via`, which must be an address inside the network's CIDR. */
export interface NetworkRoute {
  cidr: string;
  /** Route priority, as `ip route`'s `metric`. Omit for the platform default. Several routes to the same `cidr` at the same metric are one multipath route — the guest spreads flows across their gateways — while different metrics make a primary and a backup. */
  metric?: number | null;
  via: string;
}
export const NetworkRoute = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    cidr: S.String,
    metric: S.optional(S.NullOr(S.Number)),
    via: S.String,
  }),
).annotate({ identifier: "NetworkRoute" }) as any as S.Schema<NetworkRoute>;

/** Static routes to install in the guest. */
export type AttachNetworkRoutesList = Array<NetworkRoute>;
export const AttachNetworkRoutesList = /*@__PURE__*/ S.Array(
  NetworkRoute,
) as any as S.Schema<AttachNetworkRoutesList>;

/** Attach a VM to one of your private networks. A VM may be on at most one. */
export interface AttachNetwork {
  /** The IPv4 address to take inside the network: an explicit address, `true` to auto-allocate, or `false` to opt out. Omitted = allocate one (networks are dual-stack), except on a network with no IPv4 CIDR, where the VM simply joins over IPv6. */
  ipv4?: unknown;
  /** The IPv6 address to take: an explicit address, `true` to auto-allocate (the default), or `false` to opt out. Every network has an IPv6 CIDR. */
  ipv6?: unknown;
  /** Static routes to install in the guest. */
  routes?: AttachNetworkRoutesList;
  /** The network to join, by id or by your slug for it. Also accepted as `vpcId`, the name the rest of the API uses. */
  vpc: string;
}
export const AttachNetwork = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ipv4: S.optional(S.Unknown),
    ipv6: S.optional(S.Unknown),
    routes: S.optional(AttachNetworkRoutesList),
    vpc: S.String,
  }),
).annotate({ identifier: "AttachNetwork" }) as any as S.Schema<AttachNetwork>;

/** Put the VM on your private networks. Omit to leave it off them. At most one network is accepted today. Also accepted as `vpcs`, the name the rest of the API uses. */
export type CreateVmRequestNetworksList = Array<AttachNetwork>;
export const CreateVmRequestNetworksList = /*@__PURE__*/ S.Array(
  AttachNetwork,
) as any as S.Schema<CreateVmRequestNetworksList>;

/** What a rule does with the named session it matches. Only `allow` exists today, and it is required on every rule rather than defaulted: a default would mean that the day `deny` arrives, every rule written before it silently acquires an action it never stated. */
export type TlsAction = "allow";
export const TlsAction = S.String;

/** One end of a rule: which session this side matches. A `source` says *who* may open the session (`vmId`/`vpcId`/`public: true`); a `destination` says *where it lands* (`vmId`+`port`, `host`+`port`, or `public: true` for the domain's own origin). Unknown fields are rejected rather than ignored: a rule is a security statement, and a typo that quietly widened it is the wrong failure. */
export interface TlsEndpoint {
  /** **Destination only.** Pin the session to this host instead of resolving the domain, while still presenting `domain` as the SNI. */
  host?: string | null;
  /** **Destination only.** The port the session lands on. Required with a `vmId` or `host` destination; defaults to 443 for a `public` origin. */
  port?: number | null;
  /** On a source, every publicly routable client — the open Internet dials the name. On a destination, the domain's own public origin. Only `true` is meaningful; omit the field otherwise. */
  public?: boolean | null;
  /** A single VM, by id or by your slug for it. On a source, the VM that may open the session; on a destination, the VM it lands on. Answers hold its id. */
  vmId?: string | null;
  /** Everything on one private network, by id or by your slug for it. **Source only** — a landing is one place, so a network cannot be a destination. Answers hold its id. */
  vpcId?: string | null;
}
export const TlsEndpoint = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    host: S.optional(S.NullOr(S.String)),
    port: S.optional(S.NullOr(S.Number)),
    public: S.optional(S.NullOr(S.Boolean)),
    vmId: S.optional(S.NullOr(S.String)),
    vpcId: S.optional(S.NullOr(S.String)),
  }),
).annotate({ identifier: "TlsEndpoint" }) as any as S.Schema<TlsEndpoint>;

/** A reusable authorization check attached to public HTTP ingress. */
export interface TlsForwardAuthReference {
  /** The configuration id returned by `POST /v5/tls/forward-auth`. */
  id: string;
}
export const TlsForwardAuthReference = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
  }),
).annotate({
  identifier: "TlsForwardAuthReference",
}) as any as S.Schema<TlsForwardAuthReference>;

/** Case-sensitive HTTP method tokens. Omitted matches any; an empty list matches none. */
export type HttpRequestMatchMethodList = Array<string>;
export const HttpRequestMatchMethodList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<HttpRequestMatchMethodList>;

/** Exact request path. Prefix and regex matching are not supported yet. */
export interface HttpPathMatch {
  exact: string;
}
export const HttpPathMatch = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    exact: S.String,
  }),
).annotate({ identifier: "HttpPathMatch" }) as any as S.Schema<HttpPathMatch>;

/** Select which HTTP egress requests receive a rule's transforms. Included dimensions are ANDed; methods are ORed. An empty object matches all. A non-match skips every transform, without denying access to the origin. See <https://vercel.com/docs/sandbox/concepts/firewall#matchers>. */
export interface HttpRequestMatch {
  /** Case-sensitive HTTP method tokens. Omitted matches any; an empty list matches none. */
  method?: HttpRequestMatchMethodList | null;
  path?: HttpPathMatch | null;
}
export const HttpRequestMatch = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    method: S.optional(S.NullOr(HttpRequestMatchMethodList)),
    path: S.optional(S.NullOr(HttpPathMatch)),
  }),
).annotate({
  identifier: "HttpRequestMatch",
}) as any as S.Schema<HttpRequestMatch>;

/** How the edge treats the session's bytes, and therefore which of its front doors serves the rule. `http` (443) and `postgres` (5432) terminate the session, so each pairs with the transform kind that speaks it and each needs a certificate for the name. `minecraft` (25565) routes on the server address in the client's handshake and then splices — no TLS, so no certificate and no transform. The other two doors carry a terminating protocol and a splicing one at once, and a rule picks which it is. `tcp` shares 443 with `http`: match on SNI, splice the bytes, terminate nothing, so your VM's own certificate answers the handshake. `imap` and `imaps` share 993 for mail clients — `imap` terminates on a certificate issued for the name and forwards plain IMAP to the port your server listens on (143 by default), `imaps` splices to a server holding its own certificate on 993. All three splicing shapes are public ingress only. `socks5` (1080) goes the other way and is the only egress-only protocol. Your VM opens SOCKS5 to the platform edge with no authentication, names its own destination inside the request, and the edge opens the session onward through your proxy provider using the credentials a `socks5` transform carries — so the workload can use the subscription without ever holding it. Nothing is terminated: your TLS runs end to end to the real origin. The rule's `domain` is an allow-list of destinations rather than a name anything resolves, which is why `*` is allowed here and refused everywhere else. */
export type TlsProtocolDocumented =
  | "tcp"
  | "http"
  | "postgres"
  | "minecraft"
  | "imap"
  | "imaps"
  | "socks5";
export const TlsProtocolDocumented = S.String;

/** Set request headers on an `http` session. Header names are preserved (they are not secret); values are write-only. */
export type TlsTransformDocumentedCase0HeadersMap = {
  [key: string]: string | undefined;
};
export const TlsTransformDocumentedCase0HeadersMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<TlsTransformDocumentedCase0HeadersMap>;

/** Set request headers on an `http` session. Header names are preserved (they are not secret); values are write-only. */
export interface TlsTransformDocumentedCase0 {
  /** Set request headers on an `http` session. Header names are preserved (they are not secret); values are write-only. */
  headers: TlsTransformDocumentedCase0HeadersMap;
}
export const TlsTransformDocumentedCase0 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    headers: TlsTransformDocumentedCase0HeadersMap,
  }),
).annotate({
  identifier: "TlsTransformDocumentedCase0",
}) as any as S.Schema<TlsTransformDocumentedCase0>;

export type PatchOperationCase0Op = "add";
export const PatchOperationCase0Op = S.String;

/** 'add' operation */
export interface PatchOperationCase0 {
  /** JSON-Pointer value [RFC6901](https://tools.ietf.org/html/rfc6901) that references a location within the target document where the operation is performed. */
  path: string;
  /** Value to add to the target location. */
  value: unknown;
  op: PatchOperationCase0Op;
}
export const PatchOperationCase0 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    path: S.String,
    value: S.Unknown,
    op: PatchOperationCase0Op,
  }),
).annotate({
  identifier: "PatchOperationCase0",
}) as any as S.Schema<PatchOperationCase0>;

export type PatchOperationCase1Op = "remove";
export const PatchOperationCase1Op = S.String;

/** 'remove' operation */
export interface PatchOperationCase1 {
  /** JSON-Pointer value [RFC6901](https://tools.ietf.org/html/rfc6901) that references a location within the target document where the operation is performed. */
  path: string;
  op: PatchOperationCase1Op;
}
export const PatchOperationCase1 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    path: S.String,
    op: PatchOperationCase1Op,
  }),
).annotate({
  identifier: "PatchOperationCase1",
}) as any as S.Schema<PatchOperationCase1>;

export type PatchOperationCase2Op = "replace";
export const PatchOperationCase2Op = S.String;

/** 'replace' operation */
export interface PatchOperationCase2 {
  /** JSON-Pointer value [RFC6901](https://tools.ietf.org/html/rfc6901) that references a location within the target document where the operation is performed. */
  path: string;
  /** Value to replace with. */
  value: unknown;
  op: PatchOperationCase2Op;
}
export const PatchOperationCase2 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    path: S.String,
    value: S.Unknown,
    op: PatchOperationCase2Op,
  }),
).annotate({
  identifier: "PatchOperationCase2",
}) as any as S.Schema<PatchOperationCase2>;

export type PatchOperationCase3Op = "move";
export const PatchOperationCase3Op = S.String;

/** 'move' operation */
export interface PatchOperationCase3 {
  /** JSON-Pointer value [RFC6901](https://tools.ietf.org/html/rfc6901) that references a location to move value from. */
  from: string;
  /** JSON-Pointer value [RFC6901](https://tools.ietf.org/html/rfc6901) that references a location within the target document where the operation is performed. */
  path: string;
  op: PatchOperationCase3Op;
}
export const PatchOperationCase3 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    from: S.String,
    path: S.String,
    op: PatchOperationCase3Op,
  }),
).annotate({
  identifier: "PatchOperationCase3",
}) as any as S.Schema<PatchOperationCase3>;

export type PatchOperationCase4Op = "copy";
export const PatchOperationCase4Op = S.String;

/** 'copy' operation */
export interface PatchOperationCase4 {
  /** JSON-Pointer value [RFC6901](https://tools.ietf.org/html/rfc6901) that references a location to copy value from. */
  from: string;
  /** JSON-Pointer value [RFC6901](https://tools.ietf.org/html/rfc6901) that references a location within the target document where the operation is performed. */
  path: string;
  op: PatchOperationCase4Op;
}
export const PatchOperationCase4 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    from: S.String,
    path: S.String,
    op: PatchOperationCase4Op,
  }),
).annotate({
  identifier: "PatchOperationCase4",
}) as any as S.Schema<PatchOperationCase4>;

export type PatchOperationCase5Op = "test";
export const PatchOperationCase5Op = S.String;

/** 'test' operation */
export interface PatchOperationCase5 {
  /** JSON-Pointer value [RFC6901](https://tools.ietf.org/html/rfc6901) that references a location within the target document where the operation is performed. */
  path: string;
  /** Value to test against. */
  value: unknown;
  op: PatchOperationCase5Op;
}
export const PatchOperationCase5 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    path: S.String,
    value: S.Unknown,
    op: PatchOperationCase5Op,
  }),
).annotate({
  identifier: "PatchOperationCase5",
}) as any as S.Schema<PatchOperationCase5>;

/** JSON Patch single patch operation */
export type PatchOperation =
  | PatchOperationCase0
  | PatchOperationCase1
  | PatchOperationCase2
  | PatchOperationCase3
  | PatchOperationCase4
  | PatchOperationCase5;
export const PatchOperation =
  /*@__PURE__*/ S.Unknown as any as S.Schema<PatchOperation>;

/** Ordered RFC 6902 operations. Values are write-only and read back as `"***"`. A rule accepts one patch, with at most 128 operations and 64 KiB of configuration. */
export type JsonPatch = Array<PatchOperation>;
export const JsonPatch = /*@__PURE__*/ S.Array(
  PatchOperation,
) as any as S.Schema<JsonPatch>;

/** Apply RFC 6902 operations to JSON HTTP egress request bodies. Values are write-only; operation names and JSON Pointer paths remain visible. */
export interface TlsTransformDocumentedCase1 {
  /** Apply RFC 6902 operations to JSON HTTP egress request bodies. Values are write-only; operation names and JSON Pointer paths remain visible. */
  jsonPatch: JsonPatch;
}
export const TlsTransformDocumentedCase1 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    jsonPatch: JsonPatch,
  }),
).annotate({
  identifier: "TlsTransformDocumentedCase1",
}) as any as S.Schema<TlsTransformDocumentedCase1>;

/** Credentials injected into a Postgres startup handshake. The guest connects with no password; the edge completes the real handshake upstream. */
export interface PostgresTransform {
  /** The database to select. Optional: omitted leaves the guest's choice. */
  database?: string | null;
  /** The upstream password. Write-only: sealed at rest, read back as `"***"`. */
  password: string | Redacted.Redacted<string>;
  /** The upstream role to authenticate as. */
  username: string;
}
export const PostgresTransform = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    database: S.optional(S.NullOr(S.String)),
    password: S.String.pipe(T.SensitiveValue({})),
    username: S.String,
  }),
).annotate({
  identifier: "PostgresTransform",
}) as any as S.Schema<PostgresTransform>;

/** Inject credentials into a `postgres` handshake. */
export interface TlsTransformDocumentedCase2 {
  /** Inject credentials into a `postgres` handshake. */
  postgres: PostgresTransform;
}
export const TlsTransformDocumentedCase2 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    postgres: PostgresTransform,
  }),
).annotate({
  identifier: "TlsTransformDocumentedCase2",
}) as any as S.Schema<TlsTransformDocumentedCase2>;

/** Credentials presented to an upstream SOCKS5 proxy. Your VM connects to the platform's SOCKS5 front with no authentication at all; the edge authenticates to your provider on its behalf, so the subscription is never inside the VM. */
export interface Socks5Transform {
  /** The password to present to the upstream proxy. Write-only: sealed at rest, read back as `"***"`. */
  password: string | Redacted.Redacted<string>;
  /** The username to present to the upstream proxy. */
  username: string;
}
export const Socks5Transform = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    password: S.String.pipe(T.SensitiveValue({})),
    username: S.String,
  }),
).annotate({
  identifier: "Socks5Transform",
}) as any as S.Schema<Socks5Transform>;

/** Authenticate a `socks5` session to your upstream proxy provider. */
export interface TlsTransformDocumentedCase3 {
  /** Authenticate a `socks5` session to your upstream proxy provider. */
  socks5: Socks5Transform;
}
export const TlsTransformDocumentedCase3 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    socks5: Socks5Transform,
  }),
).annotate({
  identifier: "TlsTransformDocumentedCase3",
}) as any as S.Schema<TlsTransformDocumentedCase3>;

/** The rewrites a rule applies, in order. */
export type TlsTransformDocumented =
  | TlsTransformDocumentedCase0
  | TlsTransformDocumentedCase1
  | TlsTransformDocumentedCase2
  | TlsTransformDocumentedCase3;
export const TlsTransformDocumented =
  /*@__PURE__*/ S.Unknown as any as S.Schema<TlsTransformDocumented>;

/** Rewrites applied in flight, in order. Secrets here are write-only. */
export type CreateTlsRuleRequestTransformList = Array<TlsTransformDocumented>;
export const CreateTlsRuleRequestTransformList = /*@__PURE__*/ S.Array(
  TlsTransformDocumented,
) as any as S.Schema<CreateTlsRuleRequestTransformList>;

export interface CreateTlsRuleRequest {
  /** Required, and `allow` is the only accepted value. */
  action: TlsAction | (string & {});
  /** A free-form note, up to 1024 characters. */
  description?: string | null;
  destination?: TlsEndpoint;
  /** The name the session is addressed to: exact, `*.suffix`, or `*`. */
  domain: string;
  forwardAuth?: TlsForwardAuthReference | null;
  match?: HttpRequestMatch | null;
  protocol?: TlsProtocolDocumented | (string & {}) | null;
  source?: TlsEndpoint;
  /** Rewrites applied in flight, in order. Secrets here are write-only. */
  transform?: CreateTlsRuleRequestTransformList;
}
export const CreateTlsRuleRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    action: TlsAction,
    description: S.optional(S.NullOr(S.String)),
    destination: S.optional(TlsEndpoint),
    domain: S.String,
    forwardAuth: S.optional(S.NullOr(TlsForwardAuthReference)),
    match: S.optional(S.NullOr(HttpRequestMatch)),
    protocol: S.optional(S.NullOr(TlsProtocolDocumented)),
    source: S.optional(TlsEndpoint),
    transform: S.optional(CreateTlsRuleRequestTransformList),
  }),
).annotate({
  identifier: "CreateTlsRuleRequest",
}) as any as S.Schema<CreateTlsRuleRequest>;

export type TlsSpecRulesList = Array<CreateTlsRuleRequest>;
export const TlsSpecRulesList = /*@__PURE__*/ S.Array(
  CreateTlsRuleRequest,
) as any as S.Schema<TlsSpecRulesList>;

/** The `tls` block of a VM create: rules created with the VM and deleted with it. Inside it, an endpoint with no identity means the VM being created — which is how a rule names a VM whose id does not exist yet. */
export interface TlsSpec {
  rules?: TlsSpecRulesList;
}
export const TlsSpec = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    rules: S.optional(TlsSpecRulesList),
  }),
).annotate({ identifier: "TlsSpec" }) as any as S.Schema<TlsSpec>;

export interface CreateVmRequest {
  /** Delete the VM once it has gone this many seconds without running. Every start resets the clock; a running VM is never deleted for this. -1 (or omitting this) means never; 0 makes the VM ephemeral — deleted the moment it stops. Some plans cap how long an unused VM is kept. On those, omitting this (or sending -1) gets you the cap rather than "keep forever", and asking for longer than the cap is a 400. */
  autoDeleteSeconds?: number | null;
  /** Boot the VM again if it stops without you asking it to — the VMM crashes, the guest kernel panics, the machine it runs on loses power. Defaults to `true`. It never fights you: shutting the VM down, pausing it, running `poweroff` inside it, or hitting an idle timeout all leave it off. This only decides who gets the last word when a *failure* stops it. */
  automaticRestart?: boolean | null;
  displayName?: string | null;
  /** Firewall rules to create with the VM, and delete with it. An endpoint with no identity means the VM being created, so `{ "source": { "public": true }, "destination": { "port": 443, "protocol": "tcp" } }` opens inbound HTTPS to it. **Required.** A VM gets nothing implicitly — no outbound Internet, no inbound — so a caller that wants either says so here. `{"rules": []}` is a legitimate answer: a VM reachable only through a mapped domain or SSH. */
  firewall: FirewallSpec;
  /** Pause the VM after this many seconds without network activity. -1 (or omitting this) means never pause for idleness. */
  idleTimeoutSeconds?: number | null;
  /** Pause the VM once a single run has lasted this many seconds, however busy it is. Starting it again gives it a fresh budget — unlike `maxRunTotalSeconds`, which never resets. -1 (or omitting this) means no cap. */
  maxRunSeconds?: number | null;
  /** How many seconds this VM may run in total, ever. Spending the budget pauses the VM, and every later start is a 409 until you raise this. -1 (or omitting this) means no budget. */
  maxRunTotalSeconds?: number | null;
  /** Up to 64 metadata entries; keys and values are limited to 63 characters. */
  metadata?: CreateVmRequestMetadataMap;
  /** Put the VM on your private networks. Omit to leave it off them. At most one network is accepted today. Also accepted as `vpcs`, the name the rest of the API uses. */
  networks?: CreateVmRequestNetworksList;
  /** Take `slug` even if another VM in this account already holds it. The slug moves atomically: the current holder keeps running, stays addressable by id, and just loses its slug. Without this flag a taken slug is a 409 CONFLICT. Requires `slug`. */
  reassignSlug?: boolean;
  /** URL-safe identifier, unique within your account: 1–63 chars of `[a-z0-9-]`, no leading, trailing, or repeated hyphens. */
  slug?: string | null;
  /** Boot from this snapshot: its id, your slug for it, or a public `{owner}/{slug}`. Omit for the platform default. */
  snapshotId?: string | null;
  tls?: TlsSpec | null;
  /** Delete the VM this many seconds after it is created, whatever it is doing at the time. A deadline, not an idle window: nothing resets it. -1 (or omitting this) means no deadline. */
  ttlSeconds?: number | null;
}
export const CreateVmRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    autoDeleteSeconds: S.optional(S.NullOr(S.Number)),
    automaticRestart: S.optional(S.NullOr(S.Boolean)),
    displayName: S.optional(S.NullOr(S.String)),
    firewall: FirewallSpec,
    idleTimeoutSeconds: S.optional(S.NullOr(S.Number)),
    maxRunSeconds: S.optional(S.NullOr(S.Number)),
    maxRunTotalSeconds: S.optional(S.NullOr(S.Number)),
    metadata: S.optional(CreateVmRequestMetadataMap),
    networks: S.optional(CreateVmRequestNetworksList),
    reassignSlug: S.optional(S.Boolean),
    slug: S.optional(S.NullOr(S.String)),
    snapshotId: S.optional(S.NullOr(S.String)),
    tls: S.optional(S.NullOr(TlsSpec)),
    ttlSeconds: S.optional(S.NullOr(S.Number)),
  }).pipe(T.Http({ method: "POST", uri: "/v5/vms", code: 200 })),
).annotate({
  identifier: "CreateVmRequest",
}) as any as S.Schema<CreateVmRequest>;

export type VmMetadataMap = { [key: string]: string | undefined };
export const VmMetadataMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<VmMetadataMap>;

export type VmNetworkRoutesList = Array<NetworkRoute>;
export const VmNetworkRoutesList = /*@__PURE__*/ S.Array(
  NetworkRoute,
) as any as S.Schema<VmNetworkRoutesList>;

/** A VM's place on a private network. */
export interface VmNetwork {
  /** The network's CIDR, alongside `ipv4`. */
  cidr?: string | null;
  /** The network's IPv6 CIDR, alongside `ipv6`. */
  cidrV6?: string | null;
  /** The VM's IPv4 address inside the network; absent when the member opted out of IPv4, or the network has no IPv4 CIDR. */
  ipv4?: string | null;
  /** The VM's IPv6 address, when the network has an IPv6 CIDR. */
  ipv6?: string | null;
  routes?: VmNetworkRoutesList;
  /** Deprecated: the network's id under its old name. Read `vpcId`. */
  vpc: string;
  /** The network's display name now, under the same rules. */
  vpcDisplayName?: string | null;
  vpcId?: string | null;
  /** What the network is called *now*. Absent when it has no slug and when it is gone, so read against `vpcSlugAtAttach` a renamed network tells apart from a deleted one. */
  vpcSlug?: string | null;
  /** Your slug for the network when the VM was attached to it, if it had one. A convenience for reading an answer back — `vpc` is the id, and the id is what to store or compare. It keeps naming what the VM was attached by after a rename, so this is not a way to find the network again. */
  vpcSlugAtAttach?: string | null;
}
export const VmNetwork = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    cidr: S.optional(S.NullOr(S.String)),
    cidrV6: S.optional(S.NullOr(S.String)),
    ipv4: S.optional(S.NullOr(S.String)),
    ipv6: S.optional(S.NullOr(S.String)),
    routes: S.optional(VmNetworkRoutesList),
    vpc: S.String,
    vpcDisplayName: S.optional(S.NullOr(S.String)),
    vpcId: S.optional(S.NullOr(S.String)),
    vpcSlug: S.optional(S.NullOr(S.String)),
    vpcSlugAtAttach: S.optional(S.NullOr(S.String)),
  }),
).annotate({ identifier: "VmNetwork" }) as any as S.Schema<VmNetwork>;

/** Deprecated: the same list as `vpcs`, kept for old readers. */
export type VmNetworksList = Array<VmNetwork>;
export const VmNetworksList = /*@__PURE__*/ S.Array(
  VmNetwork,
) as any as S.Schema<VmNetworksList>;

export type VmLabelSelectorMatchLabelsMap = {
  [key: string]: string | undefined;
};
export const VmLabelSelectorMatchLabelsMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<VmLabelSelectorMatchLabelsMap>;

/** Selects VMs by their metadata: a VM matches when its metadata contains every `matchLabels` entry. At least 1 and at most 8 entries; keys and values are limited to 63 characters, like the metadata they match. */
export interface VmLabelSelector {
  matchLabels: VmLabelSelectorMatchLabelsMap;
}
export const VmLabelSelector = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    matchLabels: VmLabelSelectorMatchLabelsMap,
  }),
).annotate({
  identifier: "VmLabelSelector",
}) as any as S.Schema<VmLabelSelector>;

/** The topology domain an anti-affinity rule spreads across. `node` — the only domain today — means "not on the same host machine". */
export type VmPlacementTopology = "node";
export const VmPlacementTopology = S.String;

/** One anti-affinity rule: "not on the same `topology` domain as any VM matching `selector`". At most 8 rules per VM. */
export interface VmAntiAffinityRule {
  selector: VmLabelSelector;
  topology: VmPlacementTopology;
}
export const VmAntiAffinityRule = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    selector: VmLabelSelector,
    topology: VmPlacementTopology,
  }),
).annotate({
  identifier: "VmAntiAffinityRule",
}) as any as S.Schema<VmAntiAffinityRule>;

/** Hard rules: the VM will not land on a topology domain already hosting a VM matched by any rule's selector. */
export type VmPlacementAntiAffinityList = Array<VmAntiAffinityRule>;
export const VmPlacementAntiAffinityList = /*@__PURE__*/ S.Array(
  VmAntiAffinityRule,
) as any as S.Schema<VmPlacementAntiAffinityList>;

/** Placement constraints for a VM, fixed at create time and honored again whenever the platform relocates the VM. */
export interface VmPlacement {
  /** Hard rules: the VM will not land on a topology domain already hosting a VM matched by any rule's selector. */
  antiAffinity?: VmPlacementAntiAffinityList;
}
export const VmPlacement = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    antiAffinity: S.optional(VmPlacementAntiAffinityList),
  }),
).annotate({ identifier: "VmPlacement" }) as any as S.Schema<VmPlacement>;

export interface VmResources {
  /** vCPU count. */
  cpu: number;
  /** Memory, MiB. */
  memory: number;
  /** Disk, MiB. */
  storage: number;
}
export const VmResources = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    cpu: S.Number,
    memory: S.Number,
    storage: S.Number,
  }),
).annotate({ identifier: "VmResources" }) as any as S.Schema<VmResources>;

export type VmState = "starting" | "running" | "pausing" | "paused" | "stopped";
export const VmState = S.String;

/** A VM as the owning account sees it. Placement and topology internals (hosting manager, VXLAN ids, node-local network allocation) are not part of this contract. */
export interface Vm {
  /** Whether `autoDeleteSeconds` above is your plan's doing rather than yours. A window your plan set goes away by itself if you move to a plan that does not reclaim unused VMs; one you set is yours and stays. */
  autoDeleteFromPlan?: boolean;
  /** Delete the VM once it has gone this long without running. Absent means never; 0 means the VM is ephemeral — deleted the moment it stops. Note that this may be set for you: some plans cap how long an unused VM is kept, and the cap shows up here. */
  autoDeleteSeconds?: number | null;
  /** Whether the VM is booted again when it stops without being asked to. */
  automaticRestart?: boolean | null;
  /** Total CPU time consumed, seconds. */
  cpuTimeSeconds?: number | null;
  createdAt: string;
  displayName?: string | null;
  /** The public IPv4 address the VM's outbound traffic is seen as coming from. It belongs to your account rather than to this VM: every VM you own shares it, and it survives restarts and moves between machines. Optional, and worth reading as optional even though a VM in service has one: nothing promises an account keeps a single outbound address, so allow for it being absent rather than depending on it being there. */
  egressIpv4?: string | null;
  /** The public IPv6 address the VM's outbound traffic is seen as coming from. Today it is the VM's own `publicIpv6` — it appears from the address it answers on — but read it as its own field: where a VM answers and where it appears from are two questions, and `egressIpv4` above already answers them differently. */
  egressIpv6?: string | null;
  id: string;
  idleTimeoutSeconds?: number | null;
  /** The image the VM booted from, when it booted from one. */
  image?: string | null;
  lastNetworkActivity?: string | null;
  /** Pause the VM once one run has lasted this long, however busy it is. */
  maxRunSeconds?: number | null;
  /** The VM's lifetime runtime budget in seconds. Spending it pauses the VM and makes every later start a 409 until you raise this. */
  maxRunTotalSeconds?: number | null;
  metadata?: VmMetadataMap;
  /** Deprecated: the same list as `vpcs`, kept for old readers. */
  networks?: VmNetworksList;
  placement?: VmPlacement | null;
  /** The VM's stable public IPv6 address — assigned at create and kept for the VM's life, across restarts and moves between machines. */
  publicIpv6?: string | null;
  resources: VmResources;
  /** URL-safe identifier, unique within the account; usable anywhere an id is. */
  slug?: string | null;
  snapshotId?: string | null;
  /** The snapshot's display name now, under the same rules. */
  sourceSnapshotDisplayName?: string | null;
  /** What that snapshot is called *now* — its own slug, unqualified. Absent when it has no slug and when it is gone, so read against `sourceSnapshotSlugAtCreate` a renamed origin tells apart from a deleted one. */
  sourceSnapshotSlug?: string | null;
  /** What that snapshot was called when the VM was created, qualified `{owner}/{slug}` when it belongs to another account. A name, not a pointer: slugs move, so boot from `snapshotId`, not from this. */
  sourceSnapshotSlugAtCreate?: string | null;
  state: VmState;
  /** Seconds this VM has spent running, across every run it has ever had. What `maxRunTotalSeconds` is measured against. */
  totalRunSeconds?: number;
  /** Delete the VM this long after it was created, whatever it is doing. */
  ttlSeconds?: number | null;
  updatedAt: string;
}
export const Vm = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    autoDeleteFromPlan: S.optional(S.Boolean),
    autoDeleteSeconds: S.optional(S.NullOr(S.Number)),
    automaticRestart: S.optional(S.NullOr(S.Boolean)),
    cpuTimeSeconds: S.optional(S.NullOr(S.Number)),
    createdAt: S.String,
    displayName: S.optional(S.NullOr(S.String)),
    egressIpv4: S.optional(S.NullOr(S.String)),
    egressIpv6: S.optional(S.NullOr(S.String)),
    id: S.String,
    idleTimeoutSeconds: S.optional(S.NullOr(S.Number)),
    image: S.optional(S.NullOr(S.String)),
    lastNetworkActivity: S.optional(S.NullOr(S.String)),
    maxRunSeconds: S.optional(S.NullOr(S.Number)),
    maxRunTotalSeconds: S.optional(S.NullOr(S.Number)),
    metadata: S.optional(VmMetadataMap),
    networks: S.optional(VmNetworksList),
    placement: S.optional(S.NullOr(VmPlacement)),
    publicIpv6: S.optional(S.NullOr(S.String)),
    resources: VmResources,
    slug: S.optional(S.NullOr(S.String)),
    snapshotId: S.optional(S.NullOr(S.String)),
    sourceSnapshotDisplayName: S.optional(S.NullOr(S.String)),
    sourceSnapshotSlug: S.optional(S.NullOr(S.String)),
    sourceSnapshotSlugAtCreate: S.optional(S.NullOr(S.String)),
    state: VmState,
    totalRunSeconds: S.optional(S.Number),
    ttlSeconds: S.optional(S.NullOr(S.Number)),
    updatedAt: S.String,
  }),
).annotate({ identifier: "Vm" }) as any as S.Schema<Vm>;

export interface DeleteVmRequest {
  /** VM id, or your slug for it */
  vmIdOrSlug: string;
}
export const DeleteVmRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    vmIdOrSlug: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "DELETE", uri: "/v5/vms/{vmIdOrSlug}", code: 200 })),
).annotate({
  identifier: "DeleteVmRequest",
}) as any as S.Schema<DeleteVmRequest>;

export interface DeleteVmResponse {}
export const DeleteVmResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "DeleteVmResponse",
}) as any as S.Schema<DeleteVmResponse>;

/** Extra environment variables. Keys must be valid POSIX names. */
export type ExecVmRequestEnvMap = { [key: string]: string | undefined };
export const ExecVmRequestEnvMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<ExecVmRequestEnvMap>;

export interface ExecVmRequest {
  /** VM id, or your slug for it */
  vmIdOrSlug: string;
  /** The command line, run through the guest's shell. */
  command: string;
  /** Extra environment variables. Keys must be valid POSIX names. */
  env?: ExecVmRequestEnvMap;
  linuxUser?: string | null;
  /** Base64-encoded stdin, up to 1 MiB decoded. */
  stdin?: string | null;
  /** Wall-clock limit, milliseconds. 1–300000; defaults to 30000 (30 s). The guest kills the command at the deadline. */
  timeoutMs?: number | null;
}
export const ExecVmRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    vmIdOrSlug: S.String.pipe(T.Label()),
    command: S.String,
    env: S.optional(ExecVmRequestEnvMap),
    linuxUser: S.optional(S.NullOr(S.String)),
    stdin: S.optional(S.NullOr(S.String)),
    timeoutMs: S.optional(S.NullOr(S.Number)),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v5/vms/{vmIdOrSlug}/exec-await",
      code: 200,
    }),
  ),
).annotate({ identifier: "ExecVmRequest" }) as any as S.Schema<ExecVmRequest>;

export interface ExecResponse {
  /** The command's exit status; absent if it was killed by the timeout. */
  statusCode?: number | null;
  stderr?: string | null;
  stdout?: string | null;
}
export const ExecResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    statusCode: S.optional(S.NullOr(S.Number)),
    stderr: S.optional(S.NullOr(S.String)),
    stdout: S.optional(S.NullOr(S.String)),
  }),
).annotate({ identifier: "ExecResponse" }) as any as S.Schema<ExecResponse>;

export interface GetVmRequest {
  /** VM id, or your slug for it */
  vmIdOrSlug: string;
}
export const GetVmRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    vmIdOrSlug: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/v5/vms/{vmIdOrSlug}", code: 200 })),
).annotate({ identifier: "GetVmRequest" }) as any as S.Schema<GetVmRequest>;

export interface ListPtySessionsRequest {
  /** VM id, or your slug for it */
  vmIdOrSlug: string;
  /** Only sessions owned by this guest Linux user; omit for all of them */
  linuxUser?: string;
}
export const ListPtySessionsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    vmIdOrSlug: S.String.pipe(T.Label()),
    linuxUser: S.optional(S.String.pipe(T.Query())),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/v5/vms/{vmIdOrSlug}/pty/sessions",
      code: 200,
    }),
  ),
).annotate({
  identifier: "ListPtySessionsRequest",
}) as any as S.Schema<ListPtySessionsRequest>;

/** One interactive terminal session. Sessions are owned by the guest agent and outlive the connection that opened them: dropping the WebSocket only detaches, so the shell keeps running and a later client can reattach. An exited session stays listable and attachable briefly, so a client can still read the final output and exit code. */
export interface PtySession {
  /** Last-known terminal width, in columns. */
  cols: number;
  /** Unix timestamp, in seconds, when the session was opened. */
  createdUnix: number;
  /** Exit code, present only once the session has exited. */
  exitCode?: number | null;
  /** Linux user the session runs as. Absent means the guest default, root. */
  linuxUser?: string | null;
  /** Last-known terminal height, in rows. */
  rows: number;
  /** Per-VM session id, for attaching or closing this session. */
  sessionId: number;
  /** The name this session was opened with, while it still answers to it. Released as soon as the shell exits, so an exited session reports none and is addressable only by `sessionId`. */
  slug?: string | null;
  /** `running` while the shell is alive, `exited` once it has terminated. */
  state: string;
}
export const PtySession = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    cols: S.Number,
    createdUnix: S.Number,
    exitCode: S.optional(S.NullOr(S.Number)),
    linuxUser: S.optional(S.NullOr(S.String)),
    rows: S.Number,
    sessionId: S.Number,
    slug: S.optional(S.NullOr(S.String)),
    state: S.String,
  }),
).annotate({ identifier: "PtySession" }) as any as S.Schema<PtySession>;

export type ListPtySessionsResponseSessionsList = Array<PtySession>;
export const ListPtySessionsResponseSessionsList = /*@__PURE__*/ S.Array(
  PtySession,
) as any as S.Schema<ListPtySessionsResponseSessionsList>;

export interface ListPtySessionsResponse {
  sessions: ListPtySessionsResponseSessionsList;
}
export const ListPtySessionsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sessions: ListPtySessionsResponseSessionsList,
  }),
).annotate({
  identifier: "ListPtySessionsResponse",
}) as any as S.Schema<ListPtySessionsResponse>;

export interface ListVmsRequest {
  /** Only VMs in this state. */
  state?: VmState | (string & {});
  /** Exact slug match. */
  slug?: string;
  search?: string;
  /** Only VMs booted from this snapshot. */
  snapshotId?: string;
  /** Only VMs attached to this network (id, or your slug for it). */
  vpc?: string;
  /** Comma-separated `key:value` pairs, e.g. `env:prod,region:us-west`. */
  metadata?: string;
  limit?: number;
  offset?: number;
}
export const ListVmsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    state: S.optional(VmState.pipe(T.Query())),
    slug: S.optional(S.String.pipe(T.Query())),
    search: S.optional(S.String.pipe(T.Query())),
    snapshotId: S.optional(S.String.pipe(T.Query())),
    vpc: S.optional(S.String.pipe(T.Query())),
    metadata: S.optional(S.String.pipe(T.Query())),
    limit: S.optional(S.Number.pipe(T.Query())),
    offset: S.optional(S.Number.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/v5/vms", code: 200 })),
).annotate({ identifier: "ListVmsRequest" }) as any as S.Schema<ListVmsRequest>;

export type ListVmsResponseVmsList = Array<Vm>;
export const ListVmsResponseVmsList = /*@__PURE__*/ S.Array(
  Vm,
) as any as S.Schema<ListVmsResponseVmsList>;

export interface ListVmsResponse {
  pausedCount: number;
  pausingCount?: number;
  runningCount: number;
  startingCount: number;
  stoppedCount: number;
  totalCount: number;
  vms: ListVmsResponseVmsList;
}
export const ListVmsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    pausedCount: S.Number,
    pausingCount: S.optional(S.Number),
    runningCount: S.Number,
    startingCount: S.Number,
    stoppedCount: S.Number,
    totalCount: S.Number,
    vms: ListVmsResponseVmsList,
  }),
).annotate({
  identifier: "ListVmsResponse",
}) as any as S.Schema<ListVmsResponse>;

export interface OpenPtyRequest {
  /** VM id, or your slug for it */
  vmIdOrSlug: string;
  /** Command to run; omit for a login shell */
  exec?: string;
  /** Initial width in columns (default 80) */
  cols?: number;
  /** Initial height in rows (default 24) */
  rows?: number;
  /** Guest Linux user to run as; omit for the guest's default user (uid 1000, else root) */
  linuxUser?: string;
  /** Name this session so you can reattach to it later without storing the id we mint. If a session already has this name you get that one back — alive, or exited with its final output and exit code still readable — and `exec` is NOT run; the `sessionInfo` frame's `created` says which happened. Starting over is DELETE then open again. Must not be all digits: session ids are bare integers, so an all-digit name could not be told apart from one. */
  slug?: string;
  /** Respawn the shell in place when it exits, keeping this session's id, name and scrollback — for a terminal that should outlive whatever runs in it. A command that dies at startup is not respawned forever; after a few exits in quick succession the session is left dead so the error is readable. Leave this off if you are waiting on a command's exit code: a session that comes back to life reports `running` again. */
  replaceOnExit?: boolean;
}
export const OpenPtyRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    vmIdOrSlug: S.String.pipe(T.Label()),
    exec: S.optional(S.String.pipe(T.Query())),
    cols: S.optional(S.Number.pipe(T.Query())),
    rows: S.optional(S.Number.pipe(T.Query())),
    linuxUser: S.optional(S.String.pipe(T.Query())),
    slug: S.optional(S.String.pipe(T.Query())),
    replaceOnExit: S.optional(S.Boolean.pipe(T.Query())),
  }).pipe(
    T.Http({ method: "GET", uri: "/v5/vms/{vmIdOrSlug}/pty", code: 200 }),
  ),
).annotate({ identifier: "OpenPtyRequest" }) as any as S.Schema<OpenPtyRequest>;

export interface OpenPtyResponse {}
export const OpenPtyResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "OpenPtyResponse",
}) as any as S.Schema<OpenPtyResponse>;

export interface PauseVmRequest {
  /** VM id, or your slug for it */
  vmIdOrSlug: string;
}
export const PauseVmRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    vmIdOrSlug: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({ method: "POST", uri: "/v5/vms/{vmIdOrSlug}/pause", code: 200 }),
  ),
).annotate({ identifier: "PauseVmRequest" }) as any as S.Schema<PauseVmRequest>;

export interface ResizeVmRequest {
  /** VM id, or your slug for it */
  vmIdOrSlug: string;
  cpu?: number | null;
  /** Memory, MiB. */
  memory?: number | null;
  /** Disk, MiB. Disks can only grow. */
  storage?: number | null;
}
export const ResizeVmRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    vmIdOrSlug: S.String.pipe(T.Label()),
    cpu: S.optional(S.NullOr(S.Number)),
    memory: S.optional(S.NullOr(S.Number)),
    storage: S.optional(S.NullOr(S.Number)),
  }).pipe(
    T.Http({ method: "POST", uri: "/v5/vms/{vmIdOrSlug}/resize", code: 200 }),
  ),
).annotate({
  identifier: "ResizeVmRequest",
}) as any as S.Schema<ResizeVmRequest>;

export interface StartVmRequest {
  /** VM id, or your slug for it */
  vmIdOrSlug: string;
}
export const StartVmRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    vmIdOrSlug: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({ method: "POST", uri: "/v5/vms/{vmIdOrSlug}/start", code: 200 }),
  ),
).annotate({ identifier: "StartVmRequest" }) as any as S.Schema<StartVmRequest>;

/** Metadata merged into the VM. Only the keys you send are touched; send an empty value to remove one. */
export type UpdateVmRequestMetadataMap = { [key: string]: string | undefined };
export const UpdateVmRequestMetadataMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<UpdateVmRequestMetadataMap>;

export interface UpdateVmRequest {
  /** VM id, or your slug for it */
  vmIdOrSlug: string;
  /** Seconds of not running before the VM is deleted; -1 removes the window (on a plan that caps it, -1 puts you back on the cap rather than off it), and 0 makes the VM ephemeral — deleted the moment it stops, or right away if it is already stopped. */
  autoDeleteSeconds?: number | null;
  /** Turn automatic restart on or off. Applies to the next failure: this neither boots a stopped VM nor stops a running one. */
  automaticRestart?: boolean | null;
  /** Change the display label; an empty string clears it. */
  displayName?: string | null;
  /** Seconds of network idleness before pause; -1 removes the timeout. */
  idleTimeoutSeconds?: number | null;
  /** Seconds one run may last before the VM is paused; -1 removes the cap. */
  maxRunSeconds?: number | null;
  /** The lifetime runtime budget in seconds; -1 removes it. Raising this past `totalRunSeconds` is how you start a VM that spent its budget. */
  maxRunTotalSeconds?: number | null;
  /** Metadata merged into the VM. Only the keys you send are touched; send an empty value to remove one. */
  metadata?: UpdateVmRequestMetadataMap | null;
  /** Take `slug` even if another VM in this account already holds it. The slug moves atomically: the current holder keeps running, stays addressable by id, and just loses its slug. Without this flag a taken slug is a 409 CONFLICT. Requires a non-empty `slug`. */
  reassignSlug?: boolean;
  /** Change the slug; an empty string clears it. */
  slug?: string | null;
  /** Seconds from creation — not from now — before the VM is deleted; -1 removes the deadline. A value already in the past deletes it shortly. */
  ttlSeconds?: number | null;
}
export const UpdateVmRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    vmIdOrSlug: S.String.pipe(T.Label()),
    autoDeleteSeconds: S.optional(S.NullOr(S.Number)),
    automaticRestart: S.optional(S.NullOr(S.Boolean)),
    displayName: S.optional(S.NullOr(S.String)),
    idleTimeoutSeconds: S.optional(S.NullOr(S.Number)),
    maxRunSeconds: S.optional(S.NullOr(S.Number)),
    maxRunTotalSeconds: S.optional(S.NullOr(S.Number)),
    metadata: S.optional(S.NullOr(UpdateVmRequestMetadataMap)),
    reassignSlug: S.optional(S.Boolean),
    slug: S.optional(S.NullOr(S.String)),
    ttlSeconds: S.optional(S.NullOr(S.Number)),
  }).pipe(T.Http({ method: "PATCH", uri: "/v5/vms/{vmIdOrSlug}", code: 200 })),
).annotate({
  identifier: "UpdateVmRequest",
}) as any as S.Schema<UpdateVmRequest>;

/** At most one network is accepted today. An empty list detaches the VM. Also accepted as `vpcs`, the name the rest of the API uses. */
export type UpdateVmNetworksRequestNetworksList = Array<AttachNetwork>;
export const UpdateVmNetworksRequestNetworksList = /*@__PURE__*/ S.Array(
  AttachNetwork,
) as any as S.Schema<UpdateVmNetworksRequestNetworksList>;

export interface UpdateVmNetworksRequest {
  /** VM id, or your slug for it */
  vmIdOrSlug: string;
  /** At most one network is accepted today. An empty list detaches the VM. Also accepted as `vpcs`, the name the rest of the API uses. */
  networks?: UpdateVmNetworksRequestNetworksList;
}
export const UpdateVmNetworksRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    vmIdOrSlug: S.String.pipe(T.Label()),
    networks: S.optional(UpdateVmNetworksRequestNetworksList),
  }).pipe(
    T.Http({ method: "PUT", uri: "/v5/vms/{vmIdOrSlug}/networks", code: 200 }),
  ),
).annotate({
  identifier: "UpdateVmNetworksRequest",
}) as any as S.Schema<UpdateVmNetworksRequest>;

export type AttachPtyError = BadRequest | NotFound | FreestyleOpError;
/** Reattach to an existing terminal session over a WebSocket, addressed by id or by the slug it was opened with. The retained scrollback replays first, then live output follows. */
export const attachPty: API.OperationMethod<
  AttachPtyRequest,
  AttachPtyResponse,
  AttachPtyError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AttachPtyRequest,
  output: AttachPtyResponse,
  errors: [BadRequest, NotFound, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));

export type ClosePtySessionError = NotFound | FreestyleOpError;
/** Kill a terminal session and remove it, addressed by id or by the slug it was opened with. */
export const closePtySession: API.OperationMethod<
  ClosePtySessionRequest,
  ClosePtySessionResponse,
  ClosePtySessionError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ClosePtySessionRequest,
  output: ClosePtySessionResponse,
  errors: [NotFound, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));

export type CreateVmError = BadRequest | Conflict | FreestyleOpError;
/** Boot a new VM. */
export const createVm: API.OperationMethod<
  CreateVmRequest,
  Vm,
  CreateVmError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateVmRequest,
  output: Vm,
  errors: [BadRequest, Conflict, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));

export type DeleteVmError = NotFound | FreestyleOpError;
/** Permanently destroy the VM. */
export const deleteVm: API.OperationMethod<
  DeleteVmRequest,
  DeleteVmResponse,
  DeleteVmError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteVmRequest,
  output: DeleteVmResponse,
  errors: [NotFound, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));

export type ExecVmError =
  | BadRequest
  | Forbidden
  | NotFound
  | Conflict
  | FreestyleOpError;
/** Run a command in the guest and wait for it to finish. A non-zero exit status is still a 200. Read `statusCode`, which is null if the command was killed by its timeout. */
export const execVm: API.OperationMethod<
  ExecVmRequest,
  ExecResponse,
  ExecVmError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ExecVmRequest,
  output: ExecResponse,
  errors: [BadRequest, Forbidden, NotFound, Conflict, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));

export type GetVmError = NotFound | FreestyleOpError;
/** Fetch a VM by its id or its slug. */
export const getVm: API.OperationMethod<
  GetVmRequest,
  Vm,
  GetVmError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetVmRequest,
  output: Vm,
  errors: [NotFound, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));

export type ListPtySessionsError = NotFound | FreestyleOpError;
/** List the VM's terminal sessions, running and recently exited. Scoped to one Linux user when the request names one, otherwise every session on the VM. */
export const listPtySessions: API.OperationMethod<
  ListPtySessionsRequest,
  ListPtySessionsResponse,
  ListPtySessionsError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListPtySessionsRequest,
  output: ListPtySessionsResponse,
  errors: [NotFound, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));

export type ListVmsError = NotFound | FreestyleOpError;
/** List your VMs. */
export const listVms: API.PaginatedOperationMethod<
  ListVmsRequest,
  ListVmsResponse,
  ListVmsError,
  FreestyleOpContext,
  Vm
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListVmsRequest,
    output: ListVmsResponse,
    errors: [NotFound, UnknownFreestyleError],
    protocol: FreestyleProtocol,
    retry: Retry.Retry,
    pagination: {
      inputToken: "offset",
      outputToken: "totalCount",
      items: "vms",
      pageSize: "limit",
    } as const,
  }),
  paginateOffset,
) as any;

export type OpenPtyError = BadRequest | NotFound | Conflict | FreestyleOpError;
/** Open an interactive terminal on the VM over a WebSocket. The session outlives this connection: closing the socket only detaches, so it can be reattached or listed later. It also outlives the host that serves it — a host restart reattaches this socket to the same session and replays its scrollback behind a terminal reset, with no break in the stream. */
export const openPty: API.OperationMethod<
  OpenPtyRequest,
  OpenPtyResponse,
  OpenPtyError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: OpenPtyRequest,
  output: OpenPtyResponse,
  errors: [BadRequest, NotFound, Conflict, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));

export type PauseVmError = NotFound | Conflict | FreestyleOpError;
/** Freeze a running VM, keeping its memory so a later start resumes it exactly. */
export const pauseVm: API.OperationMethod<
  PauseVmRequest,
  Vm,
  PauseVmError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: PauseVmRequest,
  output: Vm,
  errors: [NotFound, Conflict, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));

export type ResizeVmError = BadRequest | NotFound | Conflict | FreestyleOpError;
/** Change a VM's vCPU, memory, or disk. Resizing is grow-only on every axis: each can go up but never down. Omitted fields keep their current value. vCPU and memory apply live to a running VM, apply on resume for a paused one, and apply at the next boot for a stopped one. Growing the disk needs a running VM, since it grows a live block device and then its filesystem. */
export const resizeVm: API.OperationMethod<
  ResizeVmRequest,
  Vm,
  ResizeVmError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ResizeVmRequest,
  output: Vm,
  errors: [BadRequest, NotFound, Conflict, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));

export type StartVmError = NotFound | Conflict | FreestyleOpError;
/** Boot a stopped VM, or resume a paused one. */
export const startVm: API.OperationMethod<
  StartVmRequest,
  Vm,
  StartVmError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: StartVmRequest,
  output: Vm,
  errors: [NotFound, Conflict, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));

export type UpdateVmError = BadRequest | NotFound | Conflict | FreestyleOpError;
/** Rename a VM, change its slug or idle timeout, or merge in metadata. */
export const updateVm: API.OperationMethod<
  UpdateVmRequest,
  Vm,
  UpdateVmError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateVmRequest,
  output: Vm,
  errors: [BadRequest, NotFound, Conflict, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));

export type UpdateVmNetworksError =
  | BadRequest
  | NotFound
  | Conflict
  | FreestyleOpError;
/** Set the VM's private networks — attach, update, or detach in one declarative call (an empty list detaches). Works while the VM is stopped, running, or paused: a stopped VM picks the change up on its next start, a running VM is reconfigured live, and a paused VM applies it on resume. At most one network is accepted today. The VM keeps its address while it stays in the same network; moving networks (or naming a different address) renumbers it. */
export const updateVmNetworks: API.OperationMethod<
  UpdateVmNetworksRequest,
  Vm,
  UpdateVmNetworksError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateVmNetworksRequest,
  output: Vm,
  errors: [BadRequest, NotFound, Conflict, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));
