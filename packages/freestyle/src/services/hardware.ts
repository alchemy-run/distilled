// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/spec-mirror-freestyle/specs/openapi.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  FreestyleProtocol,
  type FreestyleOpError,
  type FreestyleOpContext,
} from "../protocol.ts";
import { UnknownFreestyleError } from "../errors.ts";
import * as Retry from "../retry.ts";

export type { FreestyleOpError, FreestyleOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export interface GetHardwarePowerRequest {
  /** Hardware machine id */
  hardwareId: string;
}
export const GetHardwarePowerRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    hardwareId: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/v5/hardware/{hardwareId}/power",
      code: 200,
    }),
  ),
).annotate({
  identifier: "GetHardwarePowerRequest",
}) as any as S.Schema<GetHardwarePowerRequest>;

/** A machine's power, and what is left of its rate limit. */
export interface HardwarePowerState {
  /** How many power cords feed it — why control is all-or-nothing. */
  cords?: number | null;
  lastActionAt?: string | null;
  /** Why the power state is unknown, when it is. */
  message?: string | null;
  /** When the next power-cutting action will be accepted. Absent means now. */
  nextAllowedAt?: string | null;
  /** Every cord is live. Absent when the power state could not be read — which is not the same as the machine being off. */
  on?: boolean | null;
  /** Power-cutting actions left in the rolling 24-hour budget. Turning power **on** does not spend it. */
  remainingToday: number;
  /** False when power control is not enabled for this machine; every other field is then absent and every action is refused. */
  supported: boolean;
  /** Present draw across the machine's cords, in watts. */
  watts?: number | null;
}
export const HardwarePowerState = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    cords: S.optional(S.NullOr(S.Number)),
    lastActionAt: S.optional(S.NullOr(S.String)),
    message: S.optional(S.NullOr(S.String)),
    nextAllowedAt: S.optional(S.NullOr(S.String)),
    on: S.optional(S.NullOr(S.Boolean)),
    remainingToday: S.Number,
    supported: S.Boolean,
    watts: S.optional(S.NullOr(S.Number)),
  }),
).annotate({
  identifier: "HardwarePowerState",
}) as any as S.Schema<HardwarePowerState>;

export interface ListHardwareRequest {}
export const ListHardwareRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}).pipe(T.Http({ method: "GET", uri: "/v5/hardware", code: 200 })),
).annotate({
  identifier: "ListHardwareRequest",
}) as any as S.Schema<ListHardwareRequest>;

/** A physical machine attached to one of your private networks. Freestyle racks and cables these, so there is no create or delete here — what the API offers is seeing them and, where it is enabled, controlling their power. */
export interface HardwareServer {
  /** The network attachment is converged and the machine is being served. */
  attached: boolean;
  createdAt: string;
  /** The machine has taken a DHCP lease — the closest thing to "it is on" that the network can see by itself. */
  dhcpBound: boolean;
  displayName?: string | null;
  id: string;
  /** Its address on the private network, when that network carries IPv4. */
  ipv4?: string | null;
  /** Its address on the private network. Every network has IPv6. */
  ipv6: string;
  /** When the network last saw traffic from it. */
  lastSeen?: string | null;
  /** The machine's MAC, once it is known. */
  mac?: string | null;
  /** Whether this machine's power can be switched through the API. Off unless Freestyle has enabled it for the machine. */
  powerControl?: boolean;
  /** Its stable public address, when it was given one. */
  publicIpv6?: string | null;
  /** The private network the machine is attached to. */
  vpcId: string;
}
export const HardwareServer = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    attached: S.Boolean,
    createdAt: S.String,
    dhcpBound: S.Boolean,
    displayName: S.optional(S.NullOr(S.String)),
    id: S.String,
    ipv4: S.optional(S.NullOr(S.String)),
    ipv6: S.String,
    lastSeen: S.optional(S.NullOr(S.String)),
    mac: S.optional(S.NullOr(S.String)),
    powerControl: S.optional(S.Boolean),
    publicIpv6: S.optional(S.NullOr(S.String)),
    vpcId: S.String,
  }),
).annotate({ identifier: "HardwareServer" }) as any as S.Schema<HardwareServer>;

export type ListHardwareResponseHardwareServersList = Array<HardwareServer>;
export const ListHardwareResponseHardwareServersList = /*@__PURE__*/ S.Array(
  HardwareServer,
) as any as S.Schema<ListHardwareResponseHardwareServersList>;

export interface ListHardwareResponse {
  hardwareServers: ListHardwareResponseHardwareServersList;
  totalCount: number;
}
export const ListHardwareResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    hardwareServers: ListHardwareResponseHardwareServersList,
    totalCount: S.Number,
  }),
).annotate({
  identifier: "ListHardwareResponse",
}) as any as S.Schema<ListHardwareResponse>;

/** What to do to a machine's power. The unit is always the whole machine. A machine with redundant supplies draws from several cords, and Freestyle switches all of them together — there is no way to ask for one, because cutting one cord browns a machine out rather than powering it off. */
export type HardwarePowerAction = "cycle" | "on" | "off";
export const HardwarePowerAction = S.String;

export interface SetHardwarePowerRequest {
  /** Hardware machine id */
  hardwareId: string;
  action: HardwarePowerAction | (string & {});
}
export const SetHardwarePowerRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    hardwareId: S.String.pipe(T.Label()),
    action: HardwarePowerAction,
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v5/hardware/{hardwareId}/power",
      code: 200,
    }),
  ),
).annotate({
  identifier: "SetHardwarePowerRequest",
}) as any as S.Schema<SetHardwarePowerRequest>;

export interface HardwarePowerOutcome {
  action: HardwarePowerAction;
  applied: boolean;
  nextAllowedAt?: string | null;
  remainingToday: number;
}
export const HardwarePowerOutcome = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    action: HardwarePowerAction,
    applied: S.Boolean,
    nextAllowedAt: S.optional(S.NullOr(S.String)),
    remainingToday: S.Number,
  }),
).annotate({
  identifier: "HardwarePowerOutcome",
}) as any as S.Schema<HardwarePowerOutcome>;

export type GetHardwarePowerError = NotFound | FreestyleOpError;
/** Read a machine's power: whether every cord is live, what it is drawing, and how much of its rate limit is left. Reading is free and never limited. */
export const getHardwarePower: API.OperationMethod<
  GetHardwarePowerRequest,
  HardwarePowerState,
  GetHardwarePowerError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetHardwarePowerRequest,
  output: HardwarePowerState,
  errors: [NotFound, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));

export type ListHardwareError = FreestyleOpError;
/** List the physical machines attached to your private networks. */
export const listHardware: API.OperationMethod<
  ListHardwareRequest,
  ListHardwareResponse,
  ListHardwareError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListHardwareRequest,
  output: ListHardwareResponse,
  errors: [UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));

export type SetHardwarePowerError = BadRequest | NotFound | FreestyleOpError;
/** Switch a physical machine's power. This is a real power outlet, so it is deliberately narrow. The unit is the whole machine: machines with redundant supplies draw from several cords and all of them are switched together, because cutting one browns the machine out instead of powering it off. `cycle` cuts power, holds a few seconds, and restores it — anything running stops immediately, with no chance to shut down cleanly. Heavily rate limited: one power-cutting action every few minutes and a small daily budget per machine, both reported by `GET /hardware/{hardwareId}/power`. Turning power back **on** is never rationed, only briefly debounced. Exceeding a limit is a 429 whose message says when to come back. */
export const setHardwarePower: API.OperationMethod<
  SetHardwarePowerRequest,
  HardwarePowerOutcome,
  SetHardwarePowerError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: SetHardwarePowerRequest,
  output: HardwarePowerOutcome,
  errors: [BadRequest, NotFound, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));
