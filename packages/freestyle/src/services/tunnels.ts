// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/spec-mirror-freestyle/specs/openapi.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  FreestyleProtocol,
  type FreestyleOpError,
  type FreestyleOpContext,
} from "../protocol.ts";
import { UnknownFreestyleError } from "../errors.ts";
import * as Retry from "../retry.ts";

export type { FreestyleOpError, FreestyleOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Conflict
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Conflict>()("Conflict", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withConflictError),
    [{ status: 409 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

/** Ranges behind your client that this network routes through the tunnel — a site-to-site setup where your client machine forwards for other hosts on its side. Hosts inside these ranges keep their real addresses inside the network. A range carved out of the network's own CIDR just works: VMs reach it with no configuration, and the range is excluded from VM address allocation (it must be free when granted). A range outside the network's CIDRs is reached by giving VMs a route via the attachment's `address`. A range may not straddle the network's CIDR boundary, nor overlap a range another tunnel already grants this network. */
export type AttachVpcToTunnelRequestRemoteCidrsList = Array<string>;
export const AttachVpcToTunnelRequestRemoteCidrsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<AttachVpcToTunnelRequestRemoteCidrsList>;

export interface AttachVpcToTunnelRequest {
  /** Tunnel id, or your slug for it */
  tunnelIdOrSlug: string;
  /** VPC id, or your slug for it */
  vpcIdOrSlug: string;
  /** Make your client this network's exit — a router members can name as a next hop. A VM routes through it with an ordinary NIC route via the attachment's address, and everything it sends the exit — whatever the destination — is forwarded to your client, which is expected to NAT it onward. Several attachments per network may be exits: members choose per route, so two exits and two same-metric routes are a redundant pair, and a dead exit stops being ARP-answered so the other takes over by itself. */
  exit?: boolean;
  /** The address you want inside the private network. Omit to be assigned one. It must be free: a VM already holding it makes this a conflict, and once yours, no VM can be given it. */
  ipv4?: string | null;
  /** An explicit IPv6 address, equivalent to passing one in `ipv4`; only one may be set. */
  ipv6?: string | null;
  /** Ranges behind your client that this network routes through the tunnel — a site-to-site setup where your client machine forwards for other hosts on its side. Hosts inside these ranges keep their real addresses inside the network. A range carved out of the network's own CIDR just works: VMs reach it with no configuration, and the range is excluded from VM address allocation (it must be free when granted). A range outside the network's CIDRs is reached by giving VMs a route via the attachment's `address`. A range may not straddle the network's CIDR boundary, nor overlap a range another tunnel already grants this network. */
  remoteCidrs?: AttachVpcToTunnelRequestRemoteCidrsList;
}
export const AttachVpcToTunnelRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    tunnelIdOrSlug: S.String.pipe(T.Label()),
    vpcIdOrSlug: S.String.pipe(T.Label()),
    exit: S.optional(S.Boolean),
    ipv4: S.optional(S.NullOr(S.String)),
    ipv6: S.optional(S.NullOr(S.String)),
    remoteCidrs: S.optional(AttachVpcToTunnelRequestRemoteCidrsList),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v5/tunnels/{tunnelIdOrSlug}/vpcs/{vpcIdOrSlug}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "AttachVpcToTunnelRequest",
}) as any as S.Schema<AttachVpcToTunnelRequest>;

/** The CIDRs the gateway routes to this network. */
export type TunnelAttachmentAllowedIpsList = Array<string>;
export const TunnelAttachmentAllowedIpsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<TunnelAttachmentAllowedIpsList>;

/** Ranges behind your client that this network routes through the tunnel (a site-to-site grant). VMs reach them with an ordinary route via `address`. Omitted when the attachment has none. */
export type TunnelAttachmentRemoteCidrsList = Array<string>;
export const TunnelAttachmentRemoteCidrsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<TunnelAttachmentRemoteCidrsList>;

/** One private network on the far side of a tunnel: the network, your address inside it, and the `[Peer]` key that carries it. */
export interface TunnelAttachment {
  /** Deprecated: the primary address, whichever family. Read `ipv4`/`ipv6`. */
  address: string;
  /** The CIDRs the gateway routes to this network. */
  allowedIps: TunnelAttachmentAllowedIpsList;
  createdAt: string;
  /** Whether this attachment is a network exit: your client forwards for destinations that are not its own address, and members route through it with an ordinary route via the attachment's address. Omitted when false. */
  exit?: boolean;
  /** Your IPv4 address inside the private network, absent on a v6-only attachment. It belongs to this tunnel for as long as the network stays attached, so you can write it into configs and rules. */
  ipv4?: string | null;
  /** Your IPv6 address inside the private network, under the same rules. */
  ipv6?: string | null;
  /** Ranges behind your client that this network routes through the tunnel (a site-to-site grant). VMs reach them with an ordinary route via `address`. Omitted when the attachment has none. */
  remoteCidrs?: TunnelAttachmentRemoteCidrsList;
  /** Deprecated: the second-family address. Read `ipv4`/`ipv6`. */
  secondaryAddress?: string | null;
  vpcCidr: string;
  vpcId: string;
}
export const TunnelAttachment = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    address: S.String,
    allowedIps: TunnelAttachmentAllowedIpsList,
    createdAt: S.String,
    exit: S.optional(S.Boolean),
    ipv4: S.optional(S.NullOr(S.String)),
    ipv6: S.optional(S.NullOr(S.String)),
    remoteCidrs: S.optional(TunnelAttachmentRemoteCidrsList),
    secondaryAddress: S.optional(S.NullOr(S.String)),
    vpcCidr: S.String,
    vpcId: S.String,
  }),
).annotate({
  identifier: "TunnelAttachment",
}) as any as S.Schema<TunnelAttachment>;

/** The private networks on the far side, oldest attachment first. */
export type TunnelAttachmentsList = Array<TunnelAttachment>;
export const TunnelAttachmentsList = /*@__PURE__*/ S.Array(
  TunnelAttachment,
) as any as S.Schema<TunnelAttachmentsList>;

/** The ranges your client routes through the tunnel — its `AllowedIPs`, fixed at create. A network can only be attached if its CIDRs fall inside these. */
export type TunnelRoutesList = Array<string>;
export const TunnelRoutesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<TunnelRoutesList>;

/** A tunnel: your WireGuard identity on the platform. It exists until you delete it: connect, disconnect and reconnect as often as you like — a session is set up on the handshake that needs it, so an idle tunnel costs nothing and never expires. A tunnel routes nowhere until you attach a private network to it; attach several and one WireGuard interface reaches them all. */
export interface Tunnel {
  /** The private networks on the far side, oldest attachment first. */
  attachments: TunnelAttachmentsList;
  /** Your fixed addresses inside the tunnel. Only the gateway ever sees them: each attached network sees an address inside its own subnet instead (`attachments[].address`). */
  clientAddressV4: string;
  clientAddressV6: string;
  /** A complete WireGuard config file, fixed for the life of the tunnel: attaching and detaching networks never changes it, so bring it up once and manage what it reaches through the API. `PrivateKey` is blank except on create and rotate-key, the only two responses that mint a keypair. */
  clientConfig: string;
  clientPublicKey: string;
  createdAt: string;
  /** Your label for this tunnel, if you gave it one. */
  displayName?: string | null;
  /** Host to dial. */
  endpointHost?: string | null;
  endpointPort: number;
  /** Deprecated: the tunnel's id under its old name. Read `tunnelId`. */
  id: string;
  /** The ranges your client routes through the tunnel — its `AllowedIPs`, fixed at create. A network can only be attached if its CIDRs fall inside these. */
  routes: TunnelRoutesList;
  /** The one `[Peer]` public key in your config. */
  serverPublicKey: string;
  /** Your handle for this tunnel, if you gave it one. Usable anywhere the id is, in place of it. */
  slug?: string | null;
  /** The tunnel's id, under the name the rest of the API uses for it — a firewall rule says `tunnelId`, so the tunnel does too. */
  tunnelId?: string | null;
  updatedAt: string;
}
export const Tunnel = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    attachments: TunnelAttachmentsList,
    clientAddressV4: S.String,
    clientAddressV6: S.String,
    clientConfig: S.String,
    clientPublicKey: S.String,
    createdAt: S.String,
    displayName: S.optional(S.NullOr(S.String)),
    endpointHost: S.optional(S.NullOr(S.String)),
    endpointPort: S.Number,
    id: S.String,
    routes: TunnelRoutesList,
    serverPublicKey: S.String,
    slug: S.optional(S.NullOr(S.String)),
    tunnelId: S.optional(S.NullOr(S.String)),
    updatedAt: S.String,
  }),
).annotate({ identifier: "Tunnel" }) as any as S.Schema<Tunnel>;

/** The ranges your client will route through the tunnel — its `AllowedIPs`, fixed for the tunnel's life so attaching and detaching networks never changes your config. Omit for the default (`10.0.0.0/8` and `fd00::/8`), which covers every network with default addressing. Only networks whose CIDRs fall inside the routes can be attached. */
export type CreateTunnelRequestRoutesList = Array<string>;
export const CreateTunnelRequestRoutesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<CreateTunnelRequestRoutesList>;

/** Ranges behind your client this network may route to; see the attach request's `remoteCidrs`. */
export type CreateTunnelVpcRemoteCidrsList = Array<string>;
export const CreateTunnelVpcRemoteCidrsList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<CreateTunnelVpcRemoteCidrsList>;

/** One network named at create, with the same address choices as an attach. */
export interface CreateTunnelVpc {
  /** See the attach request's `exit`. */
  exit?: boolean;
  /** The address you want inside it; omit to be assigned one. */
  ipv4?: string | null;
  /** An explicit IPv6 address instead; only one may be set. */
  ipv6?: string | null;
  /** Ranges behind your client this network may route to; see the attach request's `remoteCidrs`. */
  remoteCidrs?: CreateTunnelVpcRemoteCidrsList;
  /** The network to attach: a VPC id, or your slug for it. Also accepted as `vpcId`. */
  vpc: string;
}
export const CreateTunnelVpc = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    exit: S.optional(S.Boolean),
    ipv4: S.optional(S.NullOr(S.String)),
    ipv6: S.optional(S.NullOr(S.String)),
    remoteCidrs: S.optional(CreateTunnelVpcRemoteCidrsList),
    vpc: S.String,
  }),
).annotate({
  identifier: "CreateTunnelVpc",
}) as any as S.Schema<CreateTunnelVpc>;

/** Networks to attach in the same call, so one request yields a tunnel that already routes somewhere. All-or-nothing: if any attachment is refused, nothing is created. */
export type CreateTunnelRequestVpcsList = Array<CreateTunnelVpc>;
export const CreateTunnelRequestVpcsList = /*@__PURE__*/ S.Array(
  CreateTunnelVpc,
) as any as S.Schema<CreateTunnelRequestVpcsList>;

export interface CreateTunnelRequest {
  /** The public key of a keypair you already hold. Omit to have one minted for you; supply it and the platform never sees a private key at all. */
  clientPublicKey?: string | null;
  /** A free-form label, for telling several tunnels apart. */
  displayName?: string | null;
  /** The ranges your client will route through the tunnel — its `AllowedIPs`, fixed for the tunnel's life so attaching and detaching networks never changes your config. Omit for the default (`10.0.0.0/8` and `fd00::/8`), which covers every network with default addressing. Only networks whose CIDRs fall inside the routes can be attached. */
  routes?: CreateTunnelRequestRoutesList | null;
  /** A URL-safe handle, unique within your account: 1–63 chars of `[a-z0-9-]`, no leading, trailing, or repeated hyphens. Address the tunnel by it instead of by id. */
  slug?: string | null;
  /** Networks to attach in the same call, so one request yields a tunnel that already routes somewhere. All-or-nothing: if any attachment is refused, nothing is created. */
  vpcs?: CreateTunnelRequestVpcsList;
}
export const CreateTunnelRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    clientPublicKey: S.optional(S.NullOr(S.String)),
    displayName: S.optional(S.NullOr(S.String)),
    routes: S.optional(S.NullOr(CreateTunnelRequestRoutesList)),
    slug: S.optional(S.NullOr(S.String)),
    vpcs: S.optional(CreateTunnelRequestVpcsList),
  }).pipe(T.Http({ method: "POST", uri: "/v5/tunnels", code: 200 })),
).annotate({
  identifier: "CreateTunnelRequest",
}) as any as S.Schema<CreateTunnelRequest>;

/** The private networks on the far side, oldest attachment first. */
export type CreateTunnelResponseAttachmentsList = Array<TunnelAttachment>;
export const CreateTunnelResponseAttachmentsList = /*@__PURE__*/ S.Array(
  TunnelAttachment,
) as any as S.Schema<CreateTunnelResponseAttachmentsList>;

/** The ranges your client routes through the tunnel — its `AllowedIPs`, fixed at create. A network can only be attached if its CIDRs fall inside these. */
export type CreateTunnelResponseRoutesList = Array<string>;
export const CreateTunnelResponseRoutesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<CreateTunnelResponseRoutesList>;

export interface CreateTunnelResponse {
  /** The private networks on the far side, oldest attachment first. */
  attachments: CreateTunnelResponseAttachmentsList;
  /** Your fixed addresses inside the tunnel. Only the gateway ever sees them: each attached network sees an address inside its own subnet instead (`attachments[].address`). */
  clientAddressV4: string;
  clientAddressV6: string;
  /** A complete WireGuard config file, fixed for the life of the tunnel: attaching and detaching networks never changes it, so bring it up once and manage what it reaches through the API. `PrivateKey` is blank except on create and rotate-key, the only two responses that mint a keypair. */
  clientConfig: string;
  clientPublicKey: string;
  createdAt: string;
  /** Your label for this tunnel, if you gave it one. */
  displayName?: string | null;
  /** Host to dial. */
  endpointHost?: string | null;
  endpointPort: number;
  /** Deprecated: the tunnel's id under its old name. Read `tunnelId`. */
  id: string;
  /** The ranges your client routes through the tunnel — its `AllowedIPs`, fixed at create. A network can only be attached if its CIDRs fall inside these. */
  routes: CreateTunnelResponseRoutesList;
  /** The one `[Peer]` public key in your config. */
  serverPublicKey: string;
  /** Your handle for this tunnel, if you gave it one. Usable anywhere the id is, in place of it. */
  slug?: string | null;
  /** The tunnel's id, under the name the rest of the API uses for it — a firewall rule says `tunnelId`, so the tunnel does too. */
  tunnelId?: string | null;
  updatedAt: string;
  /** Returned once. Already embedded in `clientConfig`. */
  clientPrivateKey: string;
}
export const CreateTunnelResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    attachments: CreateTunnelResponseAttachmentsList,
    clientAddressV4: S.String,
    clientAddressV6: S.String,
    clientConfig: S.String,
    clientPublicKey: S.String,
    createdAt: S.String,
    displayName: S.optional(S.NullOr(S.String)),
    endpointHost: S.optional(S.NullOr(S.String)),
    endpointPort: S.Number,
    id: S.String,
    routes: CreateTunnelResponseRoutesList,
    serverPublicKey: S.String,
    slug: S.optional(S.NullOr(S.String)),
    tunnelId: S.optional(S.NullOr(S.String)),
    updatedAt: S.String,
    clientPrivateKey: S.String,
  }),
).annotate({
  identifier: "CreateTunnelResponse",
}) as any as S.Schema<CreateTunnelResponse>;

export interface DeleteTunnelRequest {
  /** Tunnel id, or your slug for it */
  tunnelIdOrSlug: string;
}
export const DeleteTunnelRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    tunnelIdOrSlug: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/v5/tunnels/{tunnelIdOrSlug}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "DeleteTunnelRequest",
}) as any as S.Schema<DeleteTunnelRequest>;

export interface DeleteTunnelResponse {}
export const DeleteTunnelResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "DeleteTunnelResponse",
}) as any as S.Schema<DeleteTunnelResponse>;

export interface DetachVpcFromTunnelRequest {
  /** Tunnel id, or your slug for it */
  tunnelIdOrSlug: string;
  /** VPC id, or your slug for it */
  vpcIdOrSlug: string;
}
export const DetachVpcFromTunnelRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    tunnelIdOrSlug: S.String.pipe(T.Label()),
    vpcIdOrSlug: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/v5/tunnels/{tunnelIdOrSlug}/vpcs/{vpcIdOrSlug}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "DetachVpcFromTunnelRequest",
}) as any as S.Schema<DetachVpcFromTunnelRequest>;

export interface GetTunnelRequest {
  /** Tunnel id, or your slug for it */
  tunnelIdOrSlug: string;
}
export const GetTunnelRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    tunnelIdOrSlug: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({ method: "GET", uri: "/v5/tunnels/{tunnelIdOrSlug}", code: 200 }),
  ),
).annotate({
  identifier: "GetTunnelRequest",
}) as any as S.Schema<GetTunnelRequest>;

export interface ListTunnelsRequest {}
export const ListTunnelsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}).pipe(T.Http({ method: "GET", uri: "/v5/tunnels", code: 200 })),
).annotate({
  identifier: "ListTunnelsRequest",
}) as any as S.Schema<ListTunnelsRequest>;

/** Newest first. Listings never carry a private key; the config returned here has `PrivateKey` blanked. */
export type ListTunnelsResponseTunnelsList = Array<Tunnel>;
export const ListTunnelsResponseTunnelsList = /*@__PURE__*/ S.Array(
  Tunnel,
) as any as S.Schema<ListTunnelsResponseTunnelsList>;

export interface ListTunnelsResponse {
  totalCount: number;
  /** Newest first. Listings never carry a private key; the config returned here has `PrivateKey` blanked. */
  tunnels: ListTunnelsResponseTunnelsList;
}
export const ListTunnelsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    totalCount: S.Number,
    tunnels: ListTunnelsResponseTunnelsList,
  }),
).annotate({
  identifier: "ListTunnelsResponse",
}) as any as S.Schema<ListTunnelsResponse>;

export interface RotateTunnelKeyRequest {
  /** Tunnel id, or your slug for it */
  tunnelIdOrSlug: string;
  /** The public key of a keypair you already hold. Omit to have a fresh one minted for you. */
  clientPublicKey?: string | null;
}
export const RotateTunnelKeyRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    tunnelIdOrSlug: S.String.pipe(T.Label()),
    clientPublicKey: S.optional(S.NullOr(S.String)),
  }).pipe(
    T.Http({
      method: "POST",
      uri: "/v5/tunnels/{tunnelIdOrSlug}/rotate-key",
      code: 200,
    }),
  ),
).annotate({
  identifier: "RotateTunnelKeyRequest",
}) as any as S.Schema<RotateTunnelKeyRequest>;

/** The private networks on the far side, oldest attachment first. */
export type RotateTunnelKeyResponseAttachmentsList = Array<TunnelAttachment>;
export const RotateTunnelKeyResponseAttachmentsList = /*@__PURE__*/ S.Array(
  TunnelAttachment,
) as any as S.Schema<RotateTunnelKeyResponseAttachmentsList>;

/** The ranges your client routes through the tunnel — its `AllowedIPs`, fixed at create. A network can only be attached if its CIDRs fall inside these. */
export type RotateTunnelKeyResponseRoutesList = Array<string>;
export const RotateTunnelKeyResponseRoutesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<RotateTunnelKeyResponseRoutesList>;

export interface RotateTunnelKeyResponse {
  /** The private networks on the far side, oldest attachment first. */
  attachments: RotateTunnelKeyResponseAttachmentsList;
  /** Your fixed addresses inside the tunnel. Only the gateway ever sees them: each attached network sees an address inside its own subnet instead (`attachments[].address`). */
  clientAddressV4: string;
  clientAddressV6: string;
  /** A complete WireGuard config file, fixed for the life of the tunnel: attaching and detaching networks never changes it, so bring it up once and manage what it reaches through the API. `PrivateKey` is blank except on create and rotate-key, the only two responses that mint a keypair. */
  clientConfig: string;
  clientPublicKey: string;
  createdAt: string;
  /** Your label for this tunnel, if you gave it one. */
  displayName?: string | null;
  /** Host to dial. */
  endpointHost?: string | null;
  endpointPort: number;
  /** Deprecated: the tunnel's id under its old name. Read `tunnelId`. */
  id: string;
  /** The ranges your client routes through the tunnel — its `AllowedIPs`, fixed at create. A network can only be attached if its CIDRs fall inside these. */
  routes: RotateTunnelKeyResponseRoutesList;
  /** The one `[Peer]` public key in your config. */
  serverPublicKey: string;
  /** Your handle for this tunnel, if you gave it one. Usable anywhere the id is, in place of it. */
  slug?: string | null;
  /** The tunnel's id, under the name the rest of the API uses for it — a firewall rule says `tunnelId`, so the tunnel does too. */
  tunnelId?: string | null;
  updatedAt: string;
  /** Returned once. Already embedded in `clientConfig`. */
  clientPrivateKey: string;
}
export const RotateTunnelKeyResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    attachments: RotateTunnelKeyResponseAttachmentsList,
    clientAddressV4: S.String,
    clientAddressV6: S.String,
    clientConfig: S.String,
    clientPublicKey: S.String,
    createdAt: S.String,
    displayName: S.optional(S.NullOr(S.String)),
    endpointHost: S.optional(S.NullOr(S.String)),
    endpointPort: S.Number,
    id: S.String,
    routes: RotateTunnelKeyResponseRoutesList,
    serverPublicKey: S.String,
    slug: S.optional(S.NullOr(S.String)),
    tunnelId: S.optional(S.NullOr(S.String)),
    updatedAt: S.String,
    clientPrivateKey: S.String,
  }),
).annotate({
  identifier: "RotateTunnelKeyResponse",
}) as any as S.Schema<RotateTunnelKeyResponse>;

export interface UpdateTunnelRequest {
  /** Tunnel id, or your slug for it */
  tunnelIdOrSlug: string;
  /** An empty string clears it. */
  displayName?: string | null;
  /** An empty string clears it. */
  slug?: string | null;
}
export const UpdateTunnelRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    tunnelIdOrSlug: S.String.pipe(T.Label()),
    displayName: S.optional(S.NullOr(S.String)),
    slug: S.optional(S.NullOr(S.String)),
  }).pipe(
    T.Http({ method: "PATCH", uri: "/v5/tunnels/{tunnelIdOrSlug}", code: 200 }),
  ),
).annotate({
  identifier: "UpdateTunnelRequest",
}) as any as S.Schema<UpdateTunnelRequest>;

export type AttachVpcToTunnelError =
  | BadRequest
  | NotFound
  | Conflict
  | FreestyleOpError;
/** Attach a private network to a tunnel. The tunnel gains an address inside the network — what that network's VMs see as your address — and a connected client can reach the network immediately, with no config change. Omit `ipv4` and an address is assigned; supply one to pin it. The network's CIDRs must fall inside the tunnel's routes and must not overlap another attached network's. */
export const attachVpcToTunnel: API.OperationMethod<
  AttachVpcToTunnelRequest,
  Tunnel,
  AttachVpcToTunnelError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: AttachVpcToTunnelRequest,
  output: Tunnel,
  errors: [BadRequest, NotFound, Conflict, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));

export type CreateTunnelError =
  | BadRequest
  | NotFound
  | Conflict
  | FreestyleOpError;
/** Create a tunnel, optionally attaching networks in the same call (`vpcs`, all-or-nothing). It lives until you delete it, and its config never changes: save it, bring it up once, and manage which networks it reaches by attaching and detaching them — no config updates, ever. Omit `clientPublicKey` and a keypair is minted for you — the response carries the only copy of the private key you will get. */
export const createTunnel: API.OperationMethod<
  CreateTunnelRequest,
  CreateTunnelResponse,
  CreateTunnelError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateTunnelRequest,
  output: CreateTunnelResponse,
  errors: [BadRequest, NotFound, Conflict, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));

export type DeleteTunnelError = NotFound | FreestyleOpError;
/** Delete a tunnel, detaching every network as it goes. This is the only thing that ends one. */
export const deleteTunnel: API.OperationMethod<
  DeleteTunnelRequest,
  DeleteTunnelResponse,
  DeleteTunnelError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteTunnelRequest,
  output: DeleteTunnelResponse,
  errors: [NotFound, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));

export type DetachVpcFromTunnelError = NotFound | FreestyleOpError;
/** Detach a private network from a tunnel: it stops being reachable and the tunnel's address inside it is released. The tunnel — and any connected client — carries on untouched. */
export const detachVpcFromTunnel: API.OperationMethod<
  DetachVpcFromTunnelRequest,
  Tunnel,
  DetachVpcFromTunnelError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DetachVpcFromTunnelRequest,
  output: Tunnel,
  errors: [NotFound, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));

export type GetTunnelError = NotFound | FreestyleOpError;
/** Read one tunnel, with its attached networks. The config comes back with a blank `PrivateKey`; fill in the key you saved at create, or rotate to mint a new one. */
export const getTunnel: API.OperationMethod<
  GetTunnelRequest,
  Tunnel,
  GetTunnelError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetTunnelRequest,
  output: Tunnel,
  errors: [NotFound, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));

export type ListTunnelsError = FreestyleOpError;
/** List your tunnels, newest first. Listings never include a private key: one is returned only by create and rotate-key. */
export const listTunnels: API.OperationMethod<
  ListTunnelsRequest,
  ListTunnelsResponse,
  ListTunnelsError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListTunnelsRequest,
  output: ListTunnelsResponse,
  errors: [UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));

export type RotateTunnelKeyError = BadRequest | NotFound | FreestyleOpError;
/** Replace a tunnel's keys, keeping its id and every attached network's address. Use this if you lost the config or need to revoke one that leaked: the old keys stop working, and the response carries the only copy of the new private key you will get. */
export const rotateTunnelKey: API.OperationMethod<
  RotateTunnelKeyRequest,
  RotateTunnelKeyResponse,
  RotateTunnelKeyError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: RotateTunnelKeyRequest,
  output: RotateTunnelKeyResponse,
  errors: [BadRequest, NotFound, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));

export type UpdateTunnelError =
  | BadRequest
  | NotFound
  | Conflict
  | FreestyleOpError;
/** Rename a tunnel or change its slug. Only the labels move: the keys, addresses, routes and attached networks are untouched, so a connected client sees nothing change and the config you saved keeps working. */
export const updateTunnel: API.OperationMethod<
  UpdateTunnelRequest,
  Tunnel,
  UpdateTunnelError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateTunnelRequest,
  output: Tunnel,
  errors: [BadRequest, NotFound, Conflict, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));
