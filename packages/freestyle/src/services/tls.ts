// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/spec-mirror-freestyle/specs/openapi.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as Redacted from "effect/Redacted";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  FreestyleProtocol,
  type FreestyleOpError,
  type FreestyleOpContext,
} from "../protocol.ts";
import { paginateOffset } from "../pagination.ts";
import { UnknownFreestyleError } from "../errors.ts";
import * as Retry from "../retry.ts";

export type { FreestyleOpError, FreestyleOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Conflict
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Conflict>()("Conflict", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withConflictError),
    [{ status: 409 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

/** Headers copied from a successful check onto the VM request. */
export type CreateTlsForwardAuthRequestAuthResponseHeadersList = Array<string>;
export const CreateTlsForwardAuthRequestAuthResponseHeadersList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<CreateTlsForwardAuthRequestAuthResponseHeadersList>;

/** Static headers added to the check. Values are write-only. */
export type CreateTlsForwardAuthRequestHeadersMap = {
  [key: string]: string | undefined;
};
export const CreateTlsForwardAuthRequestHeadersMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<CreateTlsForwardAuthRequestHeadersMap>;

/** Cookie names visible to the check but withheld from the VM. */
export type CreateTlsForwardAuthRequestProtectedCookiesList = Array<string>;
export const CreateTlsForwardAuthRequestProtectedCookiesList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<CreateTlsForwardAuthRequestProtectedCookiesList>;

export interface CreateTlsForwardAuthRequest {
  /** Headers copied from a successful check onto the VM request. */
  authResponseHeaders?: CreateTlsForwardAuthRequestAuthResponseHeadersList;
  /** Static headers added to the check. Values are write-only. */
  headers?: CreateTlsForwardAuthRequestHeadersMap;
  /** Cookie names visible to the check but withheld from the VM. */
  protectedCookies?: CreateTlsForwardAuthRequestProtectedCookiesList;
  /** Per-check deadline in milliseconds. Defaults to 1500; range 100-5000. */
  timeoutMs?: number | null;
  /** Absolute HTTPS endpoint receiving a bodyless GET before the VM request. */
  url: string;
}
export const CreateTlsForwardAuthRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    authResponseHeaders: S.optional(
      CreateTlsForwardAuthRequestAuthResponseHeadersList,
    ),
    headers: S.optional(CreateTlsForwardAuthRequestHeadersMap),
    protectedCookies: S.optional(
      CreateTlsForwardAuthRequestProtectedCookiesList,
    ),
    timeoutMs: S.optional(S.NullOr(S.Number)),
    url: S.String,
  }).pipe(T.Http({ method: "POST", uri: "/v5/tls/forward-auth", code: 200 })),
).annotate({
  identifier: "CreateTlsForwardAuthRequest",
}) as any as S.Schema<CreateTlsForwardAuthRequest>;

export type TlsForwardAuthAuthResponseHeadersList = Array<string>;
export const TlsForwardAuthAuthResponseHeadersList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<TlsForwardAuthAuthResponseHeadersList>;

export type TlsForwardAuthHeadersMap = { [key: string]: string | undefined };
export const TlsForwardAuthHeadersMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<TlsForwardAuthHeadersMap>;

export type TlsForwardAuthProtectedCookiesList = Array<string>;
export const TlsForwardAuthProtectedCookiesList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<TlsForwardAuthProtectedCookiesList>;

/** A stored forward-auth configuration. Static header values are redacted. */
export interface TlsForwardAuth {
  accountId: string;
  authResponseHeaders?: TlsForwardAuthAuthResponseHeadersList;
  createdAt: string;
  headers?: TlsForwardAuthHeadersMap;
  id: string;
  protectedCookies?: TlsForwardAuthProtectedCookiesList;
  redacted: boolean;
  timeoutMs: number;
  updatedAt: string;
  url: string;
}
export const TlsForwardAuth = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    accountId: S.String,
    authResponseHeaders: S.optional(TlsForwardAuthAuthResponseHeadersList),
    createdAt: S.String,
    headers: S.optional(TlsForwardAuthHeadersMap),
    id: S.String,
    protectedCookies: S.optional(TlsForwardAuthProtectedCookiesList),
    redacted: S.Boolean,
    timeoutMs: S.Number,
    updatedAt: S.String,
    url: S.String,
  }),
).annotate({ identifier: "TlsForwardAuth" }) as any as S.Schema<TlsForwardAuth>;

/** What a rule does with the named session it matches. Only `allow` exists today, and it is required on every rule rather than defaulted: a default would mean that the day `deny` arrives, every rule written before it silently acquires an action it never stated. */
export type TlsAction = "allow";
export const TlsAction = S.String;

/** One end of a rule: which session this side matches. A `source` says *who* may open the session (`vmId`/`vpcId`/`public: true`); a `destination` says *where it lands* (`vmId`+`port`, `host`+`port`, or `public: true` for the domain's own origin). Unknown fields are rejected rather than ignored: a rule is a security statement, and a typo that quietly widened it is the wrong failure. */
export interface TlsEndpoint {
  /** **Destination only.** Pin the session to this host instead of resolving the domain, while still presenting `domain` as the SNI. */
  host?: string | null;
  /** **Destination only.** The port the session lands on. Required with a `vmId` or `host` destination; defaults to 443 for a `public` origin. */
  port?: number | null;
  /** On a source, every publicly routable client — the open Internet dials the name. On a destination, the domain's own public origin. Only `true` is meaningful; omit the field otherwise. */
  public?: boolean | null;
  /** A single VM, by id or by your slug for it. On a source, the VM that may open the session; on a destination, the VM it lands on. Answers hold its id. */
  vmId?: string | null;
  /** Everything on one private network, by id or by your slug for it. **Source only** — a landing is one place, so a network cannot be a destination. Answers hold its id. */
  vpcId?: string | null;
}
export const TlsEndpoint = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    host: S.optional(S.NullOr(S.String)),
    port: S.optional(S.NullOr(S.Number)),
    public: S.optional(S.NullOr(S.Boolean)),
    vmId: S.optional(S.NullOr(S.String)),
    vpcId: S.optional(S.NullOr(S.String)),
  }),
).annotate({ identifier: "TlsEndpoint" }) as any as S.Schema<TlsEndpoint>;

/** A reusable authorization check attached to public HTTP ingress. */
export interface TlsForwardAuthReference {
  /** The configuration id returned by `POST /v5/tls/forward-auth`. */
  id: string;
}
export const TlsForwardAuthReference = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    id: S.String,
  }),
).annotate({
  identifier: "TlsForwardAuthReference",
}) as any as S.Schema<TlsForwardAuthReference>;

/** Case-sensitive HTTP method tokens. Omitted matches any; an empty list matches none. */
export type HttpRequestMatchMethodList = Array<string>;
export const HttpRequestMatchMethodList = /*@__PURE__*/ S.Array(
  S.String,
) as any as S.Schema<HttpRequestMatchMethodList>;

/** Exact request path. Prefix and regex matching are not supported yet. */
export interface HttpPathMatch {
  exact: string;
}
export const HttpPathMatch = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    exact: S.String,
  }),
).annotate({ identifier: "HttpPathMatch" }) as any as S.Schema<HttpPathMatch>;

/** Select which HTTP egress requests receive a rule's transforms. Included dimensions are ANDed; methods are ORed. An empty object matches all. A non-match skips every transform, without denying access to the origin. See <https://vercel.com/docs/sandbox/concepts/firewall#matchers>. */
export interface HttpRequestMatch {
  /** Case-sensitive HTTP method tokens. Omitted matches any; an empty list matches none. */
  method?: HttpRequestMatchMethodList | null;
  path?: HttpPathMatch | null;
}
export const HttpRequestMatch = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    method: S.optional(S.NullOr(HttpRequestMatchMethodList)),
    path: S.optional(S.NullOr(HttpPathMatch)),
  }),
).annotate({
  identifier: "HttpRequestMatch",
}) as any as S.Schema<HttpRequestMatch>;

/** How the edge treats the session's bytes, and therefore which of its front doors serves the rule. `http` (443) and `postgres` (5432) terminate the session, so each pairs with the transform kind that speaks it and each needs a certificate for the name. `minecraft` (25565) routes on the server address in the client's handshake and then splices — no TLS, so no certificate and no transform. The other two doors carry a terminating protocol and a splicing one at once, and a rule picks which it is. `tcp` shares 443 with `http`: match on SNI, splice the bytes, terminate nothing, so your VM's own certificate answers the handshake. `imap` and `imaps` share 993 for mail clients — `imap` terminates on a certificate issued for the name and forwards plain IMAP to the port your server listens on (143 by default), `imaps` splices to a server holding its own certificate on 993. All three splicing shapes are public ingress only. `socks5` (1080) goes the other way and is the only egress-only protocol. Your VM opens SOCKS5 to the platform edge with no authentication, names its own destination inside the request, and the edge opens the session onward through your proxy provider using the credentials a `socks5` transform carries — so the workload can use the subscription without ever holding it. Nothing is terminated: your TLS runs end to end to the real origin. The rule's `domain` is an allow-list of destinations rather than a name anything resolves, which is why `*` is allowed here and refused everywhere else. */
export type TlsProtocolDocumented =
  | "tcp"
  | "http"
  | "postgres"
  | "minecraft"
  | "imap"
  | "imaps"
  | "socks5";
export const TlsProtocolDocumented = S.String;

/** Set request headers on an `http` session. Header names are preserved (they are not secret); values are write-only. */
export type TlsTransformDocumentedCase0HeadersMap = {
  [key: string]: string | undefined;
};
export const TlsTransformDocumentedCase0HeadersMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<TlsTransformDocumentedCase0HeadersMap>;

/** Set request headers on an `http` session. Header names are preserved (they are not secret); values are write-only. */
export interface TlsTransformDocumentedCase0 {
  /** Set request headers on an `http` session. Header names are preserved (they are not secret); values are write-only. */
  headers: TlsTransformDocumentedCase0HeadersMap;
}
export const TlsTransformDocumentedCase0 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    headers: TlsTransformDocumentedCase0HeadersMap,
  }),
).annotate({
  identifier: "TlsTransformDocumentedCase0",
}) as any as S.Schema<TlsTransformDocumentedCase0>;

export type PatchOperationCase0Op = "add";
export const PatchOperationCase0Op = S.String;

/** 'add' operation */
export interface PatchOperationCase0 {
  /** JSON-Pointer value [RFC6901](https://tools.ietf.org/html/rfc6901) that references a location within the target document where the operation is performed. */
  path: string;
  /** Value to add to the target location. */
  value: unknown;
  op: PatchOperationCase0Op;
}
export const PatchOperationCase0 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    path: S.String,
    value: S.Unknown,
    op: PatchOperationCase0Op,
  }),
).annotate({
  identifier: "PatchOperationCase0",
}) as any as S.Schema<PatchOperationCase0>;

export type PatchOperationCase1Op = "remove";
export const PatchOperationCase1Op = S.String;

/** 'remove' operation */
export interface PatchOperationCase1 {
  /** JSON-Pointer value [RFC6901](https://tools.ietf.org/html/rfc6901) that references a location within the target document where the operation is performed. */
  path: string;
  op: PatchOperationCase1Op;
}
export const PatchOperationCase1 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    path: S.String,
    op: PatchOperationCase1Op,
  }),
).annotate({
  identifier: "PatchOperationCase1",
}) as any as S.Schema<PatchOperationCase1>;

export type PatchOperationCase2Op = "replace";
export const PatchOperationCase2Op = S.String;

/** 'replace' operation */
export interface PatchOperationCase2 {
  /** JSON-Pointer value [RFC6901](https://tools.ietf.org/html/rfc6901) that references a location within the target document where the operation is performed. */
  path: string;
  /** Value to replace with. */
  value: unknown;
  op: PatchOperationCase2Op;
}
export const PatchOperationCase2 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    path: S.String,
    value: S.Unknown,
    op: PatchOperationCase2Op,
  }),
).annotate({
  identifier: "PatchOperationCase2",
}) as any as S.Schema<PatchOperationCase2>;

export type PatchOperationCase3Op = "move";
export const PatchOperationCase3Op = S.String;

/** 'move' operation */
export interface PatchOperationCase3 {
  /** JSON-Pointer value [RFC6901](https://tools.ietf.org/html/rfc6901) that references a location to move value from. */
  from: string;
  /** JSON-Pointer value [RFC6901](https://tools.ietf.org/html/rfc6901) that references a location within the target document where the operation is performed. */
  path: string;
  op: PatchOperationCase3Op;
}
export const PatchOperationCase3 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    from: S.String,
    path: S.String,
    op: PatchOperationCase3Op,
  }),
).annotate({
  identifier: "PatchOperationCase3",
}) as any as S.Schema<PatchOperationCase3>;

export type PatchOperationCase4Op = "copy";
export const PatchOperationCase4Op = S.String;

/** 'copy' operation */
export interface PatchOperationCase4 {
  /** JSON-Pointer value [RFC6901](https://tools.ietf.org/html/rfc6901) that references a location to copy value from. */
  from: string;
  /** JSON-Pointer value [RFC6901](https://tools.ietf.org/html/rfc6901) that references a location within the target document where the operation is performed. */
  path: string;
  op: PatchOperationCase4Op;
}
export const PatchOperationCase4 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    from: S.String,
    path: S.String,
    op: PatchOperationCase4Op,
  }),
).annotate({
  identifier: "PatchOperationCase4",
}) as any as S.Schema<PatchOperationCase4>;

export type PatchOperationCase5Op = "test";
export const PatchOperationCase5Op = S.String;

/** 'test' operation */
export interface PatchOperationCase5 {
  /** JSON-Pointer value [RFC6901](https://tools.ietf.org/html/rfc6901) that references a location within the target document where the operation is performed. */
  path: string;
  /** Value to test against. */
  value: unknown;
  op: PatchOperationCase5Op;
}
export const PatchOperationCase5 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    path: S.String,
    value: S.Unknown,
    op: PatchOperationCase5Op,
  }),
).annotate({
  identifier: "PatchOperationCase5",
}) as any as S.Schema<PatchOperationCase5>;

/** JSON Patch single patch operation */
export type PatchOperation =
  | PatchOperationCase0
  | PatchOperationCase1
  | PatchOperationCase2
  | PatchOperationCase3
  | PatchOperationCase4
  | PatchOperationCase5;
export const PatchOperation =
  /*@__PURE__*/ S.Unknown as any as S.Schema<PatchOperation>;

/** Ordered RFC 6902 operations. Values are write-only and read back as `"***"`. A rule accepts one patch, with at most 128 operations and 64 KiB of configuration. */
export type JsonPatch = Array<PatchOperation>;
export const JsonPatch = /*@__PURE__*/ S.Array(
  PatchOperation,
) as any as S.Schema<JsonPatch>;

/** Apply RFC 6902 operations to JSON HTTP egress request bodies. Values are write-only; operation names and JSON Pointer paths remain visible. */
export interface TlsTransformDocumentedCase1 {
  /** Apply RFC 6902 operations to JSON HTTP egress request bodies. Values are write-only; operation names and JSON Pointer paths remain visible. */
  jsonPatch: JsonPatch;
}
export const TlsTransformDocumentedCase1 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    jsonPatch: JsonPatch,
  }),
).annotate({
  identifier: "TlsTransformDocumentedCase1",
}) as any as S.Schema<TlsTransformDocumentedCase1>;

/** Credentials injected into a Postgres startup handshake. The guest connects with no password; the edge completes the real handshake upstream. */
export interface PostgresTransform {
  /** The database to select. Optional: omitted leaves the guest's choice. */
  database?: string | null;
  /** The upstream password. Write-only: sealed at rest, read back as `"***"`. */
  password: string | Redacted.Redacted<string>;
  /** The upstream role to authenticate as. */
  username: string;
}
export const PostgresTransform = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    database: S.optional(S.NullOr(S.String)),
    password: S.String.pipe(T.SensitiveValue({})),
    username: S.String,
  }),
).annotate({
  identifier: "PostgresTransform",
}) as any as S.Schema<PostgresTransform>;

/** Inject credentials into a `postgres` handshake. */
export interface TlsTransformDocumentedCase2 {
  /** Inject credentials into a `postgres` handshake. */
  postgres: PostgresTransform;
}
export const TlsTransformDocumentedCase2 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    postgres: PostgresTransform,
  }),
).annotate({
  identifier: "TlsTransformDocumentedCase2",
}) as any as S.Schema<TlsTransformDocumentedCase2>;

/** Credentials presented to an upstream SOCKS5 proxy. Your VM connects to the platform's SOCKS5 front with no authentication at all; the edge authenticates to your provider on its behalf, so the subscription is never inside the VM. */
export interface Socks5Transform {
  /** The password to present to the upstream proxy. Write-only: sealed at rest, read back as `"***"`. */
  password: string | Redacted.Redacted<string>;
  /** The username to present to the upstream proxy. */
  username: string;
}
export const Socks5Transform = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    password: S.String.pipe(T.SensitiveValue({})),
    username: S.String,
  }),
).annotate({
  identifier: "Socks5Transform",
}) as any as S.Schema<Socks5Transform>;

/** Authenticate a `socks5` session to your upstream proxy provider. */
export interface TlsTransformDocumentedCase3 {
  /** Authenticate a `socks5` session to your upstream proxy provider. */
  socks5: Socks5Transform;
}
export const TlsTransformDocumentedCase3 = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    socks5: Socks5Transform,
  }),
).annotate({
  identifier: "TlsTransformDocumentedCase3",
}) as any as S.Schema<TlsTransformDocumentedCase3>;

/** The rewrites a rule applies, in order. */
export type TlsTransformDocumented =
  | TlsTransformDocumentedCase0
  | TlsTransformDocumentedCase1
  | TlsTransformDocumentedCase2
  | TlsTransformDocumentedCase3;
export const TlsTransformDocumented =
  /*@__PURE__*/ S.Unknown as any as S.Schema<TlsTransformDocumented>;

/** Rewrites applied in flight, in order. Secrets here are write-only. */
export type CreateTlsRuleRequestTransformList = Array<TlsTransformDocumented>;
export const CreateTlsRuleRequestTransformList = /*@__PURE__*/ S.Array(
  TlsTransformDocumented,
) as any as S.Schema<CreateTlsRuleRequestTransformList>;

export interface CreateTlsRuleRequest {
  /** Required, and `allow` is the only accepted value. */
  action: TlsAction | (string & {});
  /** A free-form note, up to 1024 characters. */
  description?: string | null;
  destination?: TlsEndpoint;
  /** The name the session is addressed to: exact, `*.suffix`, or `*`. */
  domain: string;
  forwardAuth?: TlsForwardAuthReference | null;
  match?: HttpRequestMatch | null;
  protocol?: TlsProtocolDocumented | (string & {}) | null;
  source?: TlsEndpoint;
  /** Rewrites applied in flight, in order. Secrets here are write-only. */
  transform?: CreateTlsRuleRequestTransformList;
}
export const CreateTlsRuleRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    action: TlsAction,
    description: S.optional(S.NullOr(S.String)),
    destination: S.optional(TlsEndpoint),
    domain: S.String,
    forwardAuth: S.optional(S.NullOr(TlsForwardAuthReference)),
    match: S.optional(S.NullOr(HttpRequestMatch)),
    protocol: S.optional(S.NullOr(TlsProtocolDocumented)),
    source: S.optional(TlsEndpoint),
    transform: S.optional(CreateTlsRuleRequestTransformList),
  }).pipe(T.Http({ method: "POST", uri: "/v5/tls", code: 200 })),
).annotate({
  identifier: "CreateTlsRuleRequest",
}) as any as S.Schema<CreateTlsRuleRequest>;

/** A resource a rule cannot outlive: deleting it deletes the rule. */
export interface TlsDependency {
  /** Your label for the named resource, under the same rules as `slug`. */
  displayName?: string | null;
  /** The resource's id. */
  id: string;
  /** `vm` or `vpc` today. New selectors add new kinds. */
  kind: string;
  /** Your handle for the named resource, when it has one — so a rule says what it points at without a lookup per id. */
  slug?: string | null;
}
export const TlsDependency = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    displayName: S.optional(S.NullOr(S.String)),
    id: S.String,
    kind: S.String,
    slug: S.optional(S.NullOr(S.String)),
  }),
).annotate({ identifier: "TlsDependency" }) as any as S.Schema<TlsDependency>;

/** What this rule depends on. Deleting any of them deletes this rule, so no rule is ever left pointing at something that no longer exists. */
export type TlsRuleDependenciesList = Array<TlsDependency>;
export const TlsRuleDependenciesList = /*@__PURE__*/ S.Array(
  TlsDependency,
) as any as S.Schema<TlsRuleDependenciesList>;

/** The rewrites this rule applies, with every secret replaced by `"***"`. */
export type TlsRuleTransformList = Array<TlsTransformDocumented>;
export const TlsRuleTransformList = /*@__PURE__*/ S.Array(
  TlsTransformDocumented,
) as any as S.Schema<TlsRuleTransformList>;

/** An allowed named session over a domain, as intent: a session addressed to `domain`, from `source`, to `destination`, with any in-flight `transform`. */
export interface TlsRule {
  action: TlsAction;
  createdAt: string;
  /** What this rule depends on. Deleting any of them deletes this rule, so no rule is ever left pointing at something that no longer exists. */
  dependencies?: TlsRuleDependenciesList;
  description?: string | null;
  destination: TlsEndpoint;
  /** The name the session is addressed to: exact, `*.suffix`, or `*`. */
  domain: string;
  forwardAuth?: TlsForwardAuthReference | null;
  id: string;
  match?: HttpRequestMatch | null;
  protocol: TlsProtocolDocumented;
  /** Whether any transform secret was redacted out of `transform`, so you know the shape you see is not the whole story. */
  redacted?: boolean;
  source: TlsEndpoint;
  /** The rewrites this rule applies, with every secret replaced by `"***"`. */
  transform?: TlsRuleTransformList;
  updatedAt: string;
}
export const TlsRule = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    action: TlsAction,
    createdAt: S.String,
    dependencies: S.optional(TlsRuleDependenciesList),
    description: S.optional(S.NullOr(S.String)),
    destination: TlsEndpoint,
    domain: S.String,
    forwardAuth: S.optional(S.NullOr(TlsForwardAuthReference)),
    id: S.String,
    match: S.optional(S.NullOr(HttpRequestMatch)),
    protocol: TlsProtocolDocumented,
    redacted: S.optional(S.Boolean),
    source: TlsEndpoint,
    transform: S.optional(TlsRuleTransformList),
    updatedAt: S.String,
  }),
).annotate({ identifier: "TlsRule" }) as any as S.Schema<TlsRule>;

export interface DeleteTlsForwardAuthRequest {
  /** forwardAuth configuration id */
  forwardAuthId: string;
}
export const DeleteTlsForwardAuthRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    forwardAuthId: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/v5/tls/forward-auth/{forwardAuthId}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "DeleteTlsForwardAuthRequest",
}) as any as S.Schema<DeleteTlsForwardAuthRequest>;

export interface DeleteTlsForwardAuthResponse {}
export const DeleteTlsForwardAuthResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "DeleteTlsForwardAuthResponse",
}) as any as S.Schema<DeleteTlsForwardAuthResponse>;

export interface DeleteTlsRuleRequest {
  /** TLS rule id */
  ruleId: string;
}
export const DeleteTlsRuleRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ruleId: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "DELETE", uri: "/v5/tls/{ruleId}", code: 200 })),
).annotate({
  identifier: "DeleteTlsRuleRequest",
}) as any as S.Schema<DeleteTlsRuleRequest>;

export interface DeleteTlsRuleResponse {}
export const DeleteTlsRuleResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "DeleteTlsRuleResponse",
}) as any as S.Schema<DeleteTlsRuleResponse>;

export interface GetTlsForwardAuthRequest {
  /** forwardAuth configuration id */
  forwardAuthId: string;
}
export const GetTlsForwardAuthRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    forwardAuthId: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/v5/tls/forward-auth/{forwardAuthId}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "GetTlsForwardAuthRequest",
}) as any as S.Schema<GetTlsForwardAuthRequest>;

export interface GetTlsRuleRequest {
  /** TLS rule id */
  ruleId: string;
}
export const GetTlsRuleRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ruleId: S.String.pipe(T.Label()),
  }).pipe(T.Http({ method: "GET", uri: "/v5/tls/{ruleId}", code: 200 })),
).annotate({
  identifier: "GetTlsRuleRequest",
}) as any as S.Schema<GetTlsRuleRequest>;

export interface ListTlsForwardAuthRequest {}
export const ListTlsForwardAuthRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}).pipe(
    T.Http({ method: "GET", uri: "/v5/tls/forward-auth", code: 200 }),
  ),
).annotate({
  identifier: "ListTlsForwardAuthRequest",
}) as any as S.Schema<ListTlsForwardAuthRequest>;

/** Newest first. */
export type ListTlsForwardAuthResponseConfigsList = Array<TlsForwardAuth>;
export const ListTlsForwardAuthResponseConfigsList = /*@__PURE__*/ S.Array(
  TlsForwardAuth,
) as any as S.Schema<ListTlsForwardAuthResponseConfigsList>;

export interface ListTlsForwardAuthResponse {
  /** Newest first. */
  configs: ListTlsForwardAuthResponseConfigsList;
  totalCount: number;
}
export const ListTlsForwardAuthResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    configs: ListTlsForwardAuthResponseConfigsList,
    totalCount: S.Number,
  }),
).annotate({
  identifier: "ListTlsForwardAuthResponse",
}) as any as S.Schema<ListTlsForwardAuthResponse>;

export interface ListTlsRulesRequest {
  /** Rules that apply to this VM: those naming it, plus those naming a private network it is attached to. Its id, or your slug for it. */
  vmId?: string;
  /** Rules naming this private network. Its id, or your slug for it. */
  vpcId?: string;
  /** Rules for exactly this domain, as written on the rule (e.g. `app.acme.com` or `*.acme.com`). */
  domain?: string;
  limit?: number;
  offset?: number;
}
export const ListTlsRulesRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    vmId: S.optional(S.String.pipe(T.Query())),
    vpcId: S.optional(S.String.pipe(T.Query())),
    domain: S.optional(S.String.pipe(T.Query())),
    limit: S.optional(S.Number.pipe(T.Query())),
    offset: S.optional(S.Number.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/v5/tls", code: 200 })),
).annotate({
  identifier: "ListTlsRulesRequest",
}) as any as S.Schema<ListTlsRulesRequest>;

/** Newest first. */
export type ListTlsRulesResponseRulesList = Array<TlsRule>;
export const ListTlsRulesResponseRulesList = /*@__PURE__*/ S.Array(
  TlsRule,
) as any as S.Schema<ListTlsRulesResponseRulesList>;

export interface ListTlsRulesResponse {
  /** Newest first. */
  rules: ListTlsRulesResponseRulesList;
  totalCount: number;
}
export const ListTlsRulesResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    rules: ListTlsRulesResponseRulesList,
    totalCount: S.Number,
  }),
).annotate({
  identifier: "ListTlsRulesResponse",
}) as any as S.Schema<ListTlsRulesResponse>;

/** Headers copied from a successful check onto the VM request. */
export type UpdateTlsForwardAuthRequestAuthResponseHeadersList = Array<string>;
export const UpdateTlsForwardAuthRequestAuthResponseHeadersList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<UpdateTlsForwardAuthRequestAuthResponseHeadersList>;

/** Static headers added to the check. Values are write-only. */
export type UpdateTlsForwardAuthRequestHeadersMap = {
  [key: string]: string | undefined;
};
export const UpdateTlsForwardAuthRequestHeadersMap = /*@__PURE__*/ S.Record(
  S.String,
  S.String,
) as any as S.Schema<UpdateTlsForwardAuthRequestHeadersMap>;

/** Cookie names visible to the check but withheld from the VM. */
export type UpdateTlsForwardAuthRequestProtectedCookiesList = Array<string>;
export const UpdateTlsForwardAuthRequestProtectedCookiesList =
  /*@__PURE__*/ S.Array(
    S.String,
  ) as any as S.Schema<UpdateTlsForwardAuthRequestProtectedCookiesList>;

export interface UpdateTlsForwardAuthRequest {
  /** forwardAuth configuration id */
  forwardAuthId: string;
  /** Headers copied from a successful check onto the VM request. */
  authResponseHeaders?: UpdateTlsForwardAuthRequestAuthResponseHeadersList;
  /** Static headers added to the check. Values are write-only. */
  headers?: UpdateTlsForwardAuthRequestHeadersMap;
  /** Cookie names visible to the check but withheld from the VM. */
  protectedCookies?: UpdateTlsForwardAuthRequestProtectedCookiesList;
  /** Per-check deadline in milliseconds. Defaults to 1500; range 100-5000. */
  timeoutMs?: number | null;
  /** Absolute HTTPS endpoint receiving a bodyless GET before the VM request. */
  url: string;
}
export const UpdateTlsForwardAuthRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    forwardAuthId: S.String.pipe(T.Label()),
    authResponseHeaders: S.optional(
      UpdateTlsForwardAuthRequestAuthResponseHeadersList,
    ),
    headers: S.optional(UpdateTlsForwardAuthRequestHeadersMap),
    protectedCookies: S.optional(
      UpdateTlsForwardAuthRequestProtectedCookiesList,
    ),
    timeoutMs: S.optional(S.NullOr(S.Number)),
    url: S.String,
  }).pipe(
    T.Http({
      method: "PUT",
      uri: "/v5/tls/forward-auth/{forwardAuthId}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "UpdateTlsForwardAuthRequest",
}) as any as S.Schema<UpdateTlsForwardAuthRequest>;

/** Rewrites applied in flight, in order. Secrets here are write-only. */
export type UpdateTlsRuleRequestTransformList = Array<TlsTransformDocumented>;
export const UpdateTlsRuleRequestTransformList = /*@__PURE__*/ S.Array(
  TlsTransformDocumented,
) as any as S.Schema<UpdateTlsRuleRequestTransformList>;

export interface UpdateTlsRuleRequest {
  /** TLS rule id */
  ruleId: string;
  /** Required, and `allow` is the only accepted value. */
  action: TlsAction | (string & {});
  /** A free-form note, up to 1024 characters. */
  description?: string | null;
  destination?: TlsEndpoint;
  /** The name the session is addressed to: exact, `*.suffix`, or `*`. */
  domain: string;
  forwardAuth?: TlsForwardAuthReference | null;
  match?: HttpRequestMatch | null;
  protocol?: TlsProtocolDocumented | (string & {}) | null;
  source?: TlsEndpoint;
  /** Rewrites applied in flight, in order. Secrets here are write-only. */
  transform?: UpdateTlsRuleRequestTransformList;
}
export const UpdateTlsRuleRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    ruleId: S.String.pipe(T.Label()),
    action: TlsAction,
    description: S.optional(S.NullOr(S.String)),
    destination: S.optional(TlsEndpoint),
    domain: S.String,
    forwardAuth: S.optional(S.NullOr(TlsForwardAuthReference)),
    match: S.optional(S.NullOr(HttpRequestMatch)),
    protocol: S.optional(S.NullOr(TlsProtocolDocumented)),
    source: S.optional(TlsEndpoint),
    transform: S.optional(UpdateTlsRuleRequestTransformList),
  }).pipe(T.Http({ method: "PUT", uri: "/v5/tls/{ruleId}", code: 200 })),
).annotate({
  identifier: "UpdateTlsRuleRequest",
}) as any as S.Schema<UpdateTlsRuleRequest>;

export type CreateTlsForwardAuthError =
  | BadRequest
  | Conflict
  | FreestyleOpError;
/** Create a reusable authorization check for public HTTP ingress. Freestyle sends a bodyless GET to the HTTPS endpoint before waking or forwarding to the VM. Static header values are write-only and sealed at rest. */
export const createTlsForwardAuth: API.OperationMethod<
  CreateTlsForwardAuthRequest,
  TlsForwardAuth,
  CreateTlsForwardAuthError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateTlsForwardAuthRequest,
  output: TlsForwardAuth,
  errors: [BadRequest, Conflict, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));

export type CreateTlsRuleError =
  | BadRequest
  | NotFound
  | Conflict
  | FreestyleOpError;
/** Allow a named session over a domain: a session addressed to `domain`, from `source`, to `destination`. A `source` says who may open it (`vmId`/`vpcId`/`public: true`); a `destination` says where it lands (`vmId`+`port`, `host`+`port`, or `public: true` for the domain's own origin). A `transform` rewrites the session in flight — setting a header, or completing a Postgres handshake with credentials the guest never holds; its secrets are write-only and read back as `"***"`. A rule naming a VM or private network is deleted automatically when that resource is. */
export const createTlsRule: API.OperationMethod<
  CreateTlsRuleRequest,
  TlsRule,
  CreateTlsRuleError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: CreateTlsRuleRequest,
  output: TlsRule,
  errors: [BadRequest, NotFound, Conflict, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));

export type DeleteTlsForwardAuthError = NotFound | Conflict | FreestyleOpError;
/** Delete an unused authorization check. A configuration referenced by a TLS rule cannot be deleted. */
export const deleteTlsForwardAuth: API.OperationMethod<
  DeleteTlsForwardAuthRequest,
  DeleteTlsForwardAuthResponse,
  DeleteTlsForwardAuthError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteTlsForwardAuthRequest,
  output: DeleteTlsForwardAuthResponse,
  errors: [NotFound, Conflict, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));

export type DeleteTlsRuleError = NotFound | FreestyleOpError;
/** Delete a TLS rule. The resources it named are untouched — the dependency runs one way. */
export const deleteTlsRule: API.OperationMethod<
  DeleteTlsRuleRequest,
  DeleteTlsRuleResponse,
  DeleteTlsRuleError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteTlsRuleRequest,
  output: DeleteTlsRuleResponse,
  errors: [NotFound, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));

export type GetTlsForwardAuthError = NotFound | FreestyleOpError;
/** Fetch one reusable authorization check. Static header values read back as `"***"`. */
export const getTlsForwardAuth: API.OperationMethod<
  GetTlsForwardAuthRequest,
  TlsForwardAuth,
  GetTlsForwardAuthError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetTlsForwardAuthRequest,
  output: TlsForwardAuth,
  errors: [NotFound, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));

export type GetTlsRuleError = NotFound | FreestyleOpError;
/** Fetch one TLS rule. Transform secrets read back as `"***"`. */
export const getTlsRule: API.OperationMethod<
  GetTlsRuleRequest,
  TlsRule,
  GetTlsRuleError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetTlsRuleRequest,
  output: TlsRule,
  errors: [NotFound, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));

export type ListTlsForwardAuthError = FreestyleOpError;
/** List your reusable HTTP authorization checks, newest first. */
export const listTlsForwardAuth: API.OperationMethod<
  ListTlsForwardAuthRequest,
  ListTlsForwardAuthResponse,
  ListTlsForwardAuthError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: ListTlsForwardAuthRequest,
  output: ListTlsForwardAuthResponse,
  errors: [UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));

export type ListTlsRulesError = NotFound | FreestyleOpError;
/** List your TLS rules, newest first. Filter by `vmId` to get the rules that apply to one VM — those naming it, plus those naming a private network it is attached to — by `vpcId` for the rules naming a private network, or by `domain` for the rules written on exactly that name. */
export const listTlsRules: API.PaginatedOperationMethod<
  ListTlsRulesRequest,
  ListTlsRulesResponse,
  ListTlsRulesError,
  FreestyleOpContext,
  TlsRule
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListTlsRulesRequest,
    output: ListTlsRulesResponse,
    errors: [NotFound, UnknownFreestyleError],
    protocol: FreestyleProtocol,
    retry: Retry.Retry,
    pagination: {
      inputToken: "offset",
      outputToken: "totalCount",
      items: "rules",
      pageSize: "limit",
    } as const,
  }),
  paginateOffset,
) as any;

export type UpdateTlsForwardAuthError =
  | BadRequest
  | NotFound
  | FreestyleOpError;
/** Replace a reusable authorization check in place, including rotating its static headers. Rules keep referencing the same id. */
export const updateTlsForwardAuth: API.OperationMethod<
  UpdateTlsForwardAuthRequest,
  TlsForwardAuth,
  UpdateTlsForwardAuthError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateTlsForwardAuthRequest,
  output: TlsForwardAuth,
  errors: [BadRequest, NotFound, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));

export type UpdateTlsRuleError = BadRequest | NotFound | FreestyleOpError;
/** Replace a TLS rule in place — the one mutation this API allows, so a rotating secret can change without dropping traffic. The body is a whole rule, exactly as create takes it. */
export const updateTlsRule: API.OperationMethod<
  UpdateTlsRuleRequest,
  TlsRule,
  UpdateTlsRuleError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateTlsRuleRequest,
  output: TlsRule,
  errors: [BadRequest, NotFound, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));
