// AUTO-GENERATED by scripts/generate.ts from .generated-specs (specs/spec-mirror-freestyle/specs/openapi.json). Do not edit.
import * as S from "@distilled.cloud/core/schema";
import * as API from "@distilled.cloud/core/api";
import * as C from "@distilled.cloud/core/category";
import * as T from "../traits.ts";
import {
  FreestyleProtocol,
  type FreestyleOpError,
  type FreestyleOpContext,
} from "../protocol.ts";
import { paginateOffset } from "../pagination.ts";
import { UnknownFreestyleError } from "../errors.ts";
import * as Retry from "../retry.ts";

export type { FreestyleOpError, FreestyleOpContext };

export class BadRequest
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<BadRequest>()("BadRequest", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 400 }],
  ) {}

export class Conflict
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<Conflict>()("Conflict", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withConflictError),
    [{ status: 409 }],
  ) {}

export class NotFound
  extends /*@__PURE__*/ T.applyErrorMatchers(
    /*@__PURE__*/ S.TaggedError<NotFound>()("NotFound", {
      code: S.Number,
      message: S.String,
    }).pipe(C.withBadRequestError),
    [{ status: 404 }],
  ) {}

export interface DeleteSnapshotRequest {
  /** Snapshot id or your slug for it */
  snapshotIdOrSlug: string;
}
export const DeleteSnapshotRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    snapshotIdOrSlug: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "DELETE",
      uri: "/v5/snapshots/{snapshotIdOrSlug}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "DeleteSnapshotRequest",
}) as any as S.Schema<DeleteSnapshotRequest>;

export interface DeleteSnapshotResponse {}
export const DeleteSnapshotResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({}),
).annotate({
  identifier: "DeleteSnapshotResponse",
}) as any as S.Schema<DeleteSnapshotResponse>;

export interface GetSnapshotRequest {
  /** Snapshot id, your slug, or `{owner}/{slug}` for a public one */
  snapshotIdOrSlug: string;
}
export const GetSnapshotRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    snapshotIdOrSlug: S.String.pipe(T.Label()),
  }).pipe(
    T.Http({
      method: "GET",
      uri: "/v5/snapshots/{snapshotIdOrSlug}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "GetSnapshotRequest",
}) as any as S.Schema<GetSnapshotRequest>;

/** A snapshot as its owner sees it. The disk/memory layer tree, replication targets, and originating manager are storage internals and are not exposed. */
export interface Snapshot {
  /** Whether `autoDeleteSeconds` above is your plan's doing rather than yours, under the same rule as on a VM. */
  autoDeleteFromPlan?: boolean;
  /** Delete the snapshot once this long has passed without a VM being created from it. Absent means never. Some plans cap this and the cap shows up here. */
  autoDeleteSeconds?: number | null;
  createdAt: string;
  displayName?: string | null;
  id: string;
  /** When a VM was last created from this snapshot. Absent if none ever has been — which is not the same as never having been touched, since renaming a snapshot is not using it. */
  lastUsedAt?: string | null;
  /** Public snapshots are bootable by any account. */
  public?: boolean;
  slug?: string | null;
  /** The source VM's display name now, under the same rules. */
  sourceVmDisplayName?: string | null;
  sourceVmId?: string | null;
  /** What the source VM is called *now*; absent when it has no slug and when it is gone. */
  sourceVmSlug?: string | null;
  /** Delete the snapshot this long after it was taken, however recently anything booted it. */
  ttlSeconds?: number | null;
  updatedAt: string;
}
export const Snapshot = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    autoDeleteFromPlan: S.optional(S.Boolean),
    autoDeleteSeconds: S.optional(S.NullOr(S.Number)),
    createdAt: S.String,
    displayName: S.optional(S.NullOr(S.String)),
    id: S.String,
    lastUsedAt: S.optional(S.NullOr(S.String)),
    public: S.optional(S.Boolean),
    slug: S.optional(S.NullOr(S.String)),
    sourceVmDisplayName: S.optional(S.NullOr(S.String)),
    sourceVmId: S.optional(S.NullOr(S.String)),
    sourceVmSlug: S.optional(S.NullOr(S.String)),
    ttlSeconds: S.optional(S.NullOr(S.Number)),
    updatedAt: S.String,
  }),
).annotate({ identifier: "Snapshot" }) as any as S.Schema<Snapshot>;

export interface ListSnapshotsRequest {
  /** Only snapshots taken from this VM. */
  sourceVmId?: string;
  search?: string;
  limit?: number;
  offset?: number;
}
export const ListSnapshotsRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    sourceVmId: S.optional(S.String.pipe(T.Query())),
    search: S.optional(S.String.pipe(T.Query())),
    limit: S.optional(S.Number.pipe(T.Query())),
    offset: S.optional(S.Number.pipe(T.Query())),
  }).pipe(T.Http({ method: "GET", uri: "/v5/snapshots", code: 200 })),
).annotate({
  identifier: "ListSnapshotsRequest",
}) as any as S.Schema<ListSnapshotsRequest>;

export type ListSnapshotsResponseSnapshotsList = Array<Snapshot>;
export const ListSnapshotsResponseSnapshotsList = /*@__PURE__*/ S.Array(
  Snapshot,
) as any as S.Schema<ListSnapshotsResponseSnapshotsList>;

export interface ListSnapshotsResponse {
  snapshots: ListSnapshotsResponseSnapshotsList;
  totalCount: number;
}
export const ListSnapshotsResponse = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    snapshots: ListSnapshotsResponseSnapshotsList,
    totalCount: S.Number,
  }),
).annotate({
  identifier: "ListSnapshotsResponse",
}) as any as S.Schema<ListSnapshotsResponse>;

export interface SnapshotVmRequest {
  /** VM id, or your slug for it */
  vmIdOrSlug: string;
  /** Delete the snapshot once this many seconds pass without a VM being created from it. Every create from it resets the clock. -1 (or omitting this) means never; some plans cap this, and on those omitting it (or sending -1) gets you the cap. */
  autoDeleteSeconds?: number | null;
  displayName?: string | null;
  slug?: string | null;
  /** Delete the snapshot this many seconds after it is taken, whatever has booted it since. -1 (or omitting this) means no deadline. */
  ttlSeconds?: number | null;
}
export const SnapshotVmRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    vmIdOrSlug: S.String.pipe(T.Label()),
    autoDeleteSeconds: S.optional(S.NullOr(S.Number)),
    displayName: S.optional(S.NullOr(S.String)),
    slug: S.optional(S.NullOr(S.String)),
    ttlSeconds: S.optional(S.NullOr(S.Number)),
  }).pipe(
    T.Http({ method: "POST", uri: "/v5/vms/{vmIdOrSlug}/snapshot", code: 200 }),
  ),
).annotate({
  identifier: "SnapshotVmRequest",
}) as any as S.Schema<SnapshotVmRequest>;

export interface SnapshotCreated {
  snapshot: Snapshot;
  snapshotId: string;
  sourceVmId?: string | null;
}
export const SnapshotCreated = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    snapshot: Snapshot,
    snapshotId: S.String,
    sourceVmId: S.optional(S.NullOr(S.String)),
  }),
).annotate({
  identifier: "SnapshotCreated",
}) as any as S.Schema<SnapshotCreated>;

export interface UpdateSnapshotRequest {
  /** Snapshot id or your slug for it */
  snapshotIdOrSlug: string;
  /** Seconds of nothing being created from it before the snapshot is deleted; -1 removes the window. On a plan that caps it, -1 puts you back on the cap rather than off it. */
  autoDeleteSeconds?: number | null;
  displayName?: string | null;
  slug?: string | null;
  /** Seconds from when the snapshot was taken — not from now — before it is deleted; -1 removes the deadline. */
  ttlSeconds?: number | null;
}
export const UpdateSnapshotRequest = /*@__PURE__*/ S.suspend(() =>
  S.Struct({
    snapshotIdOrSlug: S.String.pipe(T.Label()),
    autoDeleteSeconds: S.optional(S.NullOr(S.Number)),
    displayName: S.optional(S.NullOr(S.String)),
    slug: S.optional(S.NullOr(S.String)),
    ttlSeconds: S.optional(S.NullOr(S.Number)),
  }).pipe(
    T.Http({
      method: "PATCH",
      uri: "/v5/snapshots/{snapshotIdOrSlug}",
      code: 200,
    }),
  ),
).annotate({
  identifier: "UpdateSnapshotRequest",
}) as any as S.Schema<UpdateSnapshotRequest>;

export type DeleteSnapshotError = NotFound | Conflict | FreestyleOpError;
/** Delete a snapshot permanently. VMs already created from it are unaffected — a snapshot is a pointer to the data backing them, not the data itself — and its slug is freed immediately. */
export const deleteSnapshot: API.OperationMethod<
  DeleteSnapshotRequest,
  DeleteSnapshotResponse,
  DeleteSnapshotError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: DeleteSnapshotRequest,
  output: DeleteSnapshotResponse,
  errors: [NotFound, Conflict, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));

export type GetSnapshotError = NotFound | FreestyleOpError;
/** Fetch a snapshot by its id or its slug. */
export const getSnapshot: API.OperationMethod<
  GetSnapshotRequest,
  Snapshot,
  GetSnapshotError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: GetSnapshotRequest,
  output: Snapshot,
  errors: [NotFound, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));

export type ListSnapshotsError = FreestyleOpError;
/** List your snapshots. */
export const listSnapshots: API.PaginatedOperationMethod<
  ListSnapshotsRequest,
  ListSnapshotsResponse,
  ListSnapshotsError,
  FreestyleOpContext,
  Snapshot
> = /*@__PURE__*/ API.makePaginated(
  () => ({
    input: ListSnapshotsRequest,
    output: ListSnapshotsResponse,
    errors: [UnknownFreestyleError],
    protocol: FreestyleProtocol,
    retry: Retry.Retry,
    pagination: {
      inputToken: "offset",
      outputToken: "totalCount",
      items: "snapshots",
      pageSize: "limit",
    } as const,
  }),
  paginateOffset,
) as any;

export type SnapshotVmError = NotFound | Conflict | FreestyleOpError;
/** Capture the VM's exact state, memory and disk, so a VM booted from it resumes exactly where this one was. The VM must be running or paused. New snapshots are private, and this call returns after the snapshot is fully materialized. */
export const snapshotVm: API.OperationMethod<
  SnapshotVmRequest,
  SnapshotCreated,
  SnapshotVmError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: SnapshotVmRequest,
  output: SnapshotCreated,
  errors: [NotFound, Conflict, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));

export type UpdateSnapshotError =
  | BadRequest
  | NotFound
  | Conflict
  | FreestyleOpError;
/** Rename a snapshot or change its slug. */
export const updateSnapshot: API.OperationMethod<
  UpdateSnapshotRequest,
  Snapshot,
  UpdateSnapshotError,
  FreestyleOpContext
> = /*@__PURE__*/ API.make(() => ({
  input: UpdateSnapshotRequest,
  output: Snapshot,
  errors: [BadRequest, NotFound, Conflict, UnknownFreestyleError],
  protocol: FreestyleProtocol,
  retry: Retry.Retry,
}));
