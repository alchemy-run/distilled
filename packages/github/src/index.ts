/**
 * @distilled.cloud/github — GitHub REST API SDK for Effect.
 *
 * `./services` is generated by `scripts/generate.ts` from the Smithy models in
 * `.generated-specs` (written by `scripts/convert.ts` from GitHub's own
 * OpenAPI description submodule). Everything else in this folder is
 * hand-written.
 */
export * from "./credentials.ts";
export * from "./errors.ts";
export * as T from "./traits.ts";
export {
  GithubProtocol,
  API_VERSION,
  type GithubOpError,
  type GithubOpContext,
} from "./protocol.ts";
export * as Retry from "./retry.ts";
// Every tag except `credentials`, which collides with the auth helper
// exported from ./credentials.ts. The Credentials API is
// `@distilled.cloud/github/credentials`.
export {
  actions,
  activity,
  agentTasks,
  agents,
  apps,
  billing,
  campaigns,
  checks,
  codeQuality,
  codeScanning,
  codeSecurity,
  codesOfConduct,
  codespaces,
  copilotSpaces,
  copilot,
  dependabot,
  dependencyGraph,
  emojis,
  enterpriseTeamMemberships,
  enterpriseTeamOrganizations,
  enterpriseTeams,
  gists,
  git,
  gitignore,
  hostedCompute,
  interactions,
  issues,
  licenses,
  markdown,
  meta,
  migrations,
  oidc,
  orgs,
  packages,
  privateRegistries,
  projects,
  pulls,
  rateLimit,
  reactions,
  repos,
  search,
  secretScanning,
  securityAdvisories,
  teams,
  users,
} from "./services/index.ts";
